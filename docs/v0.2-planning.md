# v0.2.0 Planning

This document summarises the v0.2.0 milestone. The authoritative task list is
Phase 8 (Tasks 141–149) in `moves-rust-migration-plan.md`.

## What v0.2.0 is

v0.1.0 shipped a complete set of calculator implementations but did not produce
real emission rows — the data-plane wiring connecting the Parquet default DB to
the live calculator context was not yet complete. v0.2.0's defining objective is:

> **`moves run` produces emission rows that pass the canonical diff gate.**

Everything else in v0.2.0 builds on or supports that goal.

## Milestone definition

v0.2.0 is ready to tag when:

1. `moves run --runspec SampleRunSpec.xml` produces a non-empty `MOVESOutput.parquet`.
2. All 34 non-scale fixtures pass the canonical diff gate in CI.
3. `scale-county`, `scale-project`, and `scale-rates` are in the active regression suite
   and pass.
4. The WASM demo is live at the GitHub Pages URL.
5. Real (not framework-overhead) performance benchmarks are documented.

## Task summary

| Task | Title | Owner | Weeks |
|------|-------|-------|-------|
| 141 | Data-plane wiring — default-scale onroad | — | 2–3 |
| 142 | Data-plane wiring — NONROAD | — | 1 |
| 143 | Activate canonical regression gate | — | 1–2 |
| 144 | County Scale and Project Scale wiring | — | 2 |
| 145 | WASM GitHub Pages deployment | — | 0.5 |
| 146 | Performance characterization | — | 0.5–1 |
| 147 | EPA MOVES annual update | — | 1 (est.) |
| 148 | RunSpec coverage expansion | — | 2 |
| 149 | v0.2.0 release | — | 0.5 |
| **Total** | | | **~10 weeks** |

Tasks 141–148 may be parallelised across two engineers. Task 149 is serial.

## Priority order

1. **Task 141** (onroad wiring) — blocks 143, 146, 148.
2. **Task 142** (NONROAD wiring) — blocks 143.
3. **Task 143** (regression gate) — blocks 149.
4. **Task 144** (Scale wiring) — blocks 149.
5. **Tasks 145–148** — independent; run in parallel with 143–144.

## v0.1 feedback addressed

| Feedback / gap | Address by |
|----------------|-----------|
| `moves run` produces no emission rows | Tasks 141–142 |
| Regression gate is dormant | Task 143 |
| Scale fixtures excluded from CI | Task 144 |
| No hosted WASM demo URL | Task 145 |
| Performance numbers are placeholder | Task 146 |
| No exercise of upstream-tracking workflow | Task 147 |
| Narrow fixture coverage | Task 148 |

## What v0.2 does NOT include

- GPU or distributed-execution targets (explicitly out of scope)
- GUI tooling
- Regulatory validation
- Uncertainty / Monte Carlo
- Any calculator that wasn't already shipped in v0.1 (scope creep risk — file a
  bead and target v0.3 if a missing calculator surfaces from community feedback)
