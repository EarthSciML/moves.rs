# Canonical-fidelity audit report

Two-pass automated audit of the moves.rs workspace against pinned canonical
**EPA MOVES5.0.1 Java + NONROAD2008a Fortran** (`EPA_MOVES_Model` @ `25dc6c8`).

- Pass 1: 66 finder agents over all 408 source/test files → **174 raw findings**.
- Pass 2: 92 fix agents (one per file) checked each finding against the canonical
  source and applied changes. Branch: `audit/canonical-fidelity-fixes`.

## Summary

| action | count |
|---|---|
| fixed | 49 |
| hardened | 85 |
| no-change-correct | 24 |
| no-change-uncertain | 0 |
| deferred-cross-file | 17 |

## Fidelity fixes (numeric/behavior divergence corrected) (49)

- **crates/moves-calculators/src/calculators/nonroad_loader.rs** — FINDING 5: build_temporal_factors defaults missing month/day allocation fractions to 1.0 — ref: NONROAD2008a daymthf.f:99-119 (loads defmth/defday on no match); rdseas.f:215-221 (defmth=1.0/12.0, defday=1./7.); nonrdtpl.inc:64-68.
  - Genuine numeric divergence. Canonical daymthf.f:99-119 loads defmth(i)/defday(i) (NOT 1.0) for an SCC with no seasonality match, and rdseas.f:215-221 sets defmth=1.0/12.0 and defday=1./7.. The loader defaulted BOTH dimensions to 1.0, over-allocating: monthFraction=1.0 dumps the whole year into one month (should be 1/12), and dayFraction=1.0 makes 7*dayFraction=7 (should be 7*(1/7)=1). I added a per-map `default` parameter to the lookup closure and call it with 1.0/12.0 for the month map and 1.0/7.0 for the day map, matching canonical defmth/defday; updated the doc comment. No signature change; flows through nonroad_emission.rs:250 production output.
- **crates/moves-nonroad/src/emissions/evaporative.rs** — Displacement day-adjustment indexes day 1 only, dropping the per-day (jday-1) offset — ref: NONROAD2008a clcevems.f:322-324 (temiss = temiss * adjems(idxspc,jday) under ldayfl && iprtyp==IDXANN)
  - clcevems.f:324 computes `temiss = temiss * adjems(idxspc,jday)`, indexing the adjustment array by the current day-of-year. The Rust displacement_branch recomputed its own index as `dis_slot * MXDAYS` with no `(jday-1)` term, so every annual+daily day reused day 1's factor. Fixed by threading the per-day `adjems` value (already computed correctly in the main loop at `slot*MXDAYS + (jday-1)` for the Displacement slot) into displacement_branch and multiplying by it, removing the duplicate fixed-offset lookup. This makes displacement consistent with every other species branch and matches the reference per-day adjustment.
- **crates/moves-calculators/src/calculators/airtoxics.rs** — FINDING 2: synthesize_fuel_supply returns an empty map when any source table is absent, silently disabling the AT*FuelSupply formulation expansion — ref: /tmp/canonical/moves-java/calc/mwo/mworeader.go readMOVESWorkerOutput lines 514-530 (FuelSupply[FuelSupplyKey{...}]; if fsDetail == nil { continue }); /tmp/canonical/moves-java/database/AirToxicsCalculator.sql UseATRatioGas1 (INNER JOIN AT1FuelSupply, lines ~570-575)
  - The real divergence was the `None => vec![Emission { ...row.fuel_sub_type_id... }]` pass-through in execute (formerly lines 2433-2438): when a formulation-0 worker row's (year,month,fuelType) cell has no fuel supply, the port passed the block through with fuelSubTypeID=0/fuelFormulationID=0. Canonical MOVES does NOT do this — the Go MWO reader (mworeader.go readMOVESWorkerOutput, lines 515-530) does `fsDetail := FuelSupply[...]; if fsDetail == nil { continue }`, i.e. it DROPS the whole block. The port's pass-through made the fuelType-keyed PAH gas/particle paths emit toxics off a block canonical produces nothing for. I restructured the loop: a row that already carries a concrete formulation (engine never produces one, kept for robustness) is taken verbatim; otherwise it is split over the fuel supply, and a missing cell now `continue`s (drops the block) exactly like the canonical reader. I did NOT change synthesize_fuel_supply's empty-map-on-absent-table return: with the loop fix, an empty map now correctly drops every formulation-0 row (= canonical 'no fuel supply -> no AT emissions'), and canonical MOVES likewise silently skips rather than erroring on absent fuel supply, so erroring would itself diverge.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs** — FINDING 3 [medium/silent-fallback] Missing context County row falls back to GPAFract = 0.0 instead of dropping all rows — ref: /tmp/canonical/moves-java/database/CriteriaStartCalculator.sql lines 339-358 (FROM County c inner join ... ifnull(ratio,1) + GPAFract * (ifnull(ratioGPA,1)-ifnull(ratio,1)) ... WHERE c.countyID = ##context.iterLocation.countyRecordID##)
  - The canonical CSEC 2-a is `FROM County c inner join PollutantProcessAssoc ppa inner join FuelFormulation ff inner join SourceTypeModelYear stmy ... WHERE c.countyID = ##context.iterLocation.countyRecordID##`, with County being the lead INNER join. If the context county has no County row, the entire CountyFuelAdjustment cartesian product collapses to zero rows and FuelSupplyAdjustment is empty. The prior `map_or(0.0, ...)` instead blended GPAFract=0.0 and kept producing (wrong, non-empty) fuel adjustments. Changed fuel_supply_adjustment to early-return Vec::new() when the context county row is absent, reproducing inner-join semantics, and updated the comment (the previous comment falsely claimed a LEFT-JOIN-style default).
- **crates/moves-calculators/src/calculators/hcspeciation.rs** — FINDING 1: speciate_nmog_or_voc returns None on a missing HCSpeciation row, diverging from the Go reference — ref: /tmp/canonical/moves-java/calc/hcspeciation/hcspeciation.go lines 262-268, 280-286, 319-325, 338-344 (else { NewEmissionScaled(e,0) }); /tmp/canonical/moves-java/database/HCSpeciationCalculator.sql lines 413-420 (pre-fill missing HCSpeciation with speciationConstant=0,oxySpeciation=0)
  - The port returned None (drop row) on a missing HCSpeciation profile, justified by an asserted SQL INNER-JOIN exclusion. That justification is wrong: BOTH canonical implementations emit a ZERO, not a dropped row. The Go worker (the documented port target) does `emissions[80/87] = NewEmissionScaled(e, 0)` in the `hcs == nil` branch (hcspeciation.go lines 266-268, 284-286 for the THC path; 323-325, 342-344 for the altTHC path). The legacy SQL is not a counterexample either: before its INNER JOIN to HCSpeciation it runs `insert ignore into HCSpeciation (...) select ... 0.0 as speciationConstant, 0.0 as oxySpeciation` to 'Fill in missing HCSpeciation entries so that joins to the table are valid' (HCSpeciationCalculator.sql lines 413-420), so the join always matches and yields NMHC*0 = 0 — the same zero. I changed the function to return `Some(thc_emission.scaled(0.0))` (the original THC emission scaled by 0, carrying its fuel ids, exactly the Go's NewEmissionScaled(e,0)) instead of None. Renamed the previously-unused `_thc_emission` param to `thc_emission` and rewrote the comment. This also restores the Go's TOG (86) = NMOG + methane row-production semantics (NewEmissionSum), since NMOG is now present-as-zero. The function's own doc-comment (lines 581-583) and module algorithm note (lines 36-37) already described this zero behavior and are now consistent with the code again.
- **crates/moves-calculators/src/calculators/multiday_tank_vapor_venting_calculator.rs** — FINDING 1 — Missing RVP-adjustment terms silently default to a unit (1.0) adjustment instead of dropping the row like the reference INNER JOIN — ref: /tmp/canonical/moves-java/database/MultidayTankVaporVentingCalculator.sql lines 1870 and 1949 (`inner join averageTankGasoline atg on (atg.fuelTypeID = FuelType.fuelTypeID)`), plus the ATG rebuild INNER JOINs at lines 1770-1779
  - Replaced the fabricated `unwrap_or(AdjustTerms{..constant:1.0})` fallback with a `let Some(rvp) = rvp_terms.get(&sb.fuel_type_id).copied() else { continue; }` that drops the row. The canonical operating / hot-soak insert in MultidayTankVaporVentingCalculator.sql joins `inner join averageTankGasoline atg on (atg.fuelTypeID = FuelType.fuelTypeID)` (lines 1870 and 1949) for BOTH op modes 150 and 300 — an INNER JOIN. Because the ATG table is itself rebuilt via an INNER JOIN on evapRVPTemperatureAdjustment knots (line 1770/1778-1779), any fuel type lacking an RVP knot or an ATG row is absent from averageTankGasoline and therefore excluded from the insert entirely. rvp_adjustment_terms() reproduces exactly that join (the two `continue`s at the missing-ATG and missing-knot cases), so an absent entry means the reference emits NO row for that fuel type/source bin. The prior code kept the row with a unit adjustment, changing op-mode-300 emission magnitude and hiding the data gap. The justifying 'worker ATG table is always empty' comment was also false: execute() (lines ~5318-5327) loads AverageTankGasoline from scratch or the slow tier. Note this only affects operating_mode_base_rates() (opModes 150/300); the cold-soak insert (opMode 151), which the reference does NOT join to ATG, lives in the separate `for htvv in hourly` loop and is untouched.
- **crates/moves-calculators/src/calculators/so2_calculator.rs** — generalFuelRatio synthesis hardcodes GPAFract=0 and never reads fuelEffectRatioGPA, silently dropping the GPA blend term — ref: /tmp/canonical/moves-java/database/SO2Calculator.sql lines 103-124 (aggregation + County join); CreateDefault.sql lines 862-875 (generalFuelRatio schema: fuelEffectRatioGPA, minAgeID, maxAgeID)
  - Confirmed against SO2Calculator.sql lines 103-124: the pre-aggregation is sum((ifnull(fuelEffectRatio,1)+GPAFract*(ifnull(fuelEffectRatioGPA,1)-ifnull(fuelEffectRatio,1)))*marketShare), with GPAFract from the run county's County row (inner join County c on c.countyID = ##context...countyRecordID##) and fuelEffectRatioGPA a real column of the runtime generalFuelRatio table (CreateDefault.sql line 874). Added min/max age and fuelEffectRatioGPA to RawGeneralFuelRatioRow (struct, polars_schema, into_dataframe, from_dataframe; both ratios NULL->1 via ifnull). Added a CountyGpaRow TableRow reading GPAFract, added "County" to INPUT_TABLES, and rewrote the synthesis loop to compute blended = base + gpa_fract*(gpa - base) weighted by marketShare, mirroring criteria_running_calculator.rs:3222.
- **crates/moves-calculators/src/calculators/so2_calculator.rs** — generalFuelRatio synthesis ignores minAgeID/maxAgeID age window and assumes all FuelSupply rows belong to the run fuel year — ref: /tmp/canonical/moves-java/database/SO2Calculator.sql lines 109-124; ageID definition from BaseRateCalculator.sql line 245
  - Confirmed the SQL aggregates over RunSpecModelYearAge with gfr.minAgeID <= mya.ageID and gfr.maxAgeID >= mya.ageID (SO2Calculator.sql lines 120-121) and restricts FuelSupply via inner join Year y on y.yearID = mya.yearID then fs.fuelYearID = y.fuelYearID (lines 109-111). Because ageID = yearID - modelYearID (canonical relationship, e.g. BaseRateCalculator.sql line 245 'AND ageID = ##context.year## - modelYearID'), the per-(modelYear,year) age is determined, so I gate inline with age_id = year - model_year against [min_age_id,max_age_id]. Added a run_fuel_years set built from the Year table filtered to the run year, and skip FuelSupply rows whose fuel_year_id is not in it.
- **crates/moves-calculators/src/calculators/baseratecalculator/setup.rs** — EVEfficiency table schema is a placeholder mapping the wrong columns into the lookup key — ref: /tmp/canonical/moves-java/calc/baseratecalculator/baseratecalculator.go:196-198,648-661; /tmp/canonical/moves-java/database/BaseRateCalculator.sql:205-225,38-48; docs/MOVESDatabaseTables.md:576-589
  - The raw `evefficiency` DB table (read via InputDataManager MergeTableSpec::new("evefficiency")) has columns polProcessID, sourceTypeID, regClassID, ageGroupID, beginModelYearID, endModelYearID, batteryEfficiency, chargingEfficiency (confirmed in MOVESDatabaseTables.md:582-589 and CreateDefault.sql:647). The canonical worker file the Go reads (evefficiencyworker) has polProcessID, sourceTypeID, regClassID, fuelTypeID, modelYearID, batteryEfficiency, chargingEfficiency and is built by BaseRateCalculator.sql:205-225 which forces fuelTypeID=9 and expands model years via an agecategory join. The old loader stuffed ageGroupID into fuel_type_id and beginModelYearID into model_year_id, dropped endModelYearID and the real fuelTypeID, so an enabled BRC_evefficiency flag would key the divisor on garbage. Fixed: EvEfficiencyRow now carries the real raw columns (age_group_id, begin_model_year_id, end_model_year_id); polars_schema/into_dataframe/from_dataframe read/write them (added the missing endModelYearID column); and the SQL transform is performed faithfully in PreparedTables::from_inputs (see next finding).
- **crates/moves-calculators/src/calculators/baseratecalculator/setup.rs** — EVEfficiency does not expand the beginModelYearID..endModelYearID range, unlike EmissionRateAdjustment — ref: /tmp/canonical/moves-java/database/BaseRateCalculator.sql:205-225; baseratecalculator.go:196-198,1292; setup.rs IMCoverage expansion at from_inputs (constants.year_id / prepared.age_groups)
  - Rewrote the from_inputs EVEfficiency loop to mirror BaseRateCalculator.sql:205-225: for each raw row it iterates model_year_id over [max(beginModelYearID, year-40) ..= min(endModelYearID, year)], applies the agecategory join (skips any model year where age_groups[year_id - model_year_id] != row.age_group_id), forces fuel_type_id = 9 (electricity), and inserts one PolProcSourceRegFuelMyKey entry per surviving model year. This matches the EmissionRateAdjustment/IMCoverage expansion pattern already in this function and the Go EVEfficiencyKey{polProcessID, sourceTypeID, regClassID, fuelTypeID, modelYearID}. RunConstants.year_id and prepared.age_groups (populated from inputs.age_category earlier in from_inputs) are both already available, exactly as the IMCoverage block uses them.
- **crates/moves-calculators/src/generators/link_op_mode_distribution.rs** — FINDING 1: Drive schedule not filtered to current link (.filter(|_| true) no-op) — ref: LinkOperatingModeDistributionGenerator.java calculateOpModeFractionsCore() lines 1008-1015: `from driveScheduleSecondLink a ... where a.linkID=` + linkID; also calculateOpModeFractions() line 325 `select count(*) from driveScheduleSecondLink where linkID=`
  - Replaced the no-op `.filter(|_| true)` with a real `.filter(|r| r.link_id == link_id)`. This mirrors the canonical Java core SQL which restricts every drive-schedule read to the current link via `where a.linkID = <linkID>`. The fix depends on FINDING 2 (adding the link_id column to DriveScheduleSecond). Without it, a multi-link Project-domain run aggregated every link's second-by-second trace into the current link's op-mode fractions.
- **crates/moves-calculators/src/generators/link_op_mode_distribution.rs** — FINDING 2: DriveScheduleSecond schema omits linkID — ref: database/CreateDefault.sql lines 430-437: `create table driveScheduleSecondLink ( linkID integer not null, secondID smallint not null, speed float null, grade float not null default 0.0, primary key (linkID, secondID)...)`; DriveScheduleSecondLinkImporter / importer-pdb tables header `linkID,secondID,speed,grade`
  - Added `link_id: i32` field to the DriveScheduleSecond struct, added `linkID` (Int32) as the first column in polars_schema(), added the column to into_dataframe(), and read/extract it in from_dataframe() (erroring on null like the other non-null columns). Also threaded link_id through default_idle_drive_schedule(link_id) so the synthesised idle schedule carries the real link id, matching Java's INSERT `(linkID, secondID, speed, grade)`. Updated all in-file test constructors/call sites to keep the file compiling. The canonical DDL keys the table on (linkID, secondID) with linkID NOT NULL.
- **crates/moves-calculators/src/generators/link_op_mode_distribution.rs** — FINDING 3: NULL linkAvgSpeed routes to unimplemented interpolation and silently emits no rows — ref: LinkOperatingModeDistributionGenerator.java calculateOpModeFractions() lines 341-368 and 381: `select linkAvgSpeed from link...; averageSpeed = executeScalar(...); if(averageSpeed <= 0){ insert default 30s idle schedule } ... else if(!hasRunningOpModeDistribution && averageSpeed > 0) interpolateOpModeFractions(...)`
  - Changed the dispatch condition from `link_avg_speed.is_some_and(|s| s <= 0.0)` to `link_avg_speed.is_none_or(|s| s <= 0.0)`, so a missing `link` row or SQL-NULL `linkAvgSpeed` (both collapse to None) now takes the idle-schedule path instead of falling through to the empty interpolation stub. This matches Java: `averageSpeed = SQLRunner.executeScalar(select linkAvgSpeed...)` yields 0.0 when the column is NULL or the row is absent, so `if(averageSpeed <= 0)` is true and the 30-second idle default schedule is synthesised. Only a genuinely positive linkAvgSpeed (Some(s>0)) still routes to the deferred interpolation path, preserving the existing `op_mode_distribution_empty_for_interpolation_path` test (Some(35.0)). Closes the silent zero-emissions hole without touching numerically-correct branches.
- **crates/moves-calculators/src/generators/source_bin_distribution_generator.rs** — fuelYearID falls back to the calendar year when the Year table lacks the row — ref: SourceBinDistributionGenerator.java:651 (doCountyYear: `f.fuelYearID = (select fuelYearID from year where yearID=`+year+`)`)
  - The Java doCountyYear (line 651) resolves fuelYearID via `f.fuelYearID = (select fuelYearID from year where yearID=year)`. If the Year row is absent the SQL subquery yields NULL and matches nothing — it never substitutes the calendar yearID. The Rust `.unwrap_or(year_i64)` fabricated the calendar year as the fuel year, a distinct identifier that would almost never equal the real fuelYearID in fuelUsageFraction, silently producing an empty (no-fuel-switching) county-year distribution. Replaced the fallback with `.ok_or_else(|| Error::Polars(...))?` so a missing Year row surfaces as an error, matching the established missing-input guard pattern at lines 1756-1764. Numerically-correct behavior (row present) is unchanged.
- **crates/moves-calculators/src/generators/source_bin_distribution_generator.rs** — Model-year range derived from the whole Year table, not RunSpec-selected years, with a silent fallback range — ref: SourceBinDistributionGenerator.java:539-552,585-586 (doFirstTime); ExecutionRunSpec.java:68,224,420-422 (years from RunSpec, Year table unfiltered); moves-framework/src/calculator/traits.rs:99-226 (CalculatorContext has no RunSpec-years accessor)
  - Two sub-issues. (a) Whole-Year-table vs RunSpec years: Java doFirstTime (line 541) derives first/lastModelYearNeeded from `ExecutionRunSpec.theExecutionRunSpec.years` (the RunSpec-selected run years), and ExecutionRunSpec.java:224 explicitly notes the Year table is NOT filtered. The Rust generator cannot access RunSpec-selected years — CalculatorContext (traits.rs) exposes only `tables()`, `scratch()`, and `position()`, with no run-spec/years accessor — so correcting the source of years requires a different file. Deferred via followups and documented in a code comment. (b) Empty-Year fallback: the Java fallback to 1966/2060 (lines 585-586) fires only from the SQL `catch` block; an empty year set would throw on `yrs.first()` rather than reach the fallback. The Rust `.unwrap_or(FALLBACK_MODEL_YEAR_RANGE)` would silently install 1966..2060 on a legitimately-empty Year table, masking a data-load fault. Replaced with `.ok_or_else(|| Error::Polars(...))?` so an empty Year table errors. FALLBACK_MODEL_YEAR_RANGE remains pub and used by tests, so no unused-const warning.
- **crates/moves-calculators/src/generators/mesoscale_lookup/total_activity.rs** — weeks_in_month hard-coded to 1.0 because MonthOfAnyYear was deliberately left out of the input table set — ref: MesoscaleLookupTotalActivityGenerator.java calculateVMTByRoadwayHour (lines 854-895); WeeksInMonthHelper.java getWeeksPerMonth (lines 58-67) and getWeeksPerMonthSQLClause (78-107); CreateDefault.sql MonthofAnyYear (lines 1505-1512, noOfDays SMALLINT)
  - The Java MesoscaleLookupTotalActivityGenerator.calculateVMTByRoadwayHour divides VMT*monthFrac*dayFrac*hourFrac by weeksPerMonthClause = WeeksInMonthHelper.getWeeksPerMonthSQLClause, which emits a per-month CASE of noOfDays/7.0 read from MonthOfAnyYear (e.g. 31/7=4.43 for January). getWeeksPerMonth only falls back to 1 when the table is empty/unloadable or noOfDays<=0. The port hard-coded 1.0 for every month, collapsing a per-month-varying divisor to a constant and distorting cross-month apportionment. Fix: added "monthOfAnyYear" to INPUT_TABLES; defined a local MonthOfAnyYearRow (monthID, noOfDays) TableRow; read the table in execute(); built weeks_per_month = noOfDays/7.0 per month (with the Java <=0/null -> 1.0 fallback); and used the per-month value in apportion_to_hour. Updated the execute() doc comment and the in-file execute test to supply a monthOfAnyYear row (noOfDays=7 -> weeks=1.0, preserving its single-cell numerics).
- **crates/moves-cli/src/run.rs** — SnapshotFilter adds +1 to RunSpec month, off-by-one vs. the rest of the codebase and real MOVES — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/runspec/RunSpecXML.java:2035 (writes <month id=m.monthID/>, no 0-indexing); cross-checked moves-runspec/src/xml_format.rs:863 and run.rs build_runspec_tables:1173-1180
  - Removed the `+ 1`. Canonical MOVES stores monthID as 1-12 and serializes the RunSpec month key verbatim from `m.monthID` (RunSpecXML.java:2035 writes `<month id=""+m.monthID/>`). This repo's own XML parser (moves-runspec/src/xml_format.rs:863) stores `<month key>` unchanged into timespan.months, and both RunSpecFilters::from_runspec (i64::from(m)) and build_runspec_tables (m as i32) pass it through with no offset. Only SnapshotFilter shifted it, narrowing the zonemonthhour/countyyear Polars predicate to the wrong month (July->August) and silently zeroing meteorology/county-year for the requested month. Changed map to `i64::from(m)` with no +1 and updated the comment.
- **crates/moves-cli/src/run.rs** — Test pins the fabricated month off-by-one as 'correct' — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/runspec/RunSpecXML.java:2035; consistency with run.rs:2413-2426
  - Updated the assertion in snapshot_filter_derives_zone_from_county from vec![8i64] to vec![7i64] with a corrected message. The test now matches the corrected production code and is consistent with build_runspec_tables_month_group_fallback_to_month_id (run.rs:2424) which already expects monthID 7 unchanged.
- **crates/moves-framework/src/aggregation/output_aggregate.rs** — Rolled-up sourceTypeID is carried through from an arbitrary representative row instead of collapsing to NULL when sourceTypeID is not a group key — ref: AggregationSQLGenerator.java:1353-1366 (`null as sourceTypeID` when !sourceUseType; groupBy += sourceTypeID only when true)
  - Both build_emission_output and build_activity_output hardcoded source_type_id: rep.source_type_id, which carried an arbitrary representative value even when sourceTypeID was not a GROUP BY key (the source_use_type=false case, where plan.rs add_optional_dimension pushes AggregationColumn::Null("sourceTypeID") and rows of different source types merge into one group). The canonical AggregationSQLGenerator.java (lines 1353-1366) emits `null as sourceTypeID` for the master/worker/activity SELECTs when !outputEmissionsBreakdownSelection.sourceUseType, and only adds sourceTypeID to the GROUP BY when sourceUseType is true (`0 as sourceTypeID` is used solely for BaseRateOutput, which this module does not build). Replaced both with keep_opt(kept("sourceTypeID"), rep.source_type_id) so sourceTypeID collapses to NULL exactly like every other non-key dimension, and rewrote the misleading justifying comment.
- **crates/moves-framework/src/aggregation/output_aggregate.rs** — Test asserts the fabricated non-NULL sourceTypeID, baking in the divergent behavior — ref: AggregationSQLGenerator.java:1353-1366
  - The non_key_dimensions_collapse_to_null test assertion was updated to assert row.source_type_id == None (this specific edit was applied by the linter/user mid-session but matches the required fix). Additionally added two new tests that pin the corrected contract: source_type_collapses_to_null_when_not_grouped feeds two records with different sourceTypeIDs (21 and 31) into a source_use_type=false Year/Nation plan and asserts they merge into one group with source_type_id == None; source_type_kept_when_source_use_type_breakdown_on asserts they stay distinct with their values preserved when the breakdown is on. Also added scaling_plan_with_identity_placeholder_is_rejected to lock in the Finding 2 hardening. Updated the remaining onroad success-path tests (which carry WeeksPerMonth/PortionOfWeekPerDay scaling) from &UnitScaling to a new IdentityRealFactors test provider (returns 1.0 but reports supplies_temporal_factors()==true) so they exercise clean sums without tripping the new guard.
- **crates/moves-nonroad/src/input/gxr.rs** — Malformed .GXR lines silently skipped, leaving fabricated 1.0 — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdgxrf.f lines 82,98-99,137-144 (read ... ERR=7001 -> ierr=IFAIL)
  - Replaced the `if parts.len() < 3 { continue; }` skip in read_gxr (lines ~125) with a returned Error::Parse. In rdgxrf.f a short/garbled record triggers the read ERR=7001 path, which sets ierr=IFAIL and aborts; it never silently skips. Because read_gxr pre-fills the array with 1.0, silently skipping left those [county,equipment,year] cells at 1.0 (no extrapolation), corrupting projected populations. Now a truncated record surfaces a parse error at the correct line number.
- **crates/moves-nonroad/src/input/gxr.rs** — read_gxr_records swallows bad indices and bad year factors — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdgxrf.f lines 98-99 (read(line(23:27),'(F5.0)',ERR=7001)) and 137-144 (error path sets ierr=IFAIL)
  - In read_gxr_records: (a) the `parts.len() < 3` skip now returns Error::Parse; (b) the `Err(_) => continue` arms for county/equipment index parsing now return Error::Parse; (c) the `val_str.parse().unwrap_or(1.0)` for year factors now uses `.map_err(...)?` to propagate Error::Parse. Added `line_num += 1;` at the top of the record loop (line_num was already declared mut and incremented in the header-skip loop) so error line numbers are accurate. This matches rdgxrf.f, where a non-numeric F5.0 factor read or a bad record jumps to ERR=7001 and fails, and makes read_gxr_records consistent with read_gxr. Fabricating 1.0 silently turned corrupt data into 'no growth'.
- **crates/moves-avft/src/tool.rs** — KnownFractions minimum-boundary clamp silently no-ops when the default row is absent (inconsistent with proportional path) — ref: AVFTTool.sql AVFTTool_Projection_KnownFractions, lines 506-515 (enforce minimum boundary UPDATE, inner join to defaultavft on fuelTypeID/engTechID/modelYearID); cf. AVFTTool_Projection_Proportional lines 416-424
  - Replaced the `.unwrap_or(*frac)` fabricated default with an `if let Some(def) = default.get(...)` guard, so the boundary clamp only fires when a matching default row exists. The canonical SQL `UPDATE outputavft o, defaultavft d ... WHERE o.fuelTypeID=d.fuelTypeID AND o.engTechID=d.engTechID AND o.modelYearID=d.modelYearID AND o.fuelEngFraction < d.fuelEngFraction/@boundaryRatioLimit AND o.isKnownFraction='N'` is an INNER join: when no matching default row exists, no update row is produced and the clamp does not apply. The old code substituted the row's own value as the default (min=frac/2), which can never fire AND fabricates a default the SQL never computes; it was also inconsistent with the sibling `enforce_minimum_boundary` (proportional path) which already uses `if let Some(d) = def`. Now both Rust implementations match each other and the SQL JOIN semantics.
- **crates/moves-avft/src/tool.rs** — Constant projection fabricates 0.0 for (fuel,eng) future keys missing from the gap-filled last_my baseline — ref: AVFTTool.sql AVFTTool_Projection_Constant, lines 334-340 (INSERT ... FROM inputGapFilledAVFT i join defaultavft d USING(...), inner join, fraction = i.fuelEngFraction, no renormalization)
  - Replaced `baseline.get(&(fuel,eng)).copied().unwrap_or(0.0)` with a `let Some(frac) = baseline.get(&(fuel,eng)).copied() else { continue; }` so unmatched future (fuel,eng) keys are dropped, not zero-filled. The canonical SQL `INSERT INTO outputAVFT SELECT i.sourceTypeID, d.modelYearID, i.fuelTypeID, i.engTechID, i.fuelEngFraction FROM inputGapFilledAVFT i join defaultavft d USING (sourceTypeID, fuelTypeID, engTechID) WHERE i.modelYearID = lastCompleteModelYearIN AND d.modelYearID BETWEEN ...` is an INNER join keyed on (sourceTypeID, fuelTypeID, engTechID): a default future (fuel,eng) with no last_my baseline match yields no output row at all. The previous LEFT-join-with-0 behavior could inject spurious 0.0 rows for (fuel,eng) combinations introduced in projection years, changing fleet fractions. Note the Constant SQL does not renormalize the projection years, so the emitted baseline fractions are carried verbatim; dropping unmatched keys (rather than zero-filling) is the faithful behavior.
- **crates/moves-calculators/src/calculators/criteria_running_calculator.rs** — Module/method docs describe execute() as an empty skeleton, but it is fully wired and runs the calculation — ref: Self-referential: execute() body at lines 4387-4461 and test at line 4873 (execute_returns_nonempty_dataframe_for_minimal_inputs) in the same file; calculator declared at MONTH granularity / EMISSION_CALCULATOR priority line 4254.
  - Rewrote the stale module-level doc (formerly lines 156-167, claiming execute 'cannot yet read the input tables nor emit MOVESWorkerOutput' and is a 'documented shell returning an empty CalculatorOutput') and the method-level doc (formerly line 4380, 'skeleton — returns an empty CalculatorOutput'). The actual execute() materialises inputs from ctx.tables()/ctx.position(), calls calculate(), and emits real rows via emit_rows; the test execute_returns_nonempty_dataframe_for_minimal_inputs confirms it runs and produces rows. New docs state execute is a live emission path that must be validated against BaseRateCalculator, and note that the required ##context## substitutions are now surfaced as errors rather than defaulted, so reviewers do not mistake this for a no-op that cannot corrupt output. Documentation-only change; no behavior change.
- **crates/moves-calculators/src/calculators/liquid_leaking_calculator.rs** — Top-level algorithm docstring omits the /noOfRealDays divisor that the code applies — ref: /tmp/canonical/moves-java/database/LiquidLeakingCalculator.sql line 387 (no /noOfRealDays); TankVaporVentingCalculator.sql line 705; TotalActivityGenerator.java line 1985 (SHP includes noOfRealDays)
  - I updated the module docstring to match the code: the top-level Algorithm formula (line 28-30) now reads `weightedMeanBaseRate × sourceHours × opModeFraction ÷ noOfRealDays`, and the LL-9 narrative (line 64-66) now states the division explicitly. IMPORTANT NUANCE: I verified against canonical SQL and the divisor is NOT a docstring-vs-code question in the direction the finding assumed. Canonical LiquidLeakingCalculator.sql LL-9 (line 387: `(weightedMeanBaseRate*sourceHours*opModeFraction) as emissionQuant`) has NO `/noOfRealDays` and never joins DayOfAnyWeek for it; the same is true of TankVaporVentingCalculator.sql (line 705). SourceHours is already a per-month multi-day total (TotalActivityGenerator.java line 1985: `SHP = (population*noOfRealDays) - SUM(sho)`, and SHO from VMT*...*noOfRealDays), and this port reproduces that (totalactivitygenerator/activity.rs line 554 `shp: population * no_of_real_days - sho_total`). So canonical emits monthly totals with NO division here. The port's `/no_of_real_days` divisor is therefore a divergence from canonical — but it is applied identically and intentionally across ALL THREE evap calculators in this port (liquid_leaking, tank_vapor_venting line 3746, multiday_tank_vapor_venting line 5042) and is documented in the siblings' docstrings. Removing it would be a repo-wide numeric change across multiple files (out of this single-file scope) and the cross-calculator consistency strongly implies a deliberate per-day output convention with a downstream consumer. I therefore made the docstring faithful to this file's actual computation (the scoped ask) and flagged the canonical-divergence question as a followup rather than silently altering emission magnitudes.
- **crates/moves-calculators/src/calculators/nonroad_emission.rs** — DEFAULT_YEAR = 2020 substituted when iteration position carries no year — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/nonroad/NonroadEmissionCalculator.java:457-481 (doesProcessContext: returns false when context.year<=0 || context.monthID<=0 || context.dayID<=0)
  - Removed the const DEFAULT_YEAR = 2020 and the `time.year...unwrap_or(DEFAULT_YEAR)` fabrication. Canonical NonroadEmissionCalculator.doesProcessContext (lines 457-481) returns false — i.e. does NOT execute the calculator for that context — whenever year<=0, monthID<=0, or dayID<=0. The Zone-Year-Month-Day key is mandatory; a missing temporal field is a 'skip this context' signal, not a defaultable value. Fabricating 2020 and running the entire NONROAD simulation for a guessed year diverged from canonical (which would have skipped). I replaced the fallback with a guard mirroring doesProcessContext: if time.year, time.month, or time.day_id is None, the adapter returns Ok(CalculatorOutput::empty()) (the no-op that corresponds to doesProcessContext returning false) instead of substituting a year. The existing execute_with_empty_context_succeeds test still passes (empty context -> guard -> empty output). Also updated the now-stale module doc comment (which described an obsolete fully-stubbed empty-inputs/empty-output state) to describe the wired behavior and the new skip/error semantics.
- **crates/moves-calculators/src/generators/project_tag.rs** — execute_returns_empty_output_as_stub asserts the broken stub behavior as if it were correct — ref: ProjectTAG.java:142/:255; pattern guidance from rates_op_mode_distribution execute_writes_*_to_scratch test
  - The test locked in the no-op contract (asserting execute produced no DataFrame), giving false confidence. Renamed to execute_errors_until_project_domain_inputs_are_wired and changed it to assert execute now returns Err(Error::NotImplemented), matching the hardened behavior. Added a comment pointing to the canonical executeLoop behavior and instructing that, once wiring lands, this should assert the SHO/SHP/Starts/HotellingHours/SourceHours scratch tables are populated (per the finding's reference_concern). Error is in scope via `use super::*` re-exporting the top-of-file `moves_framework::Error` import, so the test compiles.
- **crates/moves-calculators/src/generators/tank_fuel_generator.rs** — NULL RVP / ETOHVolume silently coerced to 0.0 in FuelFormulation row extraction — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/ghg/TankFuelGenerator.java:236-248,257-272,304-314,343-354; /tmp/canonical/moves-java/database/CreateDefault.sql:693-699
  - The default-DB columns are declared `RVP FLOAT NULL` / `ETOHVolume FLOAT NULL` (CreateDefault.sql:696,698), so MySQL NULL semantics govern. The Java TankFuelGenerator (TFG-1a, lines 236-248) filters TFGUsedFuelFormulation only on subjectToEvapCalculations='Y', marketShare>0, region and fuelYear — it never filters NULL RVP/ETOHVolume, and the averages are computed as separate `sum(RVP*marketShare)/sum(marketShare)`, `sum(ETOHVolume*marketShare)/sum(marketShare)`, `sum(gasPortionRVP*marketShare)/sum(marketShare)` (lines 307-309). In MySQL each SUM() skips terms whose operand is NULL while the shared `sum(marketShare)` denominator still counts the row; `gasPortionRVP` itself is NULL whenever RVP or ETOHVolume is NULL (line 271), so its term is skipped — NOT fabricated. The prior `unwrap_or(0.0)` matched MySQL for the RVP and ETOH numerators but DIVERGED for the gasPortionRVP numerator (it fed a fabricated nonzero value computed from RVP=0 instead of skipping the term) and could not distinguish a real 0.0 from a NULL. Fix: changed `FuelFormulationRow.rvp` / `.etoh_volume` to `Option<f64>`, preserved the NULL through `from_dataframe` (`rvp_col.get(i)` instead of `.unwrap_or(0.0)`), made `gas_portion_rvp(Option,Option)->Option` propagate NULL, and made the weighted-sum accumulation skip NULL operands per-column while still counting marketShare in the denominator — faithfully replicating MySQL SUM() NULL-skip. The gasohol 4<=ETOHVolume<=20 test now also excludes NULL (SQL comparison with NULL is not matched). On valid data (evap-subject gasoline always has non-NULL RVP/ETOHVolume) numerics are unchanged; the change only affects the bad/edge-NULL case, where it now matches canonical MySQL instead of corrupting the commingled averages with a fabricated zero. into_dataframe builds nullable Float64 series; test constructors updated to Some(...). FuelFormulationRow here is private to this module (every other calculator defines its own local copy), so no cross-file impact.
- **crates/moves-calculators/src/generators/tank_fuel_generator.rs** — Module docs describe execute() as an empty data-plane stub, but it is fully wired and computing — ref: In-file: TankFuelGenerator::execute (this file) wired to calculate_average_tank_gasoline and write_scratch_table
  - Confirmed execute() (now ~lines 1675-1738) reads all input tables from CalculatorContext, calls calculate_average_tank_gasoline, and writes AverageTankGasoline to scratch via crate::wiring::write_scratch_table — it is not a no-op. Replaced the stale '# Data-plane deferral' doc section that claimed `Generator::execute returns an empty output` with a '# Execution wiring' section describing the actual live path, so a reviewer is not misled into thinking the emission-affecting logic is still deferred. No code behavior changed; documentation only.
- **crates/moves-calculators/src/generators/tank_temperature_generator.rs** — User-input override tables hardcoded empty in the real execute path — ref: gov/epa/otaq/moves/master/implementation/ghg/TankTemperatureGenerator.java steps 260 (lines 1588-1633 AverageTankTemperatureKeys; 1785 SoakActivityFraction isUserInput='Y'; 2359 ColdSoakInitialHourFraction isUserInput='Y'; DELETE isUserInput='N' at 181-189)
  - execute() hardcoded prior_average_tank_temperature / prior_soak_activity_fraction / prior_cold_soak_initial_hour_fraction to Vec::new(), so the blocking sets in calculate_average_tank_temperature (line ~2177), calculate_soak_activity_fraction (~2361) and calculate_cold_soak_initial_hour_fractions (~2646) were always empty and the generator overwrote every user-supplied row. Canonical MOVES (TankTemperatureGenerator.java steps 260, lines 1588-1633, 1785, 2359) builds AverageTankTemperatureKeys/SoakActivityFractionKeys/ColdSoakInitialHourFractionKeys via `SELECT DISTINCT ... WHERE isUserInput='Y'` and LEFT JOINs them with `WHERE ...Keys IS NULL` so user-input keys are excluded from regeneration; it only DELETEs the isUserInput='N' rows (lines 181-189). These three tables are merged into the execution DB by input_data_manager.rs (MergeTableSpec entries at 1096-1098, 1106-1109, 1389). Fix: added the three tables to INPUT_TABLES so the framework loads them, and changed execute() to read them via ctx.tables().iter_typed_or_empty("...")? (the crate's standard optional-table loader, returns empty when the user supplied no overrides). The existing is_user_input filter in the calculate_* functions then preserves exactly the isUserInput='Y' keys, matching canonical. Used iter_typed_or_empty (already imported via DataFrameStoreTyped, used the same way in baseratecalculator/mod.rs) and the row types' existing TableRow impls — compiles with existing types/traits.
- **crates/moves-calculators/src/generators/tank_temperature_generator.rs** — Stale module docstring claims execute() is an empty placeholder while it is fully wired
  - Replaced the '# Data-plane deferral' module-doc section (claiming execute returns an empty output and that wiring is pending) with a '# Data-plane wiring' section describing the now-complete execute path, and updated the matching struct doc-comment line that called execute 'an empty stand-in until the data plane lands'. The new docs also explicitly note that the user-input override rows are loaded and that their isUserInput='Y' keys are excluded from regeneration, so the data-flow is documented rather than masked.
- **crates/moves-default-db-convert/src/validate.rs** — Float aggregate 'sum' check is lossy (1e9-scaled, rounded) despite docs claiming exact, order-deterministic equality — ref: crates/moves-default-db-convert/src/validate.rs scale_f64_to_i128 lines 723-728; float min/max bit-pattern compare lines 792-814; inline rationale lines 575-579; field_equal Float64 to_bits compare lines 460-467. Self-contained harness, no external canonical.
  - The module doc (lines 18-22) claimed Float64 columns are validated by 'exact equality ... we sum in a deterministic primary-key order so no floating-point reordering can creep in.' The implementation (scale_f64_to_i128 at lines 723-728 plus wrapping_add in absorb_batch/absorb_tsv_row) is order-INDEPENDENT (wrapping_add over a HashMap of columns, not PK-ordered) and collapses any value below ~1e-9 to 0, so for MOVES emission-rate columns the float-sum comparison is effectively 0==0. The genuinely load-bearing exact checks are count_non_null and float_min_bits/float_max_bits, which compare exact f64 bit patterns (lines 792-814) and DO catch tiny-magnitude divergences. Rather than rewrite a defensible, intentional implementation (its rationale is documented inline at lines 575-579), I corrected the misleading doc to: (a) state the float sum is a coarse, order-independent corroborating signal that deliberately drops sub-1e-9 magnitudes, (b) state count/min/max (exact bits) are the load-bearing fidelity checks including for tiny-rate columns, and (c) warn reviewers not to rely on the float sum for small-rate fidelity. This removes the false confidence the audit flagged without changing numerically-correct behavior.
- **crates/moves-framework/src/execution/engine.rs** — Output-breakdown flags hardcoded to false, ignoring RunSpec population/nonroad selections — ref: OutputEmissionsBreakdownSelection.java:23,44,47,53 (fuelSubType/sector/engTechID/regClassID); RunSpecXML.java:1175-1219 (XML element->flag mapping, line 1206 sector==segment), :1612-1622 (outputPopulation); AggregationSQLGenerator.java:746,1683,1855 (outputPopulation consumption)
  - Wired the two flags whose RunSpec signal is actually available in the Rust model: (1) output_population now reads run_spec.output_nonroad.population.unwrap_or(false) — canonical ExecutionRunSpec.getRunSpec().outputPopulation, parsed from <outputpopulation> (RunSpecXML.java:1612-1622) and consumed by AggregationSQLGenerator.java:746/1683/1855 (selectActivityNoScaleSQL population path); (2) sector now reads run_spec.output_breakdown.segment — canonical OutputEmissionsBreakdownSelection.sector, which RunSpecXML.java:1206 parses from <sector> OR <segment> as the same flag (the Rust model surfaces it only as `segment`). add_nonroad_dimension (plan.rs:627) already gates sectorID on models.contains(Nonroad), so wiring segment is safe for Onroad runs (still NULL-collapsed). reg_class_id/fuel_sub_type/eng_tech_id remain false because the Rust RunSpec OutputBreakdown struct does not yet carry those fields (no regclassid/fuelsubtype/engtechid in XmlOutputBreakdown or OutputBreakdown) — fixing them requires extending the moves-runspec crate, which is out of scope for this file (see followups). Note: output_population is currently a not-yet-consumed field in plan.rs, so this wiring has no numeric effect today but correctly forwards the RunSpec signal for the activity-population path; sector wiring does change Nonroad sector-breakdown output as intended.
- **crates/moves-importer-pdb/src/validate.rs** — OpModeDistribution validation omits the opModeFraction sum-to-1.0 invariant — ref: /tmp/canonical/moves-java/database/OpModeDistributionImporter.sql lines 25-31 (spCheckOpModeDistributionImporter); /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/importers/LinkOpmodeDistributionImporter.java lines 254-319 (getProjectDataStatus -> getImporterDataStatusCore runs OpModeDistributionImporter.sql)
  - Confirmed against canonical reference. LinkOpmodeDistributionImporter.getProjectDataStatus calls getImporterDataStatusCore, which runs database/opModeDistributionImporter.sql. That procedure (spCheckOpModeDistributionImporter) raises 'ERROR: ... opmodeFraction is not 1.0 but instead ...' for any group whose round(sum(opModeFraction),4) <> 1.0000, grouped by (sourceTypeID, hourDayID, linkID, polProcessID). The Rust port performed only source-type/hour-day coverage checks and never enforced the per-key fraction sum, so malformed op-mode distributions passed validation and would silently mis-scale emissions (calculators weight meanBaseRate by opModeFraction). Added a per-key sum accumulator keyed on (sourceTypeID,hourDayID,linkID,polProcessID) that returns Error::Validation with a message matching the Java text when |sum - 1.0| > FRACTION_SUM_TOLERANCE (5e-5, the same 4-dp tolerance the neighboring linkSourceTypeHour validator uses). Coverage checks are preserved. All five required columns (sourceTypeID, hourDayID, linkID, polProcessID, opModeFraction) exist in OP_MODE_DISTRIBUTION schema. The existing test row sums to 1.0 so it still passes.
- **crates/moves-nonroad/src/geography/prcnat.rs** — FINDING 2 — Per-model-year missing exhaust/evap tech aborts the whole national record but returns Ok with partial output — ref: NONROAD2008a prcnat.f :363–394 (pre-calc exhaust tech loop, ISKIP/goto 9999), :412 (evap tech), :941-942 (9999 return); nonroad.f :273-276 (caller turns ISKIP into goto 333 / skip record). Cross-checked prcus.rs:539-551 (same defect, comment admits invented early-return) and process.rs:519-526 (county path skips only the single year, Ok(()))
  - In prcnat.f the per-model-year tech index is resolved in a pre-calc loop (`do iyr = iepyr - MXAGYR + 1, iepyr`, :363–:394 for exhaust + :412 for evap) that runs BEFORE the state loop. A miss there does `ierr = ISKIP; goto 9999` (:392-393), which the caller nonroad.f:274 turns into `goto 333` — the entire record is skipped and NO state output (zero, missing, or completed) is ever written. The Rust port had moved this lookup INTO the model-year loop inside the state loop and responded to a miss with `return Ok(output)`, which (a) reports Success while swallowing a data gap, and (b) truncates all remaining model years AND remaining states while emitting whatever zero/missing/partial state records were already pushed — understating totals. Since the lookup depends only on (scc, hpval, tchmdyr), not on state, I restored the Fortran structure: added a pre-state-loop resolution over the full `(iepyr - MXAGYR + 1)..=iepyr` range that checks both exhaust and evap tech; on any miss it pushes the existing MissingExhaustTech/MissingEvapTech warning and returns the still-empty output early — exactly mirroring the existing up-front tech-year check at lines 278-295 and the Fortran record-level skip. The in-loop find_exhaust_tech/find_evap_tech lookups are kept (they supply tech_names/fractions) but their per-year-miss branches were changed from `return Ok(output)` to an explicit Error::Config, because after pre-validation a miss can only mean a non-deterministic callback (a real bug) that must surface rather than silently truncate as Success. Imported MXAGYR; removed the now-redundant inner `let iepyr` shadow.
- **crates/moves-nonroad/src/population/growth.rs** — Zero base-year growth indicator is clamped to MINGRWIND in the divisor, diverging from Fortran's intentional divide-by-zero — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/grwfac.f :233 (tmpbaseyearind, warning-only) and :244-245 (factor = (growthyearind - baseyearind)/(baseyearind*(igyear-ibasyr)))
  - grwfac.f computes tmpbaseyearind = max(MINGRWIND, baseyearind) at :233 SOLELY to format the warning message (:234-240) and bumps nwarn (:240), then at :244-245 divides by the RAW, un-clamped baseyearind. When the interpolated base indicator is exactly 0 the Fortran factor is growthyearind/0 -> +/-Inf, which propagates downstream (agedist.f :132). The port previously divided by `effective_base` (the MINGRWIND clamp), producing a finite factor the Fortran never computes -- a genuine numeric divergence affecting any region whose growth curve interpolates to a zero base indicator. Per the mandate to prefer canonical fidelity above all and surface rather than mask behavior, I changed the divisor to the raw `base_indicator` so the port now reproduces the Fortran's +/-Inf exactly, while still emitting the warning (with the MINGRWIND value the Fortran's message reports). I updated the module-level 'Known divergence' note to a 'Zero base-year indicator' note describing the now-faithful behavior, and updated the two tests that pinned the old finite-factor behavior (zero_base_indicator_produces_warning now asserts +Inf; renamed zero_base_indicator_yields_finite_factor_not_fortran_inf -> zero_base_indicator_matches_fortran_inf asserting is_infinite). MINGRWIND remains imported and used in the warning construction.
- **crates/moves-wasm/src/default_db.rs** — Meteorology unmatched-row fallback sets heatIndex=0.0 instead of temperature, diverging from the CLI reference — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/general/MeteorologyGenerator.java:151-156 (heatIndex = temperature WHERE temperature < 78); cross-checked moves-cli/src/run.rs:866-878
  - Confirmed divergence. Canonical MeteorologyGenerator.java (lines 151-156) sets `heatIndex = temperature` for the low-temperature / no-humidity-polynomial path (temperature < 78F), and the CLI port (moves-cli/src/run.rs:854-878) mirrors this by pushing temps[i] for unmatched ZoneMonthHour rows with specificHumidity/molWaterFraction = 0.0. The wasm port instead pushed (0.0,0.0,0.0) for unmatched rows, zeroing heatIndex and silently suppressing AC-activity adjustments (criteria_running_calculator gates on heat_index > 67.0). Fixed by reading the ZoneMonthHour `temperature` column (cast to Float64) and, in the unmatched (None) branch, pushing temps_ca.get(i) as heatIndex while keeping specificHumidity=0.0 and molWaterFraction=0.0, matching both the canonical generator and the CLI port. Matched rows are unchanged.
- **crates/moves-wasm/src/lib.rs** — DriverRecord.median_life hardcoded to 0.0, downstream substituting an invented 1000.0-hour lifespan — ref: NONROAD/NR08a/SOURCE/getpop.f:127,221 (usetmp->usehrs from line(88:92)); prccty.f:374 (usehrs->modyr uselif); modyr.f:153,165 (uselif->scrptime, `uselif.LE.0 => 1.0` fallback); scrptime.f:31,116 (mdlfhrs median life); crates/moves-nonroad/src/input/pop.rs:25,72,158 (cols 88-92 -> PopulationRecord.usage)
  - The doc claim that '.POP carries no medianLifeFullLoad' is false. The median-life-at-full-load value IS in the .POP record at cols 88-92, parsed by read_pop into PopulationRecord.usage (input/pop.rs:25,72,158). Canonically getpop.f:127/221 reads that field into usehrs(icurec); prccty.f:374 passes it as uselif into modyr (modyr.f:153 forwards it to scrptime as mdlfhrs), and modyr.f:165 applies the only canonical fallback `if(uselif .LE. 0) uselif = 1.0` (=1.0, not 1000.0, and only after scrptime). The wasm code destructured PopulationRecord with `..`, discarding usage, and hardcoded median_life: 0.0 — fabricating a 0 lifespan for every real record and forcing the executor's neutral 1000.0-hour substitution. Fixed by destructuring `usage` and assigning median_life: usage (both f32), so the real per-record lifespan flows through. The other median_life:0.0 sites in the crate (inputs.rs:371, mod.rs:232, run.rs:548, executor.rs) are all #[cfg(test)] helpers, so they are correctly left alone.
- **crates/moves-calculators/src/calculators/airtoxicsdistance.rs** — unit_factor() silently assumes per-mile for any units string not literally containing "/km" — ref: /tmp/canonical/moves-java/database/AirToxicsDistanceCalculator.sql lines 127-135 (UseDioxinEmissionRate) and 154-162 (UseMetalEmissionRate): `case units when 'g/mile' then 1.0 when 'g/km' then 1.609344 when 'TEQ/mile' then 1.0 when 'TEQ/km' then 1.609344 else 1.0 end`
  - Replaced the permissive `contains("/km")` substring test with an exact-match CASE that mirrors the canonical SQL `case units when 'g/mile' then 1.0 when 'g/km' then 1.609344 when 'TEQ/mile' then 1.0 when 'TEQ/km' then 1.609344 else 1.0 end` in database/AirToxicsDistanceCalculator.sql (sections UseDioxinEmissionRate at lines 127-135 and UseMetalEmissionRate at lines 154-162). Important divergence eliminated: the old `contains("/km")` would scale ANY string containing the bytes "/km" by 1.609344 (e.g. a hypothetical "foo/km"), whereas the canonical reference scales ONLY the exact strings 'g/km' and 'TEQ/km' and falls everything else (its `else`) through to 1.0. The canonical reference DOES use an unconditional per-mile `else 1.0` fallback for unrecognised units, so the audit's concern that an explicit error should be raised for unknown units is NOT supported by the reference -- MOVES intentionally treats unknown units as already-per-mile. The fix therefore preserves the reference-faithful `else 1.0` while making the per-km branch an exact match rather than a substring match. Native distance unit in MOVES is miles (SQL comment lines 123-125 / 150-152).
- **crates/moves-calculators/src/calculators/crankcase_emission.rs** — regClass dropped from the CrankcaseEmissionRatio join key and replaced with a 0-is-wildcard fallback that can silently over-count emissions — ref: /tmp/canonical/moves-java/database/CrankcaseEmissionCalculator.sql lines 212-219 (main INNER JOIN equality on regClassID); /tmp/canonical/moves-java/database/BaseRateCalculator.sql lines 1513-1538 (MOVESWorkerOutput.regClassID populated from BaseRateOutput.regClassID, no collapse to 0); evap-only `0 as regClassID` at EvaporativePermeationCalculator.sql:441, LiquidLeakingCalculator.sql:343, MultidayTankVaporVentingCalculator.sql:1707,1883
  - The canonical CrankcaseEmissionCalculator.sql (lines 212-219) joins CrankcaseEmissionRatio with a literal four-key equality INNER JOIN: r.polProcessID=ppa.polProcessID AND r.sourceTypeID=mwo.sourceTypeID AND r.regClassID=mwo.regClassID AND r.fuelTypeID=mwo.fuelTypeID (plus the model-year window). There is no wildcard and no regClassID=0 special case. I verified the comment's premise that MOVESWorkerOutput regClass is collapsed to 0 on this path is FALSE: this calculator processes only the running/start/extended-idle exhaust processes (1/2/90), whose worker rows are written by BaseRateCalculator.sql's `insert into MOVESWorkerOutput (... regClassID ...) select ... regClassID from BaseRateOutput` (lines 1513-1538) carrying a concrete regClassID. The only `0 as regClassID` collapses in the canonical SQL are in the evaporative calculators (EvaporativePermeation, LiquidLeaking, MultidayTankVaporVenting), which feed other processes, not 1/2/90. The port's wildcard (accept all ratio rows in the cell when mwo.reg_class_id==0) would emit one crankcase row per per-regClass ratio row, multiplying emissions relative to the equality join. Fix: restored regClassID into the ratio_index key (now a 4-tuple) and the lookup key, and removed the wildcard branch, making the join a literal equality exactly as the SQL. Existing test fixtures use reg_class_id=30 on both sides, so they still match; only the divergent wildcard path is removed.
- **crates/moves-calculators/src/calculators/evaporative_permeation_calculator.rs** — with_reg_class toggle inferred from RegClassSourceTypeFraction emptiness instead of always WithRegClassID — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/framework/BundleUtilities.java:178 (enabledSectionNames.add("WithRegClassID") in prepareCountyDataWithRunSpec, no NoRegClassID enable)
  - Confirmed against canonical BundleUtilities.prepareCountyDataWithRunSpec, which unconditionally calls enabledSectionNames.add("WithRegClassID") (BundleUtilities.java:178) with no corresponding NoRegClassID enable anywhere, so the NoRegClassID section is dead in current MOVES. The sibling ports (liquid_leaking_calculator.rs:78-85, multiday_tank_vapor_venting_calculator.rs:115-116) implement WithRegClassID only and force the toggle on. In execute(), the port was deriving with_reg_class = !reg_class_rows.is_empty(), so an empty/absent RegClassSourceTypeFraction (a data-extraction gap) would silently flip PC-1b to the NoRegClassID path: regClassID collapsed to 0 and the base rate no longer split per regulatory class, changing output dimensions and totals while hiding the missing data. Fixed by (a) setting with_reg_class: true unconditionally in the RunContext built by execute() (matching BundleUtilities), and (b) adding a guard that returns Err(moves_framework::Error::InvalidBundle(...)) when RegClassSourceTypeFraction is empty, surfacing the data gap instead of silently switching sections. The reg_class_rows binding is still moved into inputs.reg_class_source_type_fraction afterward (guard only borrows via .is_empty()), so the change is surgical and compiles. The live-path test execute_returns_nonempty_dataframe_for_minimal_inputs supplies a non-empty RegClassSourceTypeFraction (with_reg_class fixture already true), so the guard does not break it. The calculate() logic at lines 2670-2701 and the test fixtures that call calculate() directly with with_reg_class set by hand are unchanged.
- **crates/moves-calculators/src/calculators/nrairtoxics.rs** — execute() hardcodes fuel_sub_type_id=0 and fuel_formulation_id=0 on every input emission, defeating the nrATRatio gaseous-toxics path — ref: /tmp/canonical/moves-java/calc/nrairtoxics/nrairtoxics.go:275-283 (ff==nil skip + ff.FuelSubTypeID key) and /tmp/canonical/moves-java/calc/mwo/mworeader.go:514-530 (per-row NRFuelSupply expansion supplying FuelSubTypeID/FuelFormulationID); in-crate pattern reference crates/moves-calculators/src/calculators/nrhcspeciation.rs NrThcWorkerRow + execute() lines 1361-1389
  - Confirmed the divergence against the canonical Go pipeline. In nrairtoxics.go calculate() (lines 275-283) each emission's fuel is resolved as ff := mwo.FuelFormulations[e.FuelFormulationID]; if ff == nil { continue }, and the nrATRatio key uses ff.FuelSubTypeID. The per-emission FuelFormulationID/FuelSubTypeID originate in the MWO reader (mworeader.go parseLines, lines 514-530), where each MOVESWorkerOutput row is expanded across the NRFuelSupply distribution and fb.Add(fsd.FuelSubTypeID, fsd.FuelFormulationID, ...) is called per fuel-supply detail; that expansion is performed upstream of the calculators in moves.rs's data plane, so the MOVESWorkerOutput rows the calculator consumes already carry fuelSubTypeID and fuelFormulationID columns. The companion in-crate calculator nrhcspeciation.rs (NrThcWorkerRow at lines 868-916; execute() at 1361-1389) demonstrates the exact intended pattern: it reads fuelSubTypeID/fuelFormulationID from MOVESWorkerOutput into the input Emission and writes em.fuel_sub_type_id/em.fuel_formulation_id back into the output rows. nrairtoxics's NrAirToxicsMwoRow was missing both columns and execute() hardcoded both to 0, so every VOC emission keyed nrATRatio on subtype 0 (never matching real rows, silently zeroing benzene/butadiene/aldehydes/aromatics/ethanol/MTBE) and the ff==nil skip degenerated to whether a formulation-0 row happened to exist. Fix: added fuel_sub_type_id and fuel_formulation_id fields to NrAirToxicsMwoRow and wired them through polars_schema (added fuelSubTypeID/fuelFormulationID Int32 columns), into_dataframe (two new Series), and from_dataframe (two new column reads with null-guards). In execute() the input Emission now reads row.fuel_sub_type_id/row.fuel_formulation_id (matching nrhcspeciation), and both output-row pushes (air_toxics_block and non_hap_tog_block) now write emission.fuel_sub_type_id/emission.fuel_formulation_id (the toxic Emission carries them through unchanged via Emission::scaled, mirroring the Go which preserves e.FuelSubTypeID/e.FuelFormulationID on derived emissions). Updated the in-file execute_wires_through_data_plane test's NrAirToxicsMwoRow literal to set the two new fields (subtype 0/formulation 0, matching its existing FuelFormulation row).
- **crates/moves-calculators/src/calculators/nrhcspeciation.rs** — NonHAPTOG/needed-set source diverges: nrhcspeciation derives the needed set from its own REGISTRATIONS instead of the run's NeededPolProcessIDs table — ref: /tmp/canonical/moves-java/calc/nrhcspeciation/nrhcspeciation.go calculate() — `ppid = 5*100 + fb.Key.ProcessID; if(mwo.NeededPolProcessIDs[ppid]) {...}` repeated for pollutants 5/79/80/87/86; cross-checked against crates/moves-calculators/src/calculators/nrairtoxics.rs:1684-1688 and the NrNeededPolProcessRow TableRow impl at nrairtoxics.rs:1358-1400
  - Confirmed against the canonical Go port: calc/nrhcspeciation/nrhcspeciation.go `calculate` gates EVERY speciated pollutant on `mwo.NeededPolProcessIDs[ppid]` (ppid = pollutant*100 + processID) for methane(5), NMHC(79), NMOG(80), VOC(87), and TOG(86) — i.e. the run's actual engine-assembled needed set. The Rust port instead built `needed_pol_process_ids` from the calculator's static 45-entry REGISTRATIONS list, so `is_needed()` was always true and the gate was a no-op, over-producing species the run never requested. The sibling NRAirToxicsCalculator::execute (nrairtoxics.rs:1684-1688) already sources this from the real `NeededPolProcessIDs` table, so the two nonroad calculators were inconsistent. Fix: (1) added a local `NrNeededPolProcessRow` TableRow type (private `NeededPolProcessIDs` row, mirroring nrairtoxics.rs's identical struct, which is not `pub` so could not be imported), and (2) changed execute() to read the needed set via `tables.iter_typed::<NrNeededPolProcessRow>("NeededPolProcessIDs")?.into_iter().map(|r| r.pol_process_id)`. This is read unconditionally with `?` exactly like nrairtoxics, so robustness is identical. REGISTRATIONS is still used by registrations() so no dead code. Note: NeededPolProcessIDs is a framework-provided table (nrairtoxics also reads it without listing it in INPUT_TABLES), so INPUT_TABLES was intentionally left unchanged to match the sibling calculator.
- **crates/moves-calculators/src/calculators/welltopump/co2_atmospheric.rs** — Missing context year silently falls back to an arbitrary Year-table row, then to year 0 — ref: /tmp/canonical/moves-java/database/CO2AtmosphericWTPCalculator.sql lines 56,59 (gwtp.yearID = ##context.year##, y.yearID = ##context.year##); CO2AtmosphericWTPCalculator.java subscribeToMe (chained calculator). Codebase precedent: crates/moves-calculators/src/calculators/welltopump/ch4n2o.rs:295 and total_energy.rs:314.
  - Replaced the fabricated fallback `.unwrap_or_else(|| year.first().map(|y| y.year_id).unwrap_or(0))` with an explicit error. The canonical SQL (CO2AtmosphericWTPCalculator.sql lines 56,59) binds the year-keyed GREET join `gwtp.yearID = ##context.year##` and `y.yearID = ##context.year##` directly to the MasterLoop iteration year, with no 'first Year row' or 'year 0' fallback. The Java subscribeToMe shows it is a chained calculator fired off the year-keyed energy calculators, so reaching execute() with year==None is a logic/data error, not a defaultable condition. The old code could pick an arbitrary unordered Year row (wrong GREET rate + wrong fuel supply) or year 0 (matches no GREET/fuel-year, silently returns empty). Now `pos.time.year` is propagated via `Error::RowExtraction { table: "IterationPosition", column: "year", ... }`. This is the IDENTICAL fix already applied to the sibling WTP calculators ch4n2o.rs (line 295) and total_energy.rs (line 314), which had the same fallback bug; ExecutionTime.year is documented as None only before the loop enters YEAR granularity. Also updated the `execute_returns_nonempty_dataframe_for_minimal_inputs` test to supply a year-2020 position via `with_position_and_tables` (mirroring the sibling tests), since it previously relied on the fabricated fallback. No numeric behavior on the live (year-present) path changed.
- **crates/moves-calculators/src/generators/evap_op_mode_distribution.rs** — month+1 December wrap to January reuses the same yearID, risking an empty next-month join for December runs — ref: EvaporativeEmissionsOperatingModeDistributionGenerator.java calculateOpModeDistribution step 010: lines 199-200 (sho join monthID/yearID), 206-207 (sourceHours filter monthID/yearID), 248-249 (SoakActivityFraction filter monthID/zoneID); cross-checked TotalActivityGenerator.java (no month offset on sourceHours/SHO inserts) and TankTemperatureGenerator.java:1749 calculateSoakActivityFraction (inserts d.monthID, no offset).
  - The finding flagged the December year-wrap as a symptom, but the underlying defect is broader: the Rust port had FABRICATED an entire `month+1` shift convention (a `next_month` computed in both `fraction_of_operating` and `op_mode_distribution`) that does not exist in canonical MOVES. The Java `calculateOpModeDistribution` SQL joins ALL three input tables on the context's own month/year with NO offset: sourceHours filters `sh.monthID=##context.monthID## AND sh.yearID=##context.year##` (lines 206-207), SHO joins `sho.monthID=##context.monthID## AND sho.yearID=##context.year##` (lines 199-200), and SoakActivityFraction filters `saf.monthID = ##context.monthID##` (line 248). I confirmed the upstream writers do not shift either: TotalActivityGenerator writes sourceHours/SHO under the context monthID/yearID (no `+1` anywhere), and TankTemperatureGenerator.calculateSoakActivityFraction inserts SoakActivityFraction with `d.monthID` taken from the underlying activity data for the context month. So the entire `month+1`/`next_month` logic was reading the wrong month's activity for EVERY month (not just December), silently capping mode-300 at 1 whenever the off-by-one month had no rows. Fix: removed `next_month` in both functions and filter/join on `ctx.month_id` directly; updated the doc comments citing the Java SQL lines; and corrected the three test helpers (`sh`, `sho`, `saf`) that wrote `MONTH + 1` to write `MONTH` so the unit tests still validate the correct (offset-free) join. The two negative filter tests still use `month_id: 1` (distinct from context MONTH=7), so they keep exercising the month filter.
- **crates/moves-calculators/src/generators/fueleffectsgenerator/mod.rs** — FuelFormulation with unmapped fuelSubtypeID is silently bucketed into fabricated fuelTypeID 0 — ref: FuelEffectsGenerator.java:1356-1363 (getFuelFormulations), confirmed also at 1374-1384 (getFuelSupplyFormulations), both INNER JOIN fuelSubtype->fuelFormulation
  - Replaced `*subtype_to_type.get(&r.fuel_subtype_id).unwrap_or(&0)` with a let-else that `continue`s when the fuelSubtypeID has no matching FuelSubtype row. The canonical Java `getFuelFormulations` (FuelEffectsGenerator.java:1356-1363) realizes the fuelFormulation->fuelSubtype->fuelType relationship as an INNER JOIN (`from fuelSubtype fst inner join fuelFormulation ff on ff.fuelSubtypeID=fst.fuelSubtypeID where fst.fuelTypeID=...`). Under inner-join semantics a formulation whose fuelSubtypeID has no fuelSubtype row appears for no fuel type. Dropping the row matches this exactly, whereas the prior code invented a phantom fuelTypeID 0 bucket that could poison the formulation_to_type map (lines 182-185) and corrupt fuel-effect ratio assignment. This also no longer masks a corrupt/incomplete FuelSubtype table by silently fabricating membership. The fix is type-correct (get returns Option<&i32>, binds i32) and preserves all numerically-correct behavior for rows that do map.
- **crates/moves-importer-county/src/age_distribution.rs** — ageFraction sum-to-1 check skips null sourceType/year/fraction rows — ref: /tmp/canonical/moves-java/database/AgeDistributionImporter.sql lines 71-79 (sum-to-1.0 check) and lines 83-88 (NULL ageFraction error); driver gov/epa/otaq/moves/master/implementation/importers/AgeDistributionImporter.java getImporterDataStatusCore (runs that SQL, treats any 'ERROR'-prefixed message as NOT_READY)
  - The canonical AgeDistributionImporter.sql does TWO things the Rust code was missing. (1) Lines 83-88 emit an explicit ERROR for any NULL ageFraction (`select concat('ERROR: Found a NULL ageFraction value ...') from SourceTypeAgeDistribution where ageFraction IS NULL LIMIT 1`); the Rust code silently `continue`d on null fractions and never surfaced this. (2) Lines 71-79 compute the per-(sourceTypeID,yearID) sum via SQL `sum(ageFraction)` grouped by sourceTypeID,yearID — SQL SUM skips NULLs (zero contribution) but the tuple still appears in the GROUP BY, so a tuple whose only/all fractions are NULL still fails the `round(sum,4)<>1.0000` check. The Rust `continue` on `fraction.is_null` removed such rows entirely, so a tuple consisting only of null-fraction rows would vanish from the map and never be flagged. Fix: (a) added a NULL-ageFraction error matching the SQL (emitted once, mirroring SQL `LIMIT 1`), and (b) changed the sum loop to skip only rows with a NULL sourceTypeID/yearID key (those are not part of the GROUP BY tuple) while still registering the tuple for NULL-fraction rows with a zero contribution, so missing/NULL age buckets correctly drag the sum below 1.0 and get reported. Numeric behavior for all-non-null data is unchanged.
- **crates/moves-nonroad/src/population/scrappage.rs** — Missing alternate-scrappage percent defaults to 0.0 (no scrappage) rather than the documented 100% default — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdalt.f (init loops 10/20: `altpct(i,j) = 100.0`; loop 40 else branch: `altpct(i,ncount) = 100.0`); /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/getscrp.f (`scrppct(i) = altpct(idxalt,i)`)
  - Changed `row.percents.get(idx).copied().unwrap_or(0.0)` to `unwrap_or(100.0)` to match canonical NONROAD semantics. In rdalt.f the alternate percent array `altpct(i,j)` is initialized to 100.0 for all i,j (loops labeled 10/20), and any blank/absent percent column is explicitly set to 100.0 in the else branch of loop 40 (`altpct(i,ncount) = 100.0`). getscrp.f then copies `altpct(idxalt,i)` directly, so an absent column yields the pre-filled 100.0 ('fully scrapped'). The Rust port's fabricated 0.0 was the opposite default (no scrappage), which would understate scrappage / overstate surviving population. The fallback is currently unreachable because the parser in alt_scrap.rs guarantees every row has exactly names.len() percents and idx<names.len(), but the latent default now matches canonical behavior instead of masking a future mismatch with the wrong value. Added an explanatory comment.

## Hardenings (missing-data / errors now surfaced) (85)

- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 1: calculate_retrofit returns RetrofitResult::default() (state/national/us-total) — ref: NONROAD clcrtrft.f (per-pollutant frac_retrofitted reduction); prcus.f:480-488 / prcnat.f drives it via the retrofit_loaded gate
  - In prcus.rs/prcnat.rs, calculate_retrofit is invoked (only when opt.retrofit_loaded) immediately before calculate_exhaust within the same tech-fraction>0 block. Returning the all-zero RetrofitResult silently drops the per-pollutant reduction that clcrtrft computes. The state/national/us-total exhaust calculators are not yet wired anyway, so I converted all three calculate_retrofit stubs from Ok(RetrofitResult::default()) to Err(Error::Config(...)) so a retrofit-active run fails loudly instead of biasing emissions high. Numerically-correct behavior is unchanged because there is no correct emission output on this path yet (calculate_exhaust below it now also errors).
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 4: compute_evap_iteration is todo!() on the live county path — ref: clcevems.f; process.rs:778-789 (evtchfrc>0 gate)
  - process.rs:789 calls compute_evap_iteration whenever an evap tech fraction > 0; the todo!() panics any real gasoline run. Converted the todo!() to Err(Error::Config(...)) so the unloaded-evap-EF condition propagates as a Result rather than panicking. No numerically-correct behavior changed (the path produced no valid output before).
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 5: StateAdapter/National/UsTotal calculate_exhaust and calculate_evap are todo!() — ref: clcems.f / clcevems.f; prcus.rs:592/695, prcnat.rs:495/585 (tech_fraction>0 gates)
  - prcus.f/prcnat.f call calculate_exhaust (and calculate_evap) whenever a tech fraction > 0, independent of any loaded flag; the todo!()s panic any real state/national/US-total run. Converted all six todo!()s to Err(Error::Config(...)) so the unloaded-NR*.EMF condition is a propagated error, not a panic. The acceptance tests use zero tech fractions and never reach these methods, so they are unaffected.
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 8: growth_factor returns 0.0 on empty indicator selection (State/National/UsTotal) — ref: grwfac.f:108-118,219-230 (7000 path, ierr=IFAIL); prcsta.f:458-460 (fatal on jerr!=ISUCES)
  - grwfac.f sets factor=0.0,ierr=IFAIL at entry; an empty indicator scan (iend==0) hits the 7000 ERROR path and returns ierr=IFAIL, which prcsta.f:460 (if jerr.NE.ISUCES goto 9999) treats as fatal. growth_factor is only invoked when growth_loaded=true (prcus/prcnat/state early-return otherwise), so an empty select_for_indicator result is a missing-data error, not a legitimate no-growth (which would be 1.0, or the 0.0 that grwfac returns ONLY when both indicators are zero or igyear==ibasyr — handled inside the ported growth_factor). Changed the three adapters' `if selected.is_empty() { Ok(0.0) }` to Err(Error::IndicatorMissing{..}). Also updated the four in-file state-class acceptance tests (state_to_county/state_from_national/national/us_total) to supply two flat national 'GDP' indicator rows so growth_factor resolves to 0.0 — identical downstream behavior — keeping them consistent with growth_loaded=true.
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 9: compute_exhaust_iteration substitutes load_factor=0.5/HoursPerYear when activity entry missing — ref: clcems/prccty.f read faclod(idxact) from the activity record; executor.rs:904-914 (model_year_and_agedist errors on same index)
  - The reference reads faclod/iactun directly from the validated activity record; there is no 0.5 default. model_year_and_agedist already errors on an out-of-range activity index (executor.rs:909), and compute_exhaust_iteration receives the same idxact, so the unwrap_or((0.5,HoursPerYear)) was a latent silent-corruption fallback. Changed it to ok_or_else(Error::Config) to match the existing line-909 invariant, so a broken index surfaces instead of fabricating a 0.5 load factor that would scale emissions/fuel wrong.
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 12: use_hours falls back to fabricated 1000.0 median life — ref: getpop.f:127,221 (read usehrs, ERR=7003 fatal); scrptime.f:31,118 (mdlfhrs is the real median life)
  - Two-part. (1) In-file fix applied: build_state_context/build_national_context/build_us_total_context hardcoded use_hours:1000.0, ignoring the real ctx.record.median_life that county/subcounty already read. Changed all three to read ctx.record.median_life (with the same documented neutral fallback) so the state-class paths stop silently discarding the .POP usage field. (2) Enforcing that a missing/zero median life is fatal belongs in the .POP loader, not the executor: getpop.f reads usehrs from cols 88-92 with ERR=7003 (fatal) — there is no default, and the executor faithfully consumes whatever the loader produced. Turning the executor's 1000.0 fallback into an error would also break the in-file tests that intentionally use median_life=0.0 (Finding 14). So the fatal-on-missing enforcement is deferred cross-file to getpop's port; disc_code='DEFAULT'/discharge_code:0 likewise needs the scrappage-selector field which is not carried.
- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 14: acceptance tests assert 'one row' while zeroing every tech fraction — ref: executor.rs tests (b)-(f); the EF/BSFC/evap loaders gating real emission magnitude are cross-file
  - This is a test-fidelity concern, not a numeric divergence in the production code, and a full fix (non-zero tech fractions + loaded EF/BSFC tables + assertions against a reference NONROAD run) depends on the unported EF loaders, so it cannot be completed in this file alone. As a partial hardening tied to Finding 8, I updated the four state-class tests to supply consistent growth data (so they exercise the real growth_factor path that now errors on missing indicators, instead of relying on a fabricated 0.0). I did not loosen or strengthen the row-count-only assertions further, since the magnitude assertions require the cross-file EF loaders to exist first.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs** — FINDING 1: float_col silently substitutes 0.0 for missing column or unparseable cell — ref: NONROAD2008a rdemfc.f:159-160 + error label 7005 (missing rate -> hard error); crate error.rs (only Error::Parameters).
  - float_col returns an infallible Vec<f64> and feeds every engine numeric quantity (meanBaseRate, population, deterioration coeffs, marketShare, monthFraction/dayFraction, hpAvg, loadFactor, temperature). Every call site reads a column that is part of its source table's fixed schema, so a structurally-absent column is always a schema/naming mismatch, not a 0.0 value. I changed the resolve()->None arm from `return vec![0.0; df.height()]` to a panic naming the missing column and the columns present. This surfaces a missing-rate/missing-population condition (canonical NONROAD treats a missing rate in its fixed-format input as a hard error) instead of zeroing emissions silently. I deliberately LEFT the per-cell unwrap_or(0.0) fallback intact: it tolerates the snapshot's documented mixed numeric/string-decimal storage and is not the structural divergence flagged. The crate Error enum has only a Parameters variant and the whole call chain (load_nonroad_reference -> ReferenceData) is infallible, so a panic is the only in-file mechanism to surface the failure without a cross-file API change.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs** — FINDING 2: int_col silently substitutes 0 for missing column or unparseable key — ref: NONROAD2008a prccty.f / rdemfc.f treat structural keys as mandatory; crate error.rs (infallible call chain, no suitable Err variant).
  - int_col supplies the join/lookup keys (polProcessID, engTechID, sourceTypeID, hpMin/hpMax, modelYearID, monthID, sectorID, fuelTypeID, NREquipTypeID, growthPatternID, NRBaseYearID, yearID). A missing column would collapse every key to 0 (pollutant_slot_for(0)=None drops the rate; every row maps to sourceTypeID 0). I changed the resolve()->None arm to panic naming the missing column. Per-cell parse tolerance (unwrap_or(0)) is preserved for the mixed-storage form. base_year() still guards on table presence before reading NRBaseYearID, so the legitimate fully-absent-table default (1990) is unaffected; only a present-but-renamed key column panics.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs** — FINDING 6: unit_code_for defaults unknown emission-rate units to g/HP-hr — ref: NONROAD2008a rdemfc.f:159-160 (fndchr units lookup) + error label 7005; units keyword table rdemfc.f:111-117.
  - Confirmed divergence. Canonical rdemfc.f:159-160 looks the units keyword up via fndchr; an unmatched keyword falls through to error label 7005 ('Missing or invalid tech type or units type') and aborts. The units field selects the activity basis (HP-hours vs gallons vs hours vs starts vs multiplier), so a blank/unknown value is not a safe default. I removed the blank "" case and the catch-all from the g/HP-hr arm and made the unrecognized-units arm panic with a diagnostic citing rdemfc.f. Verified the blank-units BSFC test row (emission_df line 1837, polProcessID 9901) never reaches unit_code_for: both call paths `continue` on pp==PP_BSFC before the unit lookup, and only rows with a valid pollutant_slot_for call unit_code_for, so the test suite is unaffected.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs** — FINDING 7: build_scrappage_curve fabricates a degenerate 0->100% curve when table absent or empty — ref: NONROAD2008a rdscrp.f:71-86 (required /SCRAPPAGE/ packet, goto 7000 on missing) and fndscrp.f; database/CreateNRDefault.sql:198-204 (NRScrappageCurve).
  - The scrappage curve is a required input. Canonical rdscrp.f reads it from the mandatory /SCRAPPAGE/ packet and aborts if missing (rdscrp.f:80-86 -> goto 7000); the EPA default DB always ships the NREquipTypeID=0 curve, and nonroad_emission.rs:94 lists it as a required input. The fabricated 0->100% linear curve is not canonical's sigmoid-like default and would mis-age the fleet. I replaced both degenerate-fallback branches (missing table, and empty-after-NREquipTypeID=0-filter) with panics citing rdscrp.f. Verified no test relies on the fallback: build_scrappage_curve is only called from load_nonroad_reference (production) and the snapshot-driven diag test (which provides nrscrappagecurve); the synthetic-df unit tests never reach it.
- **crates/moves-calculators/src/calculators/basicbraketirepm.rs** — Empty County extract silently yields GPAFract = 0.0, zeroing the GPA fuel-adjustment blend — ref: /tmp/canonical/moves-java/database/BasicPM25Calculator.sql:133-134 (County extract), :601-602 (GPAFract blend)
  - Replaced `county_rows.first().map(|r| r.gpa_fract).unwrap_or(0.0)` with `.ok_or_else(|| row_err("County", 0, "GPAFract", "County extract is empty"))?`. Canonical BasicPM25Calculator.sql lines 133-134 build the County extract as `FROM County WHERE countyID = ##context.iterLocation.countyRecordID##` (a single guaranteed row), and line 601-602 read its GPAFract in the step-4 blend `SUM((fuelEffectRatio + GPAFract*(fuelEffectRatioGPA - fuelEffectRatio))*marketShare) FROM County c`. An empty extract is a fatal data error in MOVES, never a 0.0 fraction; defaulting to 0.0 silently suppresses the geographic-phase-in fuel effect. Now surfaces the missing row via the existing Error::RowExtraction helper. Numerically-correct behavior (a present row, even with NULL GPAFract resolved by the data plane) is unchanged.
- **crates/moves-calculators/src/calculators/basicbraketirepm.rs** — Missing context year defaults to 0, corrupting model-year/age arithmetic and the fuel-year filter — ref: /tmp/canonical/moves-java/database/BasicPM25Calculator.sql ##context.year## macros (e.g. :146,:197-198,:384,:569)
  - Replaced `pos.time.year.map(|y| y as i32).unwrap_or(0)` with `.ok_or_else(|| row_err("County", 0, "yearID", "context year is missing"))?`. Every iteration of the canonical SQL substitutes ##context.year## with a concrete calendar year (e.g. BasicPM25Calculator.sql:146,152,197-198,384,469,569). With year=0, target_model_year (line 727) goes negative, the age/existence filters match nothing, and the Year filter (line 958) selects nothing, yielding silent empty output. A None year is a framework/context bug; now propagated as an error rather than masked.
- **crates/moves-calculators/src/calculators/basicbraketirepm.rs** — Empty Link extract silently yields roadTypeID = 0 in worker output — ref: /tmp/canonical/moves-java/database/BasicPM25Calculator.sql:165-166 (Link extract), :719 (l.roadTypeID into output)
  - Replaced `link_rows.first().map(|r| r.road_type_id).unwrap_or(0)` with `.ok_or_else(|| row_err("Link", 0, "roadTypeID", "Link extract is empty"))?`. Canonical BasicPM25Calculator.sql lines 165-166 build the Link extract as `FROM Link WHERE linkID = ##context.iterLocation.linkRecordID##` (single guaranteed row), and line 719 stamps `l.roadTypeID` onto every output row. roadTypeID 0 is not a valid MOVES road type; an empty Link is a data error that would misattribute/lose the brake/tire emissions in downstream road-type aggregation. Now fails fast.
- **crates/moves-calculators/src/calculators/basicbraketirepm.rs** — Missing state/county/zone/link IDs default to 0 in worker-output location columns — ref: /tmp/canonical/moves-java/database/BasicPM25Calculator.sql:714-717 (iterLocation.* into output stateID/countyID/zoneID/linkID)
  - Replaced each `pos.location.<id>.map(|x| x as i32).unwrap_or(0)` with `.ok_or_else(|| row_err(...))?` for state_id, county_id, zone_id, link_id. The canonical output insert (BasicPM25Calculator.sql:714-717) writes `##context.iterLocation.stateRecordID##/countyRecordID##/zoneRecordID##/linkRecordID##` verbatim as stateID/countyID/zoneID/linkID; these are always concrete record IDs for the running iteration. A None component is a context bug; defaulting to 0 would misplace emissions at location 0 in the output inventory. Now surfaces the missing context ID. Behavior for fully-populated positions is unchanged.
- **crates/moves-framework/src/data/conversions.rs** — FINDING 1: iter_typed normalization silently drops expected columns absent from the stored table — ref: /Users/ctessum/code/earthsciml/moves.rs/crates/moves-calculators/src/calculators/tank_vapor_venting_calculator.rs:723-742 (from_dataframe errors on df.column() miss); canonical MOVES SQL behavior (column-not-found is fatal)
  - Changed the normalize-path projection from `filter_map` (which silently skipped any schema column missing from the store) to a fallible `map(...).collect::<Result<Vec<Column>>>()?` that returns `Error::Polars("column '<canonical>' declared by schema for table '<name>' is not present in the stored table")` when an expected column is absent. This makes the two iter_typed branches consistent: the non-normalize fast path hands the full frame to from_dataframe (which errors via df.column() on a missing column), and now the normalize path surfaces the same missing-column condition loudly instead of building a narrower frame and depending on from_dataframe to maybe catch it. Mirrors the SQL 'column not found' a faithful execution-DB port would hit. Verified that calculator from_dataframe impls do `df.column(col).map_err(...)?` (e.g. tank_vapor_venting_calculator.rs:726), confirming a missing column is intended to be an error, not a silent drop.
- **crates/moves-framework/src/data/conversions.rs** — FINDING 2: Column dtype lookup falls back to DataType::Null on error — ref: N/A (pure robustness hardening; no canonical numeric behavior involved). Error::Polars defined in crates/moves-framework/src/error.rs:113-114
  - Replaced `arc_df.column(actual).map(|s| s.dtype().clone()).unwrap_or(DataType::Null)` with `arc_df.column(actual).map_err(|e| Error::Polars(e.to_string()))?.dtype().clone()`. The lookup should never fail (the name came from get_column_names()), but if it did, the fabricated DataType::Null would silently mis-schedule a cast (or skip a needed one) and alter how the column is decoded. Now the failure propagates as Error::Polars instead of being swallowed. No numeric divergence in the normal path since the lookup succeeds there.
- **crates/moves-framework/src/data/conversions.rs** — FINDING 3: String->Boolean coercion treats every value other than "Y"/"1" as false — ref: /tmp/canonical/moves-java/database/CreateDefault.sql:797 (subjectToEvapCalculations CHAR(1)); /tmp/canonical/moves-java/database/IMCoverageImporter.sql:17 (update set useIMyn='Y' where useIMyn='y'); local contract /Users/ctessum/code/earthsciml/moves.rs/crates/moves-importer/src/reader.rs:180-199
  - Replaced the silent whitelist `s == "Y" || s == "1"` (everything else -> false, no error) with an explicit decoder matching the project's established boolean contract in moves-importer/src/reader.rs: trimmed, case-insensitive, '1'/'true'/'y'/'yes' -> true, '0'/'false'/'n'/'no' -> false, and any other non-null value returns Error::Polars naming the column/table/value. MOVES stores these flags as CHAR(1) 'Y'/'N' columns (e.g. subjectToEvapCalculations CHAR(1) NOT NULL DEFAULT 'N' in CreateDefault.sql:797), which the default-db converter maps to Utf8 (types.rs:24, CHAR is not bool/boolean), so this string->Boolean cast is on the live path. The old whitelist silently flipped unrecognized truthy values (lowercase 'y', 'true', whitespace) to false, which could change which rows a calculator selects. MOVES Java itself normalizes 'y'->'Y' before comparing (IMCoverageImporter.sql:17), confirming these flags must be handled exactly, not silently defaulted. Hardening surfaces unexpected encodings as errors while preserving true behavior for all canonical values.
- **crates/moves-nonroad/src/emissions/evaporative.rs** — Per-species day-adjustment falls back to 1.0 when day_adjustment slice is too short — ref: NONROAD2008a clcevems.f (adjems COMMON array from nonrdact.inc, sized MXPOL x MXDAYS, populated by getact/rdnract)
  - Added an up-front length check in calculate_evaporative_emissions: `day_adjustment.len() >= MXPOL * MXDAYS`, returning Error::Config on a short slice (mirroring the existing emsday/emsbmy and per-pollutant length pre-checks). In clcevems.f the `adjems(idxspc,day)` COMMON array is always fully populated by getact/rdnract, so a missing entry is a real data gap, not a no-op. With the validation in place the index `slot*MXDAYS+(jday-1)` is always in-bounds (max evap slot 17 < MXPOL=23, jday<=365), so the residual `.get().unwrap_or(1.0)` is now effectively unreachable and left as harmless defensive code. Existing tests already supply `vec![1.0; MXPOL*MXDAYS]`, so the new check does not break them.
- **crates/moves-nonroad/src/emissions/evaporative.rs** — Daily meteorology lookup substitutes 0.0 temps/RVP for out-of-range days — ref: NONROAD2008a clcevems.f:225-228 (daytmp(jday,1..3,ireg) / dayrvp(jday,ireg) from MXDAYS-sized COMMON arrays loaded by rddaily)
  - Changed Meteorology::for_day to return Result and, in the Daily arm, return Error::Config when the per-day temps/rvps slice does not cover the requested day instead of fabricating [0.0,0.0,0.0]/0.0. clcevems.f:225-228 reads daytmp/dayrvp from COMMON arrays fully populated to MXDAYS by rddaily; a missing day is a load-time error, never an in-loop default. Zero temps would silently disable the diurnal branch (tmax<DIUMIN) and zero RVP would corrupt displacement/diurnal vapor terms, masking a truncated .tmp/.rvp file as low/zero emissions. Updated the single call site to propagate with `?`. The Static arm returns Ok unchanged, so non-daily behavior is identical.
- **crates/moves-nonroad/src/emissions/evaporative.rs** — Ethanol-correction tech-code lookup defaults to uncontrolled ('0') when tech_type_code is too short — ref: NONROAD2008a clcevems.f / evemfclc.f:177-183 (fixed-width evtectyp E+8-digit code indexed at fixed offsets)
  - The reference confirms `evtectyp` is a fixed-width character string (`E` + 8 digits) so a code too short to hold a species' control-tech slot (index 2 or 3) is not a Fortran possibility. Added a debug_assert!(tech_type_code.len() > slot_idx, ...) before the slot lookup so a malformed/truncated code is caught in debug and test builds rather than being silently treated as uncontrolled (which would skip the E10-factor inflation to 2.0 and change the result). Did not thread a Result through apply_ethanol_correction and the five permeation branches because the reference guarantees fixed width and that restructuring would be disproportionate; the existing `.unwrap_or(b'0')` is retained for release-build safety. This matches the reference_concern's own suggested remedy (debug assert).
- **crates/moves-nonroad/src/emissions/exhaust.rs** — SOx EF rewrite substitutes cvtbck=0 on divide-by-zero instead of matching Fortran — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/clcems.f:213-232 (SOx block; cvtbck at :214)
  - clcems.f:214 computes `cvtbck = 1.0 / (hpval * faclod(iact))` unconditionally — there is no zero-guard. With hp*load==0, Fortran yields IEEE infinity, and then emsfac = hpval*faclod * (... - emsthc*cvtbck) = 0 * (-Inf) = NaN, surfacing the degenerate input. The Rust code fabricated `cvtbck=0`, which makes new_ef = 0 silently (a clean zero that hides the bad data). I removed the fabricated guard and compute `cvtbck = 1.0 / (hp_avg * load_factor)` exactly as Fortran does, so a degenerate hp*load==0 propagates a non-finite value rather than being silently zeroed. For all valid inputs (hp*load>0) the numeric result is unchanged.
- **crates/moves-nonroad/src/emissions/exhaust.rs** — CO2 EF rewrite substitutes cvtbck=0 on divide-by-zero instead of matching Fortran — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/clcems.f:245-261 (CO2 block; cvtbck at :246)
  - clcems.f:246 computes `cvtbck = 1.0 / (hpval * faclod(iact))` with no zero-guard, identical to the SOx branch. The fabricated Rust `cvtbck=0` silently dropped the THC back-out term and zeroed the always-computed CO2 EF on degenerate input. I replaced it with the unconditional `1.0 / (hp_avg * load_factor)` to match Fortran; hp*load==0 now propagates a non-finite value (surfacing the data gap) instead of producing a silent clean zero. Numeric behavior for valid (hp*load>0) inputs is unchanged.
- **crates/moves-calculators/src/calculators/airtoxics.rs** — FINDING 1: Absent fuelSubTypeID / fuelFormulationID in MOVESWorkerOutput silently default to 0 — ref: /tmp/canonical/moves-java/database/AirToxicsCalculator.sql (AirToxicsMOVESWorkerOutputTemp schema, lines ~448-470 — no fuelFormulationID column); /tmp/canonical/moves-java/calc/mwo/mworeader.go readMOVESWorkerOutput (lines 510-530: fuelSubTypeID/fuelFormulationID assigned only from the FuelSupply split)
  - Two-part response. (a) The 0-default for an ABSENT column is reference-faithful: in canonical MOVES the worker output genuinely carries no fuelFormulationID (the AirToxicsMOVESWorkerOutputTemp schema in AirToxicsCalculator.sql has no fuelFormulationID column, and the Go reader assigns FuelSubTypeID/FuelFormulationID per emission only via the FuelSupply split in mworeader.go readMOVESWorkerOutput). So reading an absent column as None/0 placeholder that execute re-derives is correct. (b) The HAZARD was the `.i32().ok()` swallowing a PRESENT-but-non-i32 column as None->0, masking schema drift unlike every other column (which uses get_i32 and errors). I replaced the `.ok().and_then(|c| c.i32().ok().cloned())` with an `opt_i32` closure: an absent column yields Ok(None) (expected), but a present column that fails the i32 cast now propagates a RowExtraction error via row_err, matching the strictness of the sibling get_i32/get_f64 readers. The genuine corruption path (fabricated fuel-id 0 silently passing through and missing every ratio) is fixed under Finding 2, not here.
- **crates/moves-calculators/src/calculators/airtoxics.rs** — FINDING 3: year defaults to 0 in the real execute path, collapsing every model-year-expanded ratio extract to empty — ref: /tmp/canonical/moves-java/database/AirToxicsCalculator.sql (##context.year## bound in every extract: minorHAPRatio lines 284-285, ATRatio FuelSupply join line 360-362, AT1FuelSupply line 379); crates/moves-framework/src/error.rs (no missing-context variant; Polars(String) is the generic string error)
  - Replaced `position_filter(ctx).year.unwrap_or(0)` with `.ok_or_else(|| Error::Polars(...))?` so a year-less position in the real execute path now returns an error instead of silently feeding 0 into the `start.max(year-40)..=end.min(year)` windows and `modelYearID = year - ageID` (which collapse every realistic model-year group to empty and emit zero toxics). Canonical SQL binds `##context.year##` for every real run (AirToxicsCalculator.sql uses it in every extract's modelYear/year join), so an absent year is a missing-context bug, not a zero-emission success. The framework Error enum has no dedicated missing-context variant, so I used Error::Polars(String), the generic-string variant already used elsewhere in this file (e.g. lines ~1931-1943). The synthesize_* helpers still take an explicit `year: i32` and are unaffected, so the unit tests that seed extracts directly (which pass concrete years) keep working; the single execute() test sets year: Some(2020).
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs** — FINDING 1 [high/fabricated-default] execute() substitutes 0 for missing run-context scalars (year/county/zone/state/link/fuelRegion) — ref: /tmp/canonical/moves-java/database/CriteriaStartCalculator.sql lines 87-89,104-105,116-117,222-224,294-296 (##context.year##, ##context.iterLocation.countyRecordID##, zoneRecordID); error variant: crates/moves-framework/src/error.rs RowExtraction (lines 98-110)
  - Replaced the `unwrap_or(0)` for year/county/link/state/zone with explicit `ok_or_else` errors. The canonical SQL resolves every `##context.*##` substitution (e.g. `##context.year##`, `##context.iterLocation.countyRecordID##`/`zoneRecordID##`) before the script runs; an unresolved one is a hard preprocessor failure, never a silent 0. When this calculator actually fires (Start Exhaust, off-network, after the loop has descended to LINK granularity) all five scalars are populated; a `None` means dispatch at a granularity the SQL never runs at, so it now returns Err(RowExtraction{table:"IterationPosition",...}) instead of fabricating 0. This prevents `modelYearID = year - ageID` becoming `-ageID` (every CSEC inner join silently missing) and prevents zone_id=0 short-circuiting the met_start_adjustment zone filter on a live path. No dedicated context-error variant exists in moves-framework error.rs, so I reused RowExtraction per its documented "value was null where a non-null value is required" case. The crate-wide sibling pattern (criteria_running_calculator) still uses unwrap_or(0); see followups.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs** — FINDING 2 [high/silent-fallback] execute() derives fuel_region_id from FuelSupply.first() and pol_process_ids from ALL PollutantProcessAssoc rows — ref: /tmp/canonical/moves-java/database/CriteriaStartCalculator.sql lines 339-358 (CountyFuelAdjustment INSERT, WHERE ppa.polProcessID IN (##pollutantProcessIDs##), ##context.fuelRegionID##); /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/ghg/CriteriaStartCalculator.java lines 30-33 (super(new String[]{"102","202","302"}))
  - Two parts. (a) pol_process_ids: now intersected with the calculator's capability set {102,202,302} (new const CAPABILITY_POL_PROCESS_IDS), recovering CSEC 2-a's only processing-section WHERE clause `polProcessID IN (##pollutantProcessIDs##)`, which is otherwise a no-op against the replayed snapshot that carries every Start Exhaust pair. The Java constructor passes exactly {102,202,302}. The finer RunSpec narrowing *within* {102,202,302} happens upstream at extract and cannot be applied here (the RunSpec is not exposed through CalculatorContext) — noted in followups. (b) fuel_region_id: the empty-FuelSupply `unwrap_or(0)` now errors instead of fabricating region 0 (faithful: an empty extract means no fuel supply, not region 0). The deeper fix — sourcing fuelRegionID from `##context.fuelRegionID##` rather than FuelSupply.first() (which is non-deterministic across multiple regions) — requires adding a fuel-region field to the framework's ExecutionLocation/IterationPosition and is deferred cross-file.
- **crates/moves-calculators/src/calculators/hcspeciation.rs** — FINDING 3: ThcWorkerRow::from_dataframe substitutes fuelSubTypeID/fuelFormulationID = 0 for absent or null values — ref: Internal contract of from_dataframe in the same file (null-cell handling, e.g. lines 1233-1254); MOVESWorkerOutput schema carries fuelSubTypeID but not fuelFormulationID
  - The old code `df.column(col).ok().and_then(|c| c.i32().ok().cloned())` conflated three cases and defaulted all to 0: (1) column absent by design (acceptable — the HCFuelSupply expansion in execute overwrites both), (2) column present but wrong dtype (silently swallowed), (3) column present but a specific cell null (coerced to the invalid key 0). I split these: a new `get_opt_i32` helper returns Ok(None) only when the column is genuinely absent (Err from df.column), and propagates a dtype error via row_err when the column is present but not i32 — matching the get_i32 helper used for every other column. In the per-row closure, an absent column still defaults to 0, but a present column with a null cell now errors via `.ok_or_else(|| null(...))`, exactly like every other column in this loader. Behavior on the live execute path is unchanged (the columns are absent there, so 0 still applies before being overwritten); the change only surfaces a present-but-null/wrong-dtype fuelSubTypeID/fuelFormulationID as a data error instead of fabricating key 0, which is not a valid MOVES key and would mis-key the methaneTHCRatio/HCSpeciation lookups. This is a robustness hardening with no numeric divergence; there is no canonical equivalent because MOVESWorkerOutput->row decoding is a port-specific ingestion step.
- **crates/moves-calculators/src/calculators/so2_calculator.rs** — Division by market-share-weighted energyContent can yield inf/NaN where MariaDB yields NULL — ref: /tmp/canonical/moves-java/database/SO2Calculator.sql lines 244-258 (division by energyContent)
  - calculate() is infallible and returns Vec, so it cannot propagate an Err without a signature change. MariaDB evaluates x/0 as NULL (SO2Calculator.sql lines 257-258 divide by fc1.energyContent), inserting a NULL emission row that contributes nothing downstream. Added a guard: when fc1.energy_content == 0.0 the row is skipped with continue, matching the existing INNER-JOIN-miss skip pattern and preventing non-finite values from entering output. Updated the module doc note that previously dismissed this divergence without guarding it.
- **crates/moves-calculators/src/generators/mesoscale_lookup/total_activity.rs** — Missing month/day/hour VMT-fraction rows silently treated as 0.0 instead of erroring — ref: MesoscaleLookupTotalActivityGenerator.java calculateVMTByRoadwayHour, INNER JOIN clauses at lines 872, 880-881, 893-894
  - The Java builds the activity tuples via INNER JOIN on MonthVMTFraction (USING sourceTypeID), DayVMTFraction (USING sourceTypeID, monthID, roadTypeID) and HourVMTFraction (USING sourceTypeID, roadTypeID, dayID). A tuple missing any join key is dropped by the inner join, not emitted with fraction 0. The port enumerated a cartesian product and substituted 0.0 for any absent fraction, fabricating zero-activity rows indistinguishable from legitimate zeros and silently understating activity. Hardened by replacing the three unwrap_or(0.0) lookups (plus the new weeks_per_month lookup) with a tuple let-else over Option<&f64>: when any required fraction (or the month's weeks divisor) is absent the tuple is skipped (continue), matching the inner-join domain. Present tuples are numerically unchanged. Used the existing let Some(&x) = map.get(..) else { } pattern already used elsewhere in the crate.
- **crates/moves-calculators/src/generators/mesoscale_lookup/total_activity.rs** — Missing linkAverageSpeed row yields distance 0.0 for that link — ref: MesoscaleLookupTotalActivityGenerator.java calculateDistance (lines 1112-1124); moves-framework/src/error.rs Error enum (no missing-data variant; Polars(String) used for ad-hoc errors)
  - Java calculateDistance runs `update SHO, LinkAverageSpeed set SHO.distance=SHO.SHO*averageSpeed where LinkAverageSpeed.linkID=SHO.linkID and SHO.distance is null` — an inner join on linkID; a link lacking a LinkAverageSpeed row is simply not updated and its distance stays NULL, never a fabricated 0. The port took the absent speed as 0.0, producing distance=0 for every row on that link and silently dropping its distance-based emissions. The ShoOutputRow.distance field is a non-nullable f64, so the Java NULL cannot be represented; the faithful hardening is to surface the missing-speed data error rather than emit a fabricated zero. Replaced avg_speed.get(..).unwrap_or(0.0) with a get(..).ok_or_else(..)? that returns Error::Polars naming the offending linkID (Error has no dedicated missing-data variant; Error::Polars is the existing ad-hoc-error variant already used for execute() failures in this file).
- **crates/moves-cli/src/run.rs** — Meteorology back-fill substitutes 0 for NULL keys and silently passes through temperature for unmatched rows — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/general/MeteorologyGenerator.java:112-267 (doHeatIndex: heatIndex UPDATE has no join and runs on all rows; specificHumidity/molWaterFraction UPDATE joins PV on monthID/zoneID/hourID leaving unmatched rows unchanged); cross-checked moves-calculators/src/generators/meteorology.rs:721-764
  - Hardened the NULL-key coercion: the `to_i32` closure used `v.unwrap_or(0)` on zoneID/monthID/hourID, so a NULL key became (0,..), could never match a generator-computed key, and fell into the None branch where it received a fabricated heatIndex=temperature with zero humidity. Canonical MOVES keys ZoneMonthHour on non-null (monthID,zoneID,hourID) and never produces a 0-key row; the sibling populate_sho_distances already rejects NULL keys via into_no_null_iter. I changed to_i32 to take the column name and return an Err ('ZoneMonthHour.<col> contains a NULL key value') on any NULL key, surfacing the corrupt input instead of hiding it. I left the legitimate unmatched-row fallback (zone with no County match -> heatIndex=temperature, humidity=0) in place: canonical's join-free heatIndex UPDATE leaves heatIndex=temperature for temp<78, and its specificHumidity/molWaterFraction UPDATE leaves unmatched rows at their default (0/NULL), so that path is reference-faithful. The one remaining narrow divergence (temp>=78 unmatched rows should get the heatIndex formula, not the temperature) lives in the generator's inner join in a different file and is recorded as a followup.
- **crates/moves-framework/src/aggregation/output_aggregate.rs** — UnitScaling placeholder (always returns 1.0) is the only TemporalScalingFactors used in the real engine, so WeeksPerMonth / PortionOfWeekPerDay rescaling is silently never applied — ref: AggregationSQLGenerator.java:1576-1600 (SUM(emissionQuant*weeksPerMonthClause) / SUM(...*portionOfWeekPerDayClause) via WeeksInMonthHelper)
  - Added a default trait method TemporalScalingFactors::supplies_temporal_factors() -> bool (default false; UnitScaling keeps the default, the test MonthAsFactor and a new IdentityRealFactors override it to true). Added a require_real_factors guard called at the top of aggregate_emissions, aggregate_activity, and StreamingEmissionAgg::extend that returns Error::AggregationPlanMismatch when the plan's SUM carries a TemporalScaling other than None but the supplied factor provider is the identity placeholder. Per plan.rs add_time_columns, EVERY onroad Year/Month timestep carries WeeksPerMonth and Day/Hour carries PortionOfWeekPerDay, and the production engine always passes &UnitScaling (engine.rs:391,824,898,908), so the run previously silently applied factor 1.0 and reported e.g. a single sample week as a whole month. The reference (AggregationSQLGenerator.java:1576-1600) multiplies the metric by getWeeksPerMonthSQLClause / getPortionOfWeekPerDayClause from monthOfAnyYear/dayOfAnyWeek. The guard now surfaces the missing real-factor wiring as a loud error instead of producing numbers that diverge from the reference. Numerically-correct None-scaling paths and real-factor paths are unchanged. The actual wiring of real factors into the engine (replacing &UnitScaling) is a cross-file change and is listed as a followup.
- **crates/moves-nonroad/src/input/gxr.rs** — get_gxr_factor returns 1.0 for out-of-bounds lookups — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdgxrf.f lines 105-112 (fixed global arrays indexed by nrcgrx, bounded by MXGXRF at 106)
  - Changed get_gxr_factor to return Result<f64> instead of f64, replacing `.unwrap_or(&1.0)` with `.ok_or_else(|| Error::Parse{...})`. The Fortran indexes fixed-size global arrays dimensioned for full coverage (fipgrx/ascgrx/hpgrx etc. up to MXGXRF), so an out-of-range access is a programming/coverage bug, not a 'no growth' default. Verified there are zero callers of get_gxr_factor anywhere in the crate (grep across crates), so the signature change is self-contained and breaks no live path. Numerically-correct in-bounds behavior is unchanged (returns the stored factor).
- **crates/moves-nonroad/src/input/seasonal.rs** — read_dat_records silently skips malformed lines and fabricates 1.0 for unparseable monthly factors — ref: NONROAD2008a /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdseas.f: record read at line 109 `read(line,'(51X,12F10.0)',ERR=7003)`; error label 7003 at lines 250-259; `ierr = IFAIL` set at line 66. Cross-checked against sibling read_dat in same file (lines 128-177) which already errors.
  - Hardened read_dat_records to propagate Error::Parse on every malformed condition instead of swallowing it, mirroring the sibling read_dat and the canonical rdseas.f. Specifically: (1) a record with <13 fields now returns Error::Parse instead of `continue`; (2) a non-numeric equipment index now returns Error::Parse instead of `continue`; (3) an unparseable monthly factor now returns Error::Parse instead of `val_str.parse().unwrap_or(1.0)`. I also added `line_num += 1` in the record loop so the error reports the correct 1-based line. In rdseas.f the monthly packet is parsed with `read(line,'(51X,12F10.0)',ERR=7003)`, which jumps to error label 7003 and leaves `ierr = IFAIL` (the routine fails) on any malformed/garbage record; the reference never skips a line or substitutes a default factor. This is a public-API entry point (`pub mod seasonal`, `pub fn read_dat_records`), so it is reachable; surfacing the error is the faithful and safe behavior. No numerically-correct behavior changed (valid files parse identically; the existing test_read_dat_records still passes).
- **crates/moves-calculator-info/src/chain.rs** — Registered calculator with no path to any subscriber is silently accepted as empty roots despite 'should flag these' comment — ref: gov/epa/otaq/moves/master/framework/MOVESInstantiator.java:1522-1606 (instantiate then separate subscribeToMe() pass, Logger.logError + continue, no abort); InterconnectionTracker.java (passive recorder, no validation); GenericCalculatorBase.java:230-238 doRegistration
  - Canonical MOVES does NOT treat a registered-but-unsubscribed/unchained calculator as a fatal error: registration happens in the calculator's constructor (EmissionCalculatorRegistration.register, e.g. GenericCalculatorBase.doRegistration), but subscription/chaining happens later in subscribeToMe(); MOVESInstantiator.instantiate() instantiates and registers all needed classes, then calls subscribeToMe() in a separate pass, and on any failure only logs via Logger.logError and continues (it never aborts the loop). So returning Err(Error::DagBuild) for an orphan would DIVERGE from canonical tolerance and over-reject. The faithful behavior is the empty-roots ExecutionChain already produced (MOVES would likewise never fire that calculator, so no emission output). I hardened the silent gap: build_execution_chains now emits an explicit stderr warning when a registered calculator resolves to zero roots, matching this crate's existing eprintln! CLI convention and the canonical log-and-continue pattern, and fulfilling the doc comment's 'should flag these' promise. I also updated the find_subscriber_roots doc to point at the new flagging site and document the mirror-MOVES rationale. The DAG output and the existing orphan test (registered_calculator_not_chained_to_any_subscriber_has_no_chains) remain unchanged (warning goes to stderr, not the returned value).
- **crates/moves-calculator-info/src/chain.rs** — GenericBase Java fill-in emits process_id=0 sentinel for 'all processes' — ref: gov/epa/otaq/moves/master/framework/GenericCalculatorBase.java:256-284 (subscribeToMe subscribes per real EmissionProcess; no process id 0) and :139-155/230-238 (constructor + doRegistration over real process ids)
  - Confirmed against GenericCalculatorBase.subscribeToMe (lines 256-284): MOVES subscribes the calculator once PER REAL EmissionProcess in pollutantsByProcess (targetLoop.subscribe(this, pollutants.process, ...)), always with a real process resolved from the execution DB — there is NO process_id=0 'all processes' subscription in canonical MOVES. The static Java scanner genuinely cannot recover these numeric ids (the constructor's pollutantProcessIDs is a String[] of polProcessIDs resolved against PollutantProcessAssoc at runtime; explicit subscribe() calls pass a runtime variable, not a literal). So process_id=0 is unavoidable from static scan, but the hazard is that 0 is silently ambiguous with a real process id. Since SubscriptionEntry.process_id is a required u32 in the serialized v1 schema (changing its type/sentinel space is a cross-file schema change for consumers), I kept process_id=0 (already paired with source=JavaSource, which flags provenance) but made the previously-empty process_name self-describing ('<unresolved: all RunSpec processes>' for GenericBase, '<unresolved process>' for an empty explicit expr) so a consumer reading calculator-dag.json cannot silently mistake the 0 for a real, named process. This changes no numeric/structural output and does not affect the faithful runtime-log path (out.is_empty() gate), and global_execution_order already excludes JavaSource entries (only CalculatorInfo). The existing java_subscription_fills_in_missing_module test does not assert on process_name, so it is unaffected.
- **crates/moves-calculators/src/calculators/criteria_running_calculator.rs** — execute() fabricates year/county/zone/link/state = 0 when the run position is missing them — ref: crates/moves-framework/src/execution/execution_db.rs:130-156 (ExecutionTime: 'year — Always present once the loop has entered YEAR granularity') and lines 60-128 (ExecutionLocation: None is the explicit 'not yet at this granularity' signal); SQL ##context.year## semantics mirrored by sibling so2_calculator.rs build_inputs and the already-hardened criteria_start_calculator.rs:3670.
  - Replaced the five `.map(...).unwrap_or(0)` fallbacks in execute() with `.ok_or_else(|| missing(field))?` so a missing run-context substitution surfaces as a hard `Error::Polars` instead of fabricating a 0. This is load-bearing: `ctx.year` drives `model_year_id = year - ageID` (lines 3153, 3479, 3663) and the `y.year_id != ctx.year` Year filter (line 3289), and county/zone/link/state are stamped onto worker-output keys (lines 3531, 3702-3703, 3746, 4200-4201). A year of 0 produces negative model years that fail every join and drops all fuel-supply rows (yearIDs ~2020 != 0), silently emitting an empty/wrong inventory. The framework's own ExecutionTime/ExecutionLocation docs state these are guaranteed present once the master loop has descended to the calculator's granularity, and this calculator is declared MONTH-granularity (line 4254), where the SQL preprocessor always resolves ##context.year## / ##context.iterLocation.*RecordID##. So the fallback was dead-but-dangerous; surfacing the error is the reference-faithful behavior. Used `Error::Polars(format!(...))` to match the house ad-hoc-error style in wiring.rs (emit_rows/write_scratch_table) and switched the year cast to the equivalent infallible `i32::from`. Verified the end-to-end test execute_returns_nonempty_dataframe_for_minimal_inputs (line 4873) still passes: it sets ExecutionLocation::link(26, 26161, 90, 5001) and ExecutionTime::year(2020), so all five required fields are Some.
- **crates/moves-calculators/src/calculators/liquid_leaking_calculator.rs** — execute() substitutes 0 for an absent master-loop year/location instead of erroring — ref: /tmp/canonical/moves-java/database/LiquidLeakingCalculator.sql LL-9 INSERT lines 377-398 (context.year / context.iterLocation.*RecordID stamping + SourceHours ageID join)
  - The silent `pos.time.year.map(...).unwrap_or(0)` (and the four location fields) fabricated 0 when the master-loop position lacked a required field. Per LiquidLeakingCalculator.sql LL-9 (the INSERT at lines 380-398), `##context.year##` and `##context.iterLocation.{state,county,zone,link}RecordID##` are stamped directly onto every MOVESWorkerOutput row and the SourceHours INNER JOIN keys on `sh.ageID = ##context.year## - w.modelYearID`; a 0 year makes every age join miss and writes physically wrong location-0 IDs, all silently. These macros are always substituted from the active iteration in canonical MOVES, so an absent value is an internal wiring error. I replaced the five `unwrap_or(0)` calls with `ok_or_else(|| Error::InvalidBundle(...))?`, surfacing the missing field explicitly without changing any numerically-correct path (the cast is now `value as i32` after `?` unwraps). `InvalidBundle` is the closest existing framework Error variant (the iteration position originates from the execution-DB bundle); no new variant was needed. The benign `unwrap_or(0)` in the scratch-table-name block above (line 2588-2593) is left as-is: it only builds an optional lookup name that falls back to the raw table on miss, and the new guard immediately after returns Err before any emission math runs.
- **crates/moves-calculators/src/calculators/nonroad_emission.rs** — NonroadEmissionCalculator returns empty output whenever inputs are empty, with evap entirely stubbed to zero — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/nonroad/NonroadEmissionCalculator.java:108-134 (subscribeToMe / doesHavePollutantAndProcess) and :144-150 (doExecute runs the simulation; no empty-population early-return)
  - The finding's premise is partly stale: the data plane is fully wired (nonroad_loader.rs builds inputs/reference data from the nr* tables, and emissions_to_dataframe maps SimEmissionRows onto the MOVESOutput schema; evap is mirrored as zero-fraction entries so the county routine's evap lookup succeeds, which is documented and off the numeric path). The real hazard the finding names is genuine: an empty inputs bundle caused by the population tables failing to load was indistinguishable from a legitimately nonroad-free slice. I hardened the empty-inputs branch to distinguish the two: if nrsourceusetype or nrbaseyearequippopulation is ABSENT from the execution store, the adapter now returns Err(Error::Nonroad(...)) naming the missing table(s); only when those tables are PRESENT but yield no driver records (zero population / all filtered out by the runspec sector/fuel selection) does it no-op with CalculatorOutput::empty(). This matches canonical: subscribeToMe (NonroadEmissionCalculator.java:108-134) only subscribes when doesHavePollutantAndProcess holds, so the loader was expected to admit these nr* tables for a nonroad-enabled run; their absence is a data-load failure, not a nonroad-free run. Note the loader already hard-errors on missing required columns (int_col/float_col panic) and a missing nrscrappagecurve (build_scrappage_curve panics), so the only remaining silent-zero gap was the wholly-absent population tables, now closed.
- **crates/moves-calculators/src/calculators/baseratecalculator/mod.rs** — FINDING 1: Missing/zero noOfRealDays divisor silently leaves universalActivity un-divided (~4.3x over-emit) — ref: /tmp/canonical/moves-java/database/BaseRateCalculator.sql Section GetActivity, Process1_9_10 (lines 530-539) and Process2 (lines 544-552): raw `SHO as activity`/`starts as activity`, INNER JOIN RunSpecHourDay; /tmp/canonical/moves-java/calc/baseratecalculator/baseratecalculator.go reads the pre-built universalActivity file (lines 217-226, 1519-1521)
  - Confirmed the canonical GetActivity SQL (database/BaseRateCalculator.sql lines 528-553) selects raw `SHO as activity` / `starts as activity` with an INNER JOIN to RunSpecHourDay; the noOfRealDays division is the port's own reconciliation (documented in build_universal_activity's doc comment: weekday/5, weekend/2, otherwise a constant ~4.3x over-emit). DayOfAnyWeek (noOfRealDays) is a fixed, always-populated MOVES reference table and HourDay maps every selected hourDayID to its dayID, so a missing/zero divisor for a row that actually carries SHO/Starts activity is a genuine data/wiring defect. The old closure silently substituted 1.0 (the exact ~4.3x inflation the comment warns about), masking the gap as a successful run. Hardened: the divisor closure now returns moves_framework::Result<f64> and yields Err(Error::AggregationPlanMismatch(..)) when no noOfRealDays divisor exists; both the SHO (proc 1/9/10) and Starts (proc 2) map branches now build Ok(row) with `divisor(..)?` and collect into a Result<Vec<_>>?, propagating through build_universal_activity's existing Result return and up through execute(). Numerically-correct runs (divisor present) are unchanged. Used the existing AggregationPlanMismatch variant since error.rs has no dedicated missing-data variant (did not edit error.rs). No tests broken: the in-file execute test uses process_id:None so this path is not hit, and the divisor is only ever invoked when SHO/Starts rows exist, i.e. the live inventory path that always supplies the reference tables.
- **crates/moves-calculators/src/calculators/baseratecalculator/mod.rs** — FINDING 2: County absent from County table yields gpa_fract=0.0, silently disabling GPA fuel-effect blending — ref: /tmp/canonical/moves-java/calc/baseratecalculator/baseratecalculator.go:97 (`var County map[int]*CountyDetail`), :1079, :1102, :1234-1235 (`County[fb.Key.CountyID].GPAFract` direct deref → nil-pointer panic when absent)
  - Verified in baseratecalculator.go that County is `map[int]*CountyDetail` (line 97) and the calculator dereferences `County[fb.Key.CountyID].GPAFract` directly (lines 1079, 1102, 1234-1235); a missing key returns a nil pointer and `.GPAFract` panics, so the Go genuinely fails fast on the broken invariant that the run's single county is always loaded — the finding's reference_concern is correct, and `map_or(0.0, ..)` silently disabled all GPA blending instead. A pure fail-fast fix that returns Err would require changing the public, #[must_use] BaseRateCalculator::run and the private run_with_prepared from BaseRateCalculatorOutput to Result, which would break the external integration test file crates/moves-calculators/tests/baseratecalculator.rs (12+ call sites that bind `output` and read output.blocks directly) — a file I may not edit. An unconditional panic would instead break the two legitimate in-file execute fixtures (lines 1684, 1857) that deliberately seed an empty County with empty ratio tables (the no-GPA-county case). So I hardened with a conditional fail-fast that matches canonical exactly where it matters: gpa_fract is read from the county if present; if the county is absent it panics ONLY when GPA blending is actually live (general_fuel_ratio / criteria_ratio / alt_criteria_ratio non-empty — the tables gpa_fract is blended against in process_fuel_block and the E85 block). With all ratio maps empty (rates-only / no-county fixtures) gpa_fract is never multiplied against anything, so the historical 0.0 default is preserved and no test breaks. This surfaces the real wiring defect (county missing while fuel-effect/criteria data is present) without altering numerically-correct behavior. NOTE: if a fully unconditional fail-fast (errored Result) is preferred to exactly mirror the Go panic, that is the cross-file change listed in followups.
- **crates/moves-calculators/src/generators/avg_speed_op_mode_distribution.rs** — avgBinSpeed null silently coerced to 0.0 on read-back, unlike every sibling column — ref: /tmp/canonical/moves-java/database/CreateExecutionRates.sql line 15 (avgBinSpeed FLOAT NULL, no DEFAULT); AverageSpeedOperatingModeDistributionGenerator.java lines 309-312 (always selects avgBinSpeed into the insert); consumer baserategenerator/aggregate.rs lines 404-405/510-511
  - Changed `avg_bin_speed.get(i).unwrap_or(0.0)` to `.ok_or_else(|| null("avgBinSpeed"))?`, matching every sibling column in this `from_dataframe`, and rewrote the justifying comment which was factually wrong. Verified against canonical sources: (1) CreateExecutionRates.sql declares `avgBinSpeed FLOAT NULL` with NO DEFAULT clause (unlike avgSpeedBinID/hourDayID which have `DEFAULT '0'`), so MOVES does NOT default it to 0.0; (2) the Java generator ALWAYS populates avgBinSpeed in all three of its `insert ignore into RatesOpModeDistribution (... avgBinSpeed) select ... avgBinSpeed` statements (lines 171-177 project step020, 283-286 mesoscale, 309-312 non-project rates-first), so a real value is always written on this path; (3) downstream rate aggregation in baserategenerator/aggregate.rs (lines 404-405, 510-511, 657-658) reads avg_bin_speed and DIVIDES by it (`t = op_mode_fraction * avg_speed_fraction / avg_bin_speed`), so a fabricated 0.0 from a NULL would silently zero out (guarded by `> 0.0`) instead of failing, corrupting tire-wear rates. The comment's claim that 'MOVES leaves avgBinSpeed NULL (default 0.0)' is unsupported by the reference; a NULL here is a genuine data gap and should error like the other columns.
- **crates/moves-calculators/src/generators/project_tag.rs** — ProjectTAG::execute is a no-op stub that ships in the production registry and silently produces zero project-domain activity — ref: ProjectTAG.java:142 (executeLoop -> allocateTotalActivityBasis) and :255 (allocateTotalActivityBasis); contrasted with moves.rs totalactivitygenerator/mod.rs:3176-3257
  - Confirmed against canonical Java: ProjectTAG.executeLoop (ProjectTAG.java:142) unconditionally calls allocateTotalActivityBasis (line 255), which writes the SHO/SHP/Starts/HotellingHours/SourceHours scratch tables that BaseRateCalculator and the emission calculators consume. The Rust execute() ignored ctx and returned CalculatorOutput::empty(), so a project-domain run (ProjectTAG is registered at lib.rs:279) would silently yield zero/grossly-truncated emissions instead of the values the Java produces. The doc comment's claim that this 'mirrors TotalActivityGenerator's stub convention' is false: TotalActivityGenerator::execute (totalactivitygenerator/mod.rs:3176-3257) actually reads inputs, runs the kernels, and writes 12 scratch tables. The fully-correct fix (project ProjectTagInputs from ctx.tables(), call allocate_total_activity_basis on the position's process, write five scratch tables) requires TableRow impls and schema-registry entries for the project-domain input row types (link, linkSourceTypeHour, offNetworkLink, avft) — these live in moves-framework's schema_registry/conversions, a different file, and do not yet exist (iter_typed would error 'table not found'). So instead of fabricating a fix in another file, I hardened the live path: execute now returns Err(Error::NotImplemented) (the variant documented as 'Returned by trait impls whose body hasn't been ported yet'), which the engine propagates via g.execute(ctx)? (engine.rs:250), making a project-domain run fail loudly rather than emit silent zeros. Also corrected the module doc comment and execute's inline comment to stop claiming parity with TotalActivityGenerator and to record the missing wiring.
- **crates/moves-calculators/src/generators/totalactivitygenerator/activity.rs** — Non-restricted road retention keyed on exact float equality of summed hotelling hours — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/ghg/TotalActivityGenerator.java lines 1844-1848 (DELETE FROM VMTByAgeRoadwayDay ... sum(hotellinghours) ... > 0 / count(hotellinghoursperday) = 0)
  - Changed `restricted_hotelling == 0.0` to `restricted_hotelling <= 0.0`. The canonical DELETE (TotalActivityGenerator.java line 1844-1848) removes a non-2/4 road row when `(select sum(hotellinghours) from VMTByAgeRoadwayDay WHERE roadTypeID in (2,4)) > 0` OR `(SELECT count(*) from hotellinghoursperday) = 0`; equivalently it KEEPS non-2/4 roads only when the restricted-access hotelling sum is NOT `> 0` (i.e. `<= 0`) AND the hotellingHoursPerDay table is non-empty. The faithful Rust expression of the complement of the reference's `> 0` predicate is `<= 0.0`, not exact `== 0.0`. With non-negative hotelling hours (vmt*factor*rate, all >= 0) a tiny positive rounding residue now correctly counts as 'has restricted-access activity' — matching the Java `> 0` test — instead of being at risk of an exact-equality mismatch. Numerically-correct behavior for the common (sum exactly 0 vs clearly positive) cases is unchanged; only the float-drift edge is now aligned with the reference. Also added a clarifying comment.
- **crates/moves-default-db-convert/src/validate.rs** — Missing source TSV downgrades the whole cross-check to a non-failing warning — ref: crates/moves-default-db-convert/src/validate.rs module header lines 28-33 (transitivity contract); has_errors() lines 94-101; src/bin/validate.rs lines 81-85 (exit code). No Java/Fortran canonical exists for this harness.
  - This file is a self-contained validation harness for the Rust port's own TSV->Parquet conversion pipeline; it has no Java/Fortran canonical equivalent. The 'reference' is the module's own documented transitivity contract (lines 28-33: TSV == MariaDB ground truth, the sole thing certifying Parquet content). The old code recorded 'parquet has rows but no source TSV' only as FindingKind::Warning, which has_errors() (lines 94-101) does not count, so bin/validate.rs:81-85 exits 0 even though the entire row-total/aggregate/first-row cross-check was skipped -- shipping the data plane unverified while CI shows green. I changed the missing-TSV path to push FindingKind::RowContentError (an existing error kind that is counted by has_errors() and is semantically accurate: 'cannot cross-check Parquet content against source') whenever the table is non-empty. I also broadened the non-empty test from `parquet_rows != 0` to `parquet_rows != 0 || entry.row_count != 0` so a manifest that claims rows but produced zero Parquet rows is also flagged. Truly empty tables (both zero) still produce no finding, matching the original intent. Existing tests all provide both TSV files (round_trip_setup), so none exercise this path and none break.
- **crates/moves-framework/src/execution/engine.rs** — Chained-calculator execute() errors are silently swallowed and the chain continues — ref: OutputProcessor.java / AggregationSQLGenerator.java chainCalculator path; canonical runs chained calculators unconditionally and propagates data/SQL failures. Rust executor.rs:429-470 confirms closure errors propagate.
  - Replaced the catch-all `match out { Err(e) => { ...debug-gated eprintln...; continue } }` with `?` so a hard execute() failure propagates as a run error. This matches canonical `chainCalculator`, which runs each chained calculator unconditionally against the accumulated MOVESWorkerOutput and lets a data/SQL failure surface as a run error rather than zeroing the calculator's speciated/chained pollutants. The legitimately-empty case (calculator not relevant to this RunSpec) is unchanged: it is still represented by `Ok(None)` and `df.height()==0`, handled by the subsequent `let Some(df) = out else {...}` and the height/pollutantID guards which `continue` with an empty contribution. Only the genuine-failure `Err` path changed, so numerically-correct behavior is preserved while the previously-hidden failure becomes explicit. The enclosing closure returns Result<()> and the executor collects/propagates closure errors (executor.rs:429-470), so `?` compiles and propagates correctly.
- **crates/moves-framework/src/input/input_data_manager.rs** — execute() silently skips merge-plan tables that are absent from the Parquet snapshot (UnknownTable swallowed) — ref: InputDataManager.java:33-36 (allowMissingTables=true default; set false for the default DB at lines 68/99) and InputDataManager.java:3569-3578 (merge: mdTableName empty + allowMissingTables==false => throw "The Table X does not exist in the source database ... The merge is canceled"); the continue/skip path at 3569-3571 applies only when allowMissingTables==true, i.e. optional user databases.
  - `execute` operates on the pristine DefaultDb (the Parquet default-DB snapshot), which in canonical MOVES is merged with allowMissingTables==false: a source table missing from the default DB is a hard error that cancels the merge, NOT a silent skip. The Rust code converted DefaultDbError::UnknownTable to `return None`, mirroring the user-database (allowMissingTables==true) branch instead of the default-DB branch and silently dropping a calculator-required input table. Changed the UnknownTable arm to return Some(Err(Error::Polars(...))) with a canonical-style message ("table '{name}' does not exist in the default-DB snapshot; the merge is canceled"), so a missing default-DB table now propagates as an error. Used Error::Polars because (a) error.rs has no dedicated missing-table variant and I may not edit it, and (b) Polars is already the variant this same match arm uses to wrap DefaultDbError values, keeping the function's error surface consistent. The SchemaOnly arm is left as a silent skip — those tables are runtime-populated and faithful to the optional path. Also updated the execute() doc comment (the docstring previously justified silently skipping absent tables; it now states a missing table is a hard error and documents the new error condition). No existing test depended on the swallow (execute_skips_schema_only_tables exercises only the SchemaOnly arm), so this does not break tests.
- **crates/moves-importer-pdb/src/validate.rs** — Fraction-sum check silently skips rows with null linkID or null fraction — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/importers/LinkSourceTypeHourImporter.java lines 182-198 (SUM(sourceTypeHourFraction) GROUP BY linkID HAVING ROUND(...) <> 1.0000); schema NOT NULL enforced by crates/moves-importer-pdb/src/csv_reader.rs lines 172-180
  - linkID and sourceTypeHourFraction are both declared NOT NULL (nullable:false) in LINK_SOURCE_TYPE_HOUR, and csv_reader.rs (lines 172-180) already rejects empty non-nullable cells with Error::MissingRequiredCell at read time, so a null can never legitimately reach this loop. The canonical SQL uses SUM(), which treats NULL as contributing nothing (the row still exists in its GROUP BY group), so it does not numerically diverge for NULL fractions — but the silent `continue` here could drop a row and let the remaining rows still total 1.0, masking corruption that should fail. Replaced the silent `continue` with an explicit Error::Validation. This is pure hardening: it cannot change behavior on valid (non-null) data since these columns are guaranteed non-null upstream, but it surfaces upstream corruption instead of hiding it. Applied the same null guard to the new OpModeDistribution sum loop for the same NOT NULL columns.
- **crates/moves-nonroad/src/geography/process.rs** — bsfc lookup in county/subcounty fuel formula falls back to 0.0 on out-of-bounds index, zeroing fuel consumption — ref: NONROAD2008a prccty.f :523 (fulbmy = ... * bsfc(idxyr,i) / denful), bsfc declared :145 as bsfc(MXAGYR,MXTECH); emfclc.f populates it; crate error.rs :51-52 (Error::Config)
  - Replaced `.get(...).copied().unwrap_or(0.0)` with `.get(...).ok_or_else(|| Error::Config(...))?` so an out-of-bounds bsfc index now propagates an explicit error instead of silently substituting 0.0. Confirmed against the canonical Fortran: `prccty.f` :522-524 directly indexes `bsfc(idxyr,i)` where bsfc is the preallocated `bsfc(MXAGYR,MXTECH)` (declared at prccty.f :145) that `emfclc.f` fully populates for every (year,tech) the loop visits. The Fortran has no 0.0 fallback; a missing entry there would be an out-of-bounds read, i.e. a table/shape bug, never a legitimate physical 0. A zero bsfc would silently zero fuel consumption and the bsfc-derived CO2/SO2 chemistry, so surfacing the mismatch is the correct hardening. Behavior is numerically unchanged on the happy path (the index resolves exactly as before to factors.bsfc[idxyr0*n_tech+tech_idx]). Used Error::Config since it is the crate's invariant-violation variant (error.rs :51-52) and is already used elsewhere in this file for internal-invariant failures.
- **crates/moves-nonroad/src/geography/process.rs** — Per-year exhaust tech miss silently skips the model year without warning — ref: NONROAD2008a prccty.f :440-442 (idxtch=fndtch(...,tchmdyr) used as ntech(idxtch) with no guard); fndtch.f :100-105 (skips entries with inyr < iyrtch, returns 0 on no match); nonrdeqp.inc :98 (ntech(MXTECD), 1-based); contrast prcnat.rs :459 / prcus.rs :545 / state.rs :678 recording MissingExhaustTech
  - On a per-year exhaust find_exhaust_tech miss the code still skips the model year (return Ok(())), but now pushes a ProcessWarning{kind: MissingTechType} first so the dropped year is auditable, and the misleading comment claiming the Fortran 'silently falls through' was corrected. Checked the reference: prccty.f :440 re-resolves `idxtch = fndtch(asccod,hpval,tchmdyr)` and immediately uses it at :442 (`do 70 i=1,ntech(idxtch)`) with NO `idxtch .LE. 0` guard, unlike the up-front lookup at :251-252 which does check. ntech is declared 1-based `ntech(MXTECD)` (nonrdeqp.inc :98), so a miss (idxtch=0) reads ntech(0) out-of-bounds in Fortran — there is no clean 'skip the year' semantic; it is undefined behavior. fndtch.f matches the year 'closest to but not less than' the supplied year, so a per-year miss at tchmdyr=min(iyr,itchyr) is legitimately possible for early model years where the SCC's tech data starts after tchmdyr even though the up-front lookup at the larger itchyr matched. Either way (legitimate-late-data or data bug), silently dropping the model year understates emissions with no diagnostic trail, while the sibling missing-activity/up-front-tech paths in this file and the prcnat/prcus/state ports all record a warning. I kept the skip (no hard crash) but made it visible via the existing MissingTechType WarningKind available to this file.
- **crates/moves-nonroad/src/geography/process.rs** — Per-year evap tech miss silently skips the model year without warning (symmetric to Finding 2, same file) — ref: NONROAD2008a prccty.f :573-575 (idxevtch=fndevtch(...,tchmdyr) used as nevtech(idxevtch) with no guard)
  - Applied the same hardening to the evap-side per-year miss in run_evap_block (find_evap_tech miss): push a MissingTechType warning before skipping the model year. This was not a separately enumerated finding but is the identical hazard in the same assigned file, and Finding 2's reference_concern explicitly contrasts with prcnat recording warnings on the tech-miss path. The reference is symmetric: prccty.f :573 `idxevtch = fndevtch(asccod,hpval,tchmdyr)` is used at :575 `nevtech(idxevtch)` with no `.LE. 0` guard, so a per-year evap miss is likewise an out-of-bounds read in Fortran rather than a clean skip. Behavior otherwise unchanged.
- **crates/moves-nonroad/src/input/growth.rs** — Malformed growth-factor lines silently skipped, leaving fabricated 1.0 — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdgrow.f lines 148-149 (read with ERR=7001) and 263-270 (error handler 7001 sets failure)
  - Replaced the silent `continue` on short lines (parts.len() < 3) in read_grw with a hard Error::Parse. The canonical rdgrow.f reads each /GROWTH/ record via `read(IORGRW,8001,ERR=7001,END=333) line` and treats any read error as a fatal error (goto 7001 -> sets ierr=IFAIL and returns); it never silently skips a malformed record. The pre-filled 1.0 default array is for legitimately *absent* (county,equipment) combinations (the Fortran writes only the present records to the sorted scratch file, leaving others to their natural default), but a *present-but-truncated* line is a data corruption that must surface. The function already errored on non-numeric indices/factors on the very next lines, so this makes the short-line case consistent with that.
- **crates/moves-nonroad/src/input/growth.rs** — read_grw_records swallows bad indices and bad growth factors — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdgrow.f lines 148-149 (read with ERR=7001), 263-270 (fatal error handler); cross-checked against read_grw in same file (lines 124-155) for matching Error::Parse shape
  - Replaced the three silent fallbacks in read_grw_records: `parts.len() < 3` -> continue, bad county_idx -> continue, bad equipment_idx -> continue, and `parts[2].parse().unwrap_or(1.0)` for the growth factor. All now return Error::Parse, matching read_grw's behavior and rdgrow.f's fatal ERR=7001 read-error semantics. The unwrap_or(1.0) was the worst offender: a garbled growth factor silently became 'no growth' (1.0), corrupting projected populations with no diagnostic. Added `line_num += 1` to the record loop (line_num was already declared and seeded by the header loop) so error reports cite the correct line.
- **crates/moves-nonroad/src/population/growth.rs** — Short FIPS string substitutes a fabricated '00' state prefix that collides with the national sentinel — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/grwfac.f :113 (stfips = infips(1:2)//'000', character*5 infips)
  - grwfac.f :113 forms the state key as infips(1:2)//'000' where infips is a fixed-width character*5 and so always has >= 2 chars; the Fortran can never receive a malformed short FIPS. The Rust port takes a &str and used `fips.get(..2).unwrap_or("00")`, which fabricates a "00" prefix for a sub-2-char FIPS, yielding st_fips "00000" -- identical to the national-data sentinel -- so a malformed FIPS would silently match national growth data instead of erroring. Hardened by replacing the unwrap_or fallback with `fips.get(..2).ok_or_else(...)?` returning Error::Config (the same variant already used for this function's invariant violations), surfacing a malformed FIPS as a data error. Behavior for all well-formed (>= 2-char) FIPS is unchanged.
- **crates/moves-wasm/src/default_db.rs** — FuelSupply marketShare/marketShareCV NULLs blanket-filled with 0.0 with no fuelFormulationID=0 guard — ref: moves-cli/src/run.rs (no FuelSupply fill); crates/moves-calculators/src/calculators/criteria_running_calculator.rs:1219 (strict NULL -> error); /tmp/canonical/moves-java/database/CreateDefault.sql:764-771 (FuelSupply schema)
  - Confirmed hazard. The CLI/native path has no FuelSupply NULL fill; its strict extractor FuelSupplyRow::extract (criteria_running_calculator.rs:1219) errors via `market_share.get(i).ok_or_else(|| null("marketShare"))?` on any NULL marketShare. The wasm helper fill_f64_nulls coerced EVERY NULL in marketShare/marketShareCV to 0.0 unconditionally, never filtering on fuelFormulationID, so a genuine data gap on a real fuel row would be silently zeroed (dropping that formulation's blend-weighted contribution) instead of surfacing. The canonical schema (CreateDefault.sql:764-771) declares marketShare/marketShareCV as FLOAT NULL, consistent with only the documented all-zero placeholder (fuelFormulationID=0) carrying NULLs. Replaced fill_f64_nulls with fill_fuel_supply_placeholder_nulls(store) -> Result<(),String>: it locates the fuelFormulationID column, zero-fills NULLs ONLY on placeholder rows (fuelFormulationID==0), and returns an Err for a NULL marketShare/marketShareCV on any real (fuelFormulationID != 0) row, restoring the native strict-extractor failure behavior. Updated the call site in setup_execution_store to propagate the Result with `?`. Numerically-correct behavior (the legitimate placeholder fill) is preserved.
- **crates/moves-wasm/src/lib.rs** — run_nonroad_simulation ships using PlanRecordingExecutor and returns zero emissions — ref: NONROAD/NR08a/SOURCE/nonroad.f (per-record geography dispatch loop); crates/moves-nonroad/src/main.rs:22,31 (native path also uses PlanRecordingExecutor — dry run); crates/moves-nonroad/src/simulation/executor.rs:364-393,436-454 (ProductionExecutor needs ReferenceData with NOT-YET-LOADABLE loaders)
  - The audit's premise that the native orchestrator uses ProductionExecutor is incorrect: the native CLI crates/moves-nonroad/src/main.rs (lines 22, 31) also drives run_simulation with PlanRecordingExecutor and is explicitly documented as an 'empty run' stub. ProductionExecutor::new requires a fully-loaded ReferenceData bundle whose loaders (temporal-factor NR*.TMF, emission-factor NR*.EMF, spillage NR*.SPL, allocation NR*.SCO/.ALO) are explicitly marked 'NOT YET LOADABLE' in executor.rs (lines 364-393) and cannot be assembled from a .POP file alone — so wiring it into wasm is a cross-crate effort I cannot do in this file. Instead I HARDENED the user-facing surface so the no-emissions condition is no longer silent: the result now reports emission_rows (outputs.rows.len(), always 0 with PlanRecordingExecutor) and a produces_emissions boolean, and the doc comment now states callers MUST check produces_emissions before treating the run as numerical. No numeric behavior changed; the misleading implicit success is now explicit.
- **crates/moves-calculator-info/src/java.rs** — Symbolic priority offset silently collapsed to offset 0 (unadjusted base) — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/framework/GenericCalculatorBase.java (subscribeToMe lines 281-282: MasterLoopPriority.EMISSION_CALCULATOR+priorityAdjustment; priorityAdjustment field line 109/146); EvaporativePermeationCalculator.java super(...) passes 1; MasterLoopPriority.java (EMISSION_CALCULATOR=10, GENERATOR=100, INTERNAL_CONTROL_STRATEGY=1000)
  - Changed the symbolic-offset branch in parse_priority_expr (the `digit_end == 0` case, e.g. `MasterLoopPriority.EMISSION_CALCULATOR+priorityAdjustment`) to return None instead of fabricating `Priority { base, offset: 0 }`. The caller at find_target_loop_subscribe_calls (line 450-452) already does `continue` on a None priority, so an unresolvable symbolic offset now skips that subscribe record rather than emitting a wrong priority that could misorder a calculator within its granularity bucket.  Why hardening (not a numeric fix) and why this is the right call: I verified against canonical MOVES that this symbolic branch is NOT on the live path for any real or fixture calculator. (1) Real GenericCalculatorBase subclasses take the separate find_generic_base_subscription path (java.rs:256-268 -> find_first_int_after, line 262), which correctly reads the integer priorityAdjustment from the subclass `super(...)` call. EvaporativePermeationCalculator.java is the only subclass passing a nonzero offset (1); all others pass 0. That path never reaches parse_priority_expr. (2) The symbolic `+priorityAdjustment` literal appears exactly once in the whole canonical tree, inside GenericCalculatorBase.subscribeToMe (lines 281-282); when that file is scanned it goes through the explicit path but its granularity arg is the lowercase variable `granularity`, so find_granularity_constant returns None and the call is skipped (line 444-446) before priority matters (confirmed by the existing explicit_subscribe_priority_with_symbolic_offset test). (3) A grep of every targetLoop.subscribe call shows no real calculator uses a symbolic offset; the only non-numeric form anywhere is `+priorityAdjustment`. So the change preserves all numerically-correct behavior (literal offsets like GENERATOR-1/-2/-3/+1, and the genuine no-offset `offset: 0` at line 487 for a bare `MasterLoopPriority.X`) while removing the latent hazard of silently fabricating offset 0 if a future/edge calculator ever used a symbolic offset in an explicit subscribe.
- **crates/moves-calculators/src/calculators/basicstartpm.rs** — execute() fabricates roadTypeID=0 and year/location=0 when position/Link rows are absent — ref: /tmp/canonical/moves-java/database/BasicStartPM25Calculator.sql lines 112-113 (FROM Link WHERE linkID=##context.iterLocation.linkRecordID##), 354 (stmy.modelYearID=##context.year##-acat.ageID), 442-447 (output stateID/countyID/zoneID/roadTypeID); cross-checked against already-hardened sibling crates/moves-calculators/src/calculators/basicbraketirepm.rs:2740-2777
  - Replaced the six `unwrap_or(0)` defaults in `execute()` (road_type_id, year, state_id, county_id, zone_id, link_id) with `ok_or_else(|| row_err(...))` so a missing Link extract or missing position/context value becomes an explicit Error::RowExtraction instead of a silent zero. The canonical BasicStartPM25Calculator.sql substitutes ##context.year## and ##context.iterLocation.*RecordID## as concrete constants into every iteration (e.g. line 354 `stmy.modelYearID = ##context.year## - ageID`; lines 442/447 write roadTypeID/stateID/countyID/zoneID/linkID to MOVESWorkerOutput) and extracts roadTypeID via `FROM Link WHERE linkID = ##context.iterLocation.linkRecordID##` (lines 112-113); there is no '0' default. With year=0 the model-year/age arithmetic goes negative, no SourceTypeModelYear matches, and the calculator silently emits zero; roadTypeID=0 is an invalid road type. This matches the already-hardened sibling PM calculator basicbraketirepm.rs (lines 2740-2777) from the same BasicPM25/start family. row_err and the Result<_, Error> return type already exist in this file (row_err defined at line 1026), so no other file needs editing; behavior is unchanged when context is populated.
- **crates/moves-calculators/src/calculators/co2ae_running_start_extended_idle.rs** — Stale module doc claims execute() is an inert empty-output stub, but it is fully wired and live — ref: /tmp/canonical/moves-java/database/CO2AERunningStartExtendedIdleCalculator.sql (two-step insert-then-read-back Processing section) and /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/ghg/CO2AERunningStartExtendedIdleCalculator.java (doExecute CO2Step1A/Step2 process-ID macros)
  - Confirmed the finding: execute() at lines 1293-1322 is fully wired and on the live path. It reads RunSpecPollutantProcess to derive the Step 1a/Step 2 process filters, reads the SQL-extracted input tables (FuelSupply, FuelFormulation, FuelSubtype, Year, MonthOfAnyYear, Pollutant, MOVESWorkerOutput) via ctx.tables().iter_typed, materialises Co2aeInputs, calls self.calculate, and emits the combined Atmospheric CO2 + CO2 Equivalent rows via crate::wiring::emit_rows. The old 'Data plane' doc section (lines 117-126) falsely asserted execute 'cannot yet read MOVESWorkerOutput nor write the CO2 rows back' and is 'a documented shell returning an empty CalculatorOutput', which would mislead an auditor into treating an emission-producing calculator as inert. Replaced that section with an accurate description of the wired execute(). Verified the SQL two-step insert-then-read-back ordering against the reference and against calculate() itself: lines 1185-1188 run atmospheric_co2_rows then co2_equivalent_rows(inputs, &atmospheric_co2), and co2_equivalent_rows (line 1130) iterates inputs.worker_output.chain(atmospheric_co2.iter()), reproducing the SQL's read-back of the freshly-inserted CO2 rows. No numeric behavior changed; only the doc comment was corrected.
- **crates/moves-calculators/src/calculators/ch4n2o_running_start.rs** — Stale module doc claims execute() is a documented shell returning empty output, but it is fully wired — ref: /tmp/canonical/moves-java/CalculatorInfo.txt lines 505-506 (Registration	Nitrous Oxide (N2O)	6	Running Exhaust	1	BaseRateCalculator / Start Exhaust	2	BaseRateCalculator); cross-checked against execute() at ch4n2o_running_start.rs:1955-1968 and registrations() at :1941-1943
  - Confirmed the finding: execute() at lines 1955-1968 is fully wired — it calls build_running_inputs/build_start_inputs (which read the DataFrameStore tables) and crate::wiring::emit_rows to emit computed MOVESWorkerOutput rows, dispatching by master-loop process_id. The old module doc (lines 101-110) falsely claimed ExecutionTables/ScratchNamespace are placeholders, that execute 'cannot yet read the input tables nor emit MOVESWorkerOutput', and that it 'is a documented shell returning an empty CalculatorOutput'. Verified against CalculatorInfo.txt lines 505-506 that N2O (6) Running Exhaust (1) and Start Exhaust (2) are registered to BaseRateCalculator, so this calculator is kept off the live path by registrations() returning an empty slice (registration-gating), not by a stub. Rewrote the doc to describe the real wired execute() and the registration-gating mechanism, removing the conflation of 'not registered' with 'stubbed-out'. Doc-only change (//! comment); no behavior or compilation impact.
- **crates/moves-calculators/src/calculators/nh3/running.rs** — execute() substitutes 0 for an absent year/state/county/link master-loop position instead of erroring — ref: /tmp/canonical/moves-java/database/NH3RunningCalculator.sql lines 101,117,168-169,179,192,201-208,217-218,225-226 (context macros); NH3RunningCalculator.java line 31 (MasterLoopGranularity.YEAR). Crate pattern: crates/moves-calculators/src/calculators/basicstartpm.rs lines 2397-2411.
  - Replaced the four `.unwrap_or(0)` fallbacks in execute() (lines 752-757) with `.ok_or_else(|| row_err(...))?` guards that surface an Error::RowExtraction when any required master-loop position dimension is None. Verified against the canonical SQL (database/NH3RunningCalculator.sql): `##context.year##` is substituted into MYMAP(##context.year## - ageID) at line 117 and the SourceTypeModelYear model-year window (lines 201-208, 225-226); `##context.iterLocation.countyRecordID##` filters the I/M county join (lines 101, 168) and `##context.iterLocation.yearID##` filters it (line 169); `##context.iterLocation.linkRecordID##` drives the SHO/OpModeDistribution/Link joins (lines 179, 192, 217). At the YEAR MasterLoopGranularity this calculator subscribes (NH3RunningCalculator.java line 31) these macros are always concrete, so a None is a context/framework bug; a fabricated 0 silently zeroes or corrupts output (negative modelYearID, dropped I/M coverage, dropped activity rows). The fix uses the in-file `row_err` helper (running.rs line 256, signature matches) and mirrors the exact pattern already used by the sibling GenericCalculatorBase calculator basicstartpm.rs (lines 2397-2411). I kept state_id guarded for uniformity even though the NH3 processing SQL does not consume it, matching basicstartpm and avoiding a fabricated identifier. No numeric behavior changes on the valid path. The parallel nh3/start.rs (lines 705-709) has the identical defect but is a different file; see followups.
- **crates/moves-calculators/src/calculators/nh3/start.rs** — execute() substitutes 0 for an absent year/state/county/zone/link position instead of erroring — ref: /tmp/canonical/moves-java/database/NH3StartCalculator.sql lines 109, 174, 226-227 (##context.year##, ##context.iterLocation.zoneRecordID##/.countyRecordID##/.linkRecordID## macros); crate precedent crates/moves-calculators/src/calculators/nh3/running.rs:762-775
  - Replaced the five `.unwrap_or(0)` fallbacks in execute()'s StartContext construction with `.ok_or_else(|| row_err("Starts", 0, <col>, ...))?` so a missing required context dimension surfaces as an Error instead of being silently fabricated as 0. Confirmed against canonical NH3StartCalculator.sql, which substitutes ##context.year##, ##context.iterLocation.zoneRecordID##, .countyRecordID## and .linkRecordID## directly into its queries; the master loop always supplies these at YEAR/zone granularity, so a None is a context/framework bug. A zone_id of 0 makes multiply_by_activity's `st.zone_id != ctx.zone_id` guard reject every Starts row (no real zone is 0), and a year_id of 0 drives model_year_id (st.year_id - st.age_id, mirroring ##context.year## - ageID) negative, silently zeroing emissions. This change preserves all numerically-correct paths (valid contexts behave identically) and matches the exact precedent already applied in the sibling running.rs (lines 762-775). Uses the row_err helper defined locally at start.rs:242 and the existing Error type, so it compiles.
- **crates/moves-calculators/src/calculators/welltopump/ch4n2o.rs** — execute fabricates target_year = 0 (then first Year row) when context year is absent, silently corrupting GREET year-interpolation — ref: /tmp/canonical/moves-java/database/CH4N2OWTPCalculator.sql lines 91-147 (##context.year## substituted with no fallback); precedent crates/moves-calculators/src/calculators/criteria_start_calculator.rs:3664-3670
  - Confirmed against CH4N2OWTPCalculator.sql: `##context.year##` is substituted as a bare literal into the GREET bracket queries (GREETWellToPumpLoCH4N2O / HiCH4N2O, lines 91/97/106/112), the interpolation fraction `(##context.year## - wtpflo.yearID)/...` (lines 118-121), and the `y.yearID = wf.yearID` join (line 147). There is no 'use first Year row' or 'use 0' fallback anywhere in the reference; an absent context year is a configuration error. The previous `.unwrap_or_else(|| year.first()...unwrap_or(0))` silently clamped every WTP CH4/N2O factor to the earliest tabulated GREET year (lo_year==hi_year) instead of the run-year-interpolated rate, producing wrong emissions with no error. Replaced with `pos.time.year.map(i32::from).ok_or_else(|| Error::RowExtraction{...})?`, propagating the missing context year, exactly mirroring the established precedent in CriteriaStartCalculator (criteria_start_calculator.rs:3664-3670) which reuses RowExtraction keyed to a synthetic 'IterationPosition' table for unresolved run-context scalars. Also updated the in-file test `execute_returns_nonempty_dataframe_for_minimal_inputs`, which previously used `CalculatorContext::with_tables` (default position with `time.year = None`); it now supplies a position carrying `ExecutionTime::year(2020)` via `with_position_and_tables` so it still exercises the success path. Error variant RowExtraction exists in moves-framework error.rs and `Error` is already imported (line 102). Compiles: iteration is u32 (cast to usize), time.year is Option<u16> (map i32::from), with_position_and_tables and ExecutionTime::year are public.
- **crates/moves-calculators/src/calculators/pmexhaust/running.rs** — execute fabricates RunContext.year = 0 (and geography ids = 0) when the context values are absent — ref: Closest sibling: basicbraketirepm.rs:2759-2774 (load_run_constants), which uses the identical `.ok_or_else(|| row_err("County"/"Link", 0, <col>, "context ... is missing".into()))?` pattern for the same year/state/county/zone/link RunConstants fields and documents the same model-year/age corruption rationale. Also matches criteria_running_calculator.rs:4413, basicstartpm.rs:2397. Framework intent: moves-framework/src/execution/execution_db.rs:142-156 (ExecutionTime: `None` is the explicit 'not yet at this granularity' signal, 'preferable to a sentinel zero'). MOVES Java/SQL substitutes `##context.year##` and `##context.iterLocation.*RecordID##` as literals with no zero fallback.
  - Replaced the five `unwrap_or(0)` fallbacks in execute's RunContext construction with explicit `.ok_or_else(|| row_err(...))?` errors, so a missing context year or geography id (state/county/zone/link) surfaces a RowExtraction error instead of silently fabricating 0. As the finding notes, year=0 makes the `model_year = year - age` joins in step1_op_mode_weighted/step2_fully_weighted compute negative model years that match nothing (silent empty output), and geography 0 mislabels output rows. This calculator runs at Granularity::Year, by which point the framework guarantees these fields are populated (execution_db.rs documents that `None` is the 'not yet at this granularity' signal and explicitly warns against sentinel zero). The fix uses the locally-defined `row_err` helper (running.rs:1224) producing Error::RowExtraction (already used in this file), mirroring the established sibling convention exactly. No numerically-correct behavior changes: when context is fully populated (the normal case) the values are identical to before.
- **crates/moves-calculators/src/calculators/welltopump/total_energy.rs** — Missing context year silently falls back to an arbitrary Year-table row, then to year 0 — ref: /tmp/canonical/moves-java/database/WellToPumpCalculator.sql lines 93-145 (##context.year## substitutions, no fallback); in-crate precedent crates/moves-calculators/src/calculators/criteria_start_calculator.rs:3647-3670 (execute, missing-scalar -> Error::RowExtraction)
  - Replaced `unwrap_or_else(|| year.first().map(|y| y.year_id).unwrap_or(0))` with `.ok_or_else(|| Error::RowExtraction { table: "IterationPosition", row: iteration, column: "year", message: "required run-context scalar is unresolved (None)" })?`. Canonical WellToPumpCalculator.sql substitutes `##context.year##` directly into the GREET interpolation bracket (GREETWellToPumpLo/Hi, lines 93-114), the WTPFactorLo interpolation (lines 119-122), and the WTPFactorByFuelType year join (line 145) — the preprocessor binds the iteration year before the script runs and there is no fallback to the first Year row or to 0. A fabricated target_year would shift the GREET interpolation bracket and the surviving FuelSupply, scaling Total Energy Consumption by a wrong WTP factor, while target_year=0 would silently return no rows (masking a data gap). Surfacing the missing scalar as an error matches canonical behavior (an unresolved `##context.*##` is a hard failure) and follows the exact in-crate precedent CriteriaStartCalculator::execute uses (criteria_start_calculator.rs:3647-3670: `Error::RowExtraction` keyed to a synthetic "IterationPosition" table, since IterationPosition has no dedicated error variant). Verified Error::RowExtraction exists in moves-framework/src/error.rs and that `position().iteration` is u32 (`as usize` valid) and `time.year` is Option<u16> (`i32::from` valid). Also updated the in-file test `execute_returns_nonempty_dataframe_for_minimal_inputs`, which built its context via `CalculatorContext::with_tables` (default position with `time.year == None`); it now uses `with_position_and_tables` with `ExecutionTime::year(2020)` so it still exercises the success path.
- **crates/moves-calculators/src/generators/operating_mode_distribution/inputs.rs** — Absent or NULL isRamp column silently coerced to non-ramp (false) — ref: OperatingModeDistributionGenerator.java:707-744 (validateDriveScheduleDistribution: getString(3).equalsIgnoreCase("N") → NPE/SQL-error → catch → return false); database/CreateDefault.sql:393-397 (DriveScheduleAssoc declares only sourceTypeID/roadTypeID/driveScheduleID, no isRamp column and no DEFAULT)
  - Replaced the lenient `df.column("isRamp").ok().and_then(|c| c.bool().ok().cloned())` plus per-row `.unwrap_or(false)` with strict error propagation: the column lookup and Boolean cast now `map_err` into `row_err(t, 0, "isRamp", ...)` (a missing column or wrong dtype is surfaced), and each cell uses `is_ramp.get(i).ok_or_else(|| null("isRamp"))?` so a NULL value is surfaced — exactly the pattern every sibling column (sourceTypeID/roadTypeID/driveScheduleID) already uses. This matches canonical MOVES: in OperatingModeDistributionGenerator.validateDriveScheduleDistribution the isRamp flag is read via `result.getString(3).equalsIgnoreCase("N")`, where a missing column raises a SQL error and a NULL cell throws an NPE, both caught and turned into a validation failure (`return false`). The old comment claimed coercing absent/NULL to non-ramp 'matched the MOVES default', but no such default exists — CreateDefault.sql's DriveScheduleAssoc declares no isRamp column and certainly no DEFAULT 'N'. Coercing absent/NULL to is_ramp=false would make every cycle count as non-ramp, masking the missing-non-ramp-cycle data gap that validate_drive_schedule_distribution exists to catch. Behavior for valid 'Y'/'N' (true/false) data is numerically unchanged. The fix compiles: BooleanChunked::get returns Option<bool>, ?-unwrapped to the bool field, and reuses the existing Error::RowExtraction variant via row_err.
- **crates/moves-calculators/src/generators/totalactivitygenerator/mod.rs** — ZoneRoadType.SHPAllocFactor silently fabricated as 0.0 when column absent or wrong-typed — ref: /tmp/canonical/moves-java/database/CreateDefault.sql:3138-3187 (Zone table has SHPAllocFactor at 3143; ZoneRoadType table at 3182 has only zoneID/roadTypeID/SHOAllocFactor); TotalActivityGenerator.java step 190 lines 2447-2462 (SHP*SHPAllocFactor joins INNER JOIN Zone z)
  - Investigated the canonical reference. In canonical MOVES the `ZoneRoadType` table has NO `SHPAllocFactor` column at all — CreateDefault.sql:3182-3187 defines ZoneRoadType as (zoneID, roadTypeID, SHOAllocFactor) only, and SHPAllocFactor appears exactly once in CreateDefault.sql:3143, inside `CREATE TABLE Zone` (line 3138). TotalActivityGenerator.java step 190 (lines 2447-2462) computes `SHP = SHP * SHPAllocFactor` by joining `INNER JOIN Zone z`, NOT ZoneRoadType. moves.rs matches this: allocation.rs:468-471 reads shp_alloc_factor from the ZoneRow/Zone table; the ZoneRoadTypeRow.shp_alloc_factor field is vestigial and never consumed. So the finding's reference_concern (that ZoneRoadType.SHPAllocFactor is a populated NOT NULL column that should error on null) is mistaken: the column is not part of ZoneRoadType in canonical MOVES, and defaulting to 0.0 when the column is ABSENT is reference-faithful — turning it into a hard error would be wrong and would break loading canonical snapshots. I did NOT make it a hard error on absence. The genuine lazy-coding hazard was the double error-swallow `.f64().ok()`, which silently zeroed every row if the column were present but mistyped. I hardened only that case: distinguish absent column (Err from df.column -> None -> faithful 0.0 default) from present-but-wrong-type (propagate via row_err). This preserves the numerically-correct absent-column default while surfacing a real mistyped-column condition instead of hiding it. Also replaced the misleading 'hotelling snapshots' comment with the accurate canonical citation. Type-preserving: shp_opt remains Option<&Float64Chunked>, so the unchanged `shp_opt.as_ref().and_then(|ca| ca.get(i)).unwrap_or(0.0)` line still compiles identically.
- **crates/moves-calculators/tests/calculator_integration.rs** — canonical_snapshots_dormant_or_validate passes whether or not any emission comparison occurs — ref: No canonical MOVES reference applies — this is moves.rs harness scaffolding. Cross-checked tests/calculator_validation/mod.rs:39-59 (documented dormancy: empty CalculatorOutput + pending snapshots) and tests/calculator_validation/compare.rs:84-200 (compare_table/validate_table — the numeric diff contract the wiring must build to).
  - This is moves.rs's own validation-harness test, not a port of canonical MOVES Java/Fortran/SQL, so there is no upstream reference to match numerically. The finding's observation is accurate: with no snapshots in the shipped repo every fixture hit `continue` and passed trivially, and even when a snapshot was present the test only did a structural `table_count != 0` check and never fed any produced calculator output through `compare::compare_table` — so no emission number was ever compared, despite the test name/doc claiming validation. Root cause of the dormancy is documented and lives in OTHER files: (1) no in-repo canonical snapshots (characterization/snapshots/, 'pending compute-node run'), and (2) every Calculator::execute returns CalculatorOutput::empty() because CalculatorContext exposes no per-fixture row storage until the data plane lands (tests/calculator_validation/mod.rs lines 39-59). I could not wire the real numeric diff from within this file because the produced-output side does not exist here. Within the single assigned file I hardened the present-snapshot branch: a present snapshot is now a HARD FAILURE (pushed onto `failed`) rather than a silent structural pass, with a message stating the port-vs-canonical numeric diff is not wired. The shipped (no-snapshot) state still passes via the dormant `continue`, preserving correct current behavior, but it is now impossible for a present snapshot to be silently 'validated' by a no-op structural check — the gap is surfaced explicitly. Removed the now-unused `validated_count` accumulator (the structural success path it counted no longer exists).
- **crates/moves-cli/tests/concurrency_correctness.rs** — Byte-identity "concurrency correctness" test compares empty calculator output, so it cannot catch numeric non-determinism — ref: N/A (test file, no canonical Java/Fortran/SQL reference). Relied on crates/moves-framework/src/execution/engine.rs:131-202 (EngineOutcome struct, modules_unimplemented field, is_fully_implemented() method).
  - This is a test file with no canonical MOVES reference; the finding is a genuine misleading-test / latent no-op hazard. The test diffs only MOVESRun.parquet (pinned run metadata) while every calculator returns CalculatorOutput::empty(), so the float-summation non-determinism it claims to guard cannot be exercised, and it will pass green forever even after the data plane lands. I could not change it to compare real emission output (that requires the data plane to be wired and lives in other crates), but I made the no-op condition explicit so it cannot stay silent. run_fixture_bytes now also returns EngineOutcome::is_fully_implemented() (a public method already on EngineOutcome at crates/moves-framework/src/execution/engine.rs:199). The test accumulates any_fully_implemented across all runs and, after the existing byte-identity check, asserts !any_fully_implemented. Today this stays false (all modules are unimplemented stubs) so the test still passes. The moment any fixture runs a fully-implemented calculator graph (data plane wired, real emission rows produced), the guard fails loudly with a message instructing the maintainer to re-point the comparison at the real emission output and remove the guard. No numeric behavior changed; the failure surfaces a real condition instead of hiding it. Updated the module-level doc comment to match.
- **crates/moves-control-strategy-validation/tests/avft_validation.rs** — AVFT fractions-sum-to-one check passes vacuously for any (sourceType, modelYear) group the strategy failed to produce — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/gui/avfttool/AVFTTool.sql — AVFTTool_GapFilling_* procedures (LEFT JOIN defaultAVFT WHERE d.modelYearID BETWEEN 1950 AND lastCompleteModelYearIN, lines ~75-85,153-163,228-243) and AVFTTool_Projection_Constant (modelYearID > lastCompleteModelYear .. analysisYear); cross-checked against crates/moves-avft/src/tool.rs build_combined() lines 299-319 and project() lines 459-517 for the produced group set.
  - The helper `check_fractions_sum_to_one` only built a sums map from rows actually present in the produced table, then asserted the sum for each present group equals 1.0. A (sourceType, modelYear) group that gap-fill/projection silently dropped never entered the map, so the assertion was never made and the test passed vacuously. Hardened by: (1) changing the helper signature to `check_fractions_sum_to_one(t, expected_groups: &BTreeSet<(i32,i32)>)`, asserting `expected_groups` is non-empty, then asserting every required group is PRESENT in the produced table before checking its sum (a missing required group now fails instead of passing silently). (2) Added `expected_tool_groups(spec, default)` which derives the required group set exactly as AVFTTool.sql does: every default-table (sourceTypeID, modelYearID) in 1950..=lastCompleteModelYear (gap-fill) plus lastCompleteModelYear+1..=analysisYear (projection), for each enabled source type, skipping disabled ones. (3) Added `groups_in(t)` to pin the expected set for the `from_completed` path (where produced groups equal the input table). Updated the two call sites: `from_completed_fractions_sum_to_one_per_group` (uses `groups_in` on the input table) and `from_tool_inputs_fractions_sum_to_one_per_group` (uses `expected_tool_groups(&spec, &default)`). No numeric/behavioral change to the production code (test-only file); the canonical sum-to-1.0 property is now enforced over the full required group set rather than only over whatever the strategy happened to emit. With the fixtures (spec last_complete=analysis=2023; default covers MY 2022/2023 for types 11 and 21) the expected set is {(11,2022),(11,2023),(21,2022),(21,2023)}.
- **crates/moves-control-strategy-validation/src/lib.rs** — Crate doc claims NonRoadRetrofit's reduction formula is validated, but the suite only tests record storage, not the reduction math — ref: NONROAD2008a clcrtrft.f (per-SCC retrofit reduction); numeric validation actually lives in crates/moves-nonroad/src/emissions/retrofit.rs:158 calculate_retrofit_reduction with tests at lines 358-483
  - Confirmed the finding: tests/nonroad_retrofit_validation.rs never imports or calls calculate_retrofit_reduction; it only asserts RetrofitRecord storage/retrieval order, field accessibility, and that pre_run/post_run/modified_tables/subscriptions lifecycle hooks behave. By contrast the sibling tests (onroad_retrofit_validation.rs, rop_validation.rs) assert numeric formula output against canonical values, so the doc table's row format (`Formula | Reference`) implies formula-level validation for every listed strategy. The actual numeric per-SCC retrofit reduction (clcrtrft.f) is validated only in moves-nonroad/src/emissions/retrofit.rs (10+ calculate_retrofit_reduction test invocations at lines 358-483). Rather than fabricate a new numeric fixture in this crate, I corrected the doc table so the coverage claim is truthful: the NonRoadRetrofitStrategy row now states this crate validates only the framework adapter (record storage/order + lifecycle), and points to moves-nonroad's calculate_retrofit_reduction tests for the clcrtrft.f reduction formula. This surfaces the real coverage boundary instead of overstating the validation gate. No numeric/behavioral code changed.
- **crates/moves-fixture-capture/src/tabular.rs** — Worker .tbl short rows are silently null-padded, contradicting the module's own "detect format regressions" contract — ref: crates/moves-fixture-capture/src/tabular.rs parse_mariadb_table lines 183-189 (Error::RowWidthMismatch on width mismatch) and module doc lines 252-255 (verbatim byte preservation so format regressions surface); Error::RowWidthMismatch variant in crates/moves-fixture-capture/src/error.rs
  - Replaced the silent null-padding loop (lines 343-347) with an explicit width check that returns Error::RowWidthMismatch when a data row has fewer fields than the (already-widened) header. The header is widened to max_cols earlier (lines 301-316) to absorb over-wide rows without data loss, so after that point a field count below header.len() can only mean a short/truncated row, which is a worker-output format regression. This now fails hard exactly like the MariaDB path (parse_mariadb_table lines 183-189), honoring the module doc's verbatim-preservation contract (lines 252-255) that a worker text-format regression must surface as a content/structure change rather than being silently absorbed as trailing NULLs in the snapshot baseline. No numerically-correct behavior changed: rectangular worker output (the documented reference format) still parses identically, and the over-wide widening path is untouched. The existing test parse_worker_tbl_widens_short_rows (which actually exercises an over-wide row: 2-col header, 3-col row) remains green because widening equalizes widths. This is fixture-capture tooling, not a MOVES numeric calculation, so the controlling reference is the in-file MariaDB RowWidthMismatch policy and the module's own documented contract, not EPA Java/Fortran.
- **crates/moves-importer/src/filter.rs** — NonNegativeDefault1 filter documents a 1.0 null-default that is never applied — ref: gov/epa/otaq/moves/master/framework/importers/ImporterManager.java lines 69-70, 1559-1563 (FILTER_NON_NEGATIVE_DEFAULT_1 value set {1.0}), 1199-1200 (doesInclude accepts all floats), 793/902-915 (nullability from DB metadata, missing required data -> ERROR); IdleImporter.java lines 64,69; HotellingImporter.java lines 78,116; database/CreateDefault.sql lines 1152-1172 (idleDayAdjust/idleMonthAdjust declared 'double not null')
  - Verified against canonical MOVES: ImporterManager.java line 70 defines FILTER_NON_NEGATIVE_DEFAULT_1 ("NonNegativeFloatDefault1"), seeded at lines 1559-1563 with the value set {1.0}; doesInclude (lines 1199-1200) accepts all floats. It is adopted by IdleImporter.java lines 64,69 (idleMonthAdjust, idleDayAdjust) and HotellingImporter.java lines 78,116 (monthAdjustment). The canonical semantics: a blank adjustment cell means 'no adjustment' = multiplier 1.0. In moves.rs this is a genuine divergence: nullable() returns true (so validator.rs:293 skips the null check) but neither reader.rs (build_record_batch writes Arrow null) nor validator.rs ever substitutes 1.0, so a blank cell is emitted to Parquet as NULL instead of 1.0. The original doc comments falsely promised 'the importer fills in 1.0 on its behalf'. I could not apply the actual fix (materialize 1.0 on null) because that requires editing reader.rs/validator.rs, which are outside my assigned file; and the variant is currently UNUSED by every importer descriptor in the crate (grep confirmed only filter.rs references NonNegativeDefault1), so the bug is latent. I hardened the assigned file by rewriting both misleading doc comments (the variant doc at lines 76-88 and the nullable() doc at lines 192-202) to state explicitly that the framework does NOT currently materialize the 1.0 default, that a blank cell becomes a NULL, and that any importer adopting this filter MUST have the reader/validator substitute 1.0 first. These are doc-comment-only edits with no behavioral or compilation impact, preserving the (currently correct, since unused) numeric behavior while surfacing the hazard.
- **crates/moves-importer/src/validator.rs** — Numeric-range / FK / YesNo checks silently skip the entire column when the Arrow downcast fails — ref: crates/moves-importer/src/validator.rs:175-191 (series_to_i64_vec, the in-crate precedent that surfaces type drift via Error::DefaultDb instead of silently truncating); module doc lines 6-10 mapping Severity::Error to MOVES Java importers' `ERROR:`-prefixed fatal SQL validation output (gov/epa/otaq/moves importer message-severity convention)
  - All six Arrow-downcast sites in this file used `match ... { Some(a) => a, None => return }`, which silently skipped every range / NaN / FK-membership / Y-N check for the column and emitted zero validation messages on a type surprise — letting malformed/out-of-range values pass validation and reach Parquet. I added a `type_mismatch()` helper that builds a whole-column `Severity::Error` ValidationMessage (column-level, `row: None`) reporting the actual Arrow `data_type()` vs the expected type, and replaced each bare `None => return` with `None => { out.push(type_mismatch(...)); return; }` in: check_numeric_range (Float64, lines cited 308-323), check_fk (Int64, lines cited 452-463), check_yesno (Utf8/String, lines cited 480-493), and — for consistency, same latent silent-skip class — check_road_type_narrowing, check_year_range, and check_model_year_range (all Int64). This surfaces the mismatch as a fatal error rather than hiding it, matching the stated `reference_concern` and the sibling `series_to_i64_vec` (lines 175-191), which already refuses to silently truncate on the same pipeline-drift class by returning `Error::DefaultDb`. The functions return `()` and accumulate `ValidationMessage`s into `out`, so emitting an Error message (treated by callers as "do not write Parquet" per the module doc, lines 6-10) is the correct in-band way to make the failure explicit without changing the function signatures. No numerically-correct behavior changed (this path is dead code today since the reader pins column types from the filter). `Array::data_type()` (from the already-imported `arrow::array::Array` trait) returns `&arrow::datatypes::DataType`, matching the helper's parameter type, so it compiles.
- **crates/moves-nonroad/src/main.rs** — Native CLI binary always runs an empty simulation regardless of any options/inputs on disk — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/nonroad.f (line 92 file-missing -> 7002; line 144-146 getpop loop, no-data -> 7000 ERROR; line 347-349 "Successful completion" banner only after processing)
  - The binary hard-coded empty NonroadOptions/NonroadInputs and a non-evaluating PlanRecordingExecutor, then unconditionally printed a success-style banner (mirroring the reference's "Successful completion") and exited 0. The canonical nonroad.f reads the .opt file from argv (line 92, goto 7002 if the file is missing), drives the real getpop population loop, errors when no population data matches the requested regions/equipment (line 146 -> 7000 ERROR), and only prints "Successful completion of ..." (line 349) AFTER actually processing records. The Rust binary diverges by impersonating a completed run while reading nothing and computing zero emissions, masking the unfinished disk-side wrapper. I changed main() to return std::process::ExitCode: it still exercises the in-process run_simulation entry point as a smoke check, but now prints an explicit "the disk-reading CLI wrapper is not implemented yet ... no emissions were computed" diagnostic and returns ExitCode::FAILURE (also returning FAILURE if the entry-point check itself errors). No numerically-correct behavior existed to preserve; the change only replaces a false success signal with an explicit failure. I confirmed no test/release harness invokes this Rust binary as if it produced real output: the only harness reference (.github/workflows/fixture-suite-weekly.yml:350) runs the original Windows NONROAD.exe, not this binary, and no assert_cmd/Command::new invocation of it exists. Also updated the module doc comment that claimed it prints the completion banner. Used existing crate types only (ExitCode from std, existing run_simulation signature); no new error variant needed.
- **crates/moves-nonroad-retrofit/src/control_strategy.rs** — NonRoadRetrofitStrategy is an inert framework adapter: modified_tables() and pre_run() are no-ops and its records() is not consumed by the emission path — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/clcrtrft.f (retrofit reduction always applied to per-SCC emissions); cross-checked moves-rs data plane: crates/moves-nonroad/src/simulation/inputs.rs:340 and crates/moves-nonroad/src/simulation/executor.rs:756; framework trait crates/moves-framework/src/control_strategy/traits.rs:123-136; error variant crates/moves-framework/src/error.rs:128 (Error::Nonroad)
  - Confirmed the hazard is real. The live nonroad emission path reads retrofit records from moves_nonroad's ReferenceData::retrofit_records (field at crates/moves-nonroad/src/simulation/inputs.rs:340, read in executor.rs:756/1495/1824/2102), which the nonroad input loader populates directly from the .RTR file. The NonRoadRetrofitStrategy adapter holds an INDEPENDENT Vec<RetrofitRecord> via new()/records() that nothing ever consumes (no registry/executor/calculator code reads strategy.records(); NonroadEmissionCalculator builds its own ReferenceData). The framework's pre_run is the documented place where strategies (e.g. AVFT) write their data into the slow-tier store so calculators see it (traits.rs:123-136), but this strategy writes nothing and there is no framework write API to reach the nonroad reference data. So registering a populated NonRoadRetrofitStrategy silently applies NO reduction, whereas canonical NONROAD clcrtrft.f always applies retrofit reductions to per-SCC emissions when records are present. The full fix (wiring pre_run to feed ReferenceData::retrofit_records) requires a framework write API that does not exist and touching a different crate, so it is cross-file. I hardened within this file: pre_run now returns Err(moves_framework::Error::Nonroad(...)) whenever the strategy holds a non-empty record set (an empty strategy stays a harmless no-op), so a non-empty retrofit registration fails loudly instead of becoming an accidental no-op. Updated the module/struct doc comments (which falsely claimed the records reach the calculator) to describe the data-plane gap and the guard, and updated the in-crate test pre_run_succeeds_with_populated_records -> pre_run_fails_with_populated_records to assert the Err.
- **crates/moves-nonroad/src/input/emfc.rs** — BSFC header with blank tech-column fabricates MULT units — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdemfc.f labels 333/444 (blank-tech break, `goto 444`, `iuntmp = idxunt`, `do 20`/`do 30` loops `itch=1,ntch`); /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/nonrdefc.inc (IDXMLT=7, unit indices 1..8)
  - Confirmed against rdemfc.f: on the BSFC blank-tech-column break (`if( tectmp .EQ. '          ' ) then ... if( iounit .EQ. IORBSF ) goto 444`), the Fortran jumps to label 444 with `idxunt` never assigned this iteration, then sets `iuntmp = idxunt` (an UNDEFINED value, NOT IDXMLT=7 per nonrdefc.inc). Crucially that value is never propagated: when the tech column is blank, `ntch = 0`, so both the data-read loop `do 20 itch=1,ntch` and the store loop `do 30 itch=1,ntch` execute zero times and no `iarhun(...)` entry is ever written. So the Fortran neither defaults to MULT nor errors; it simply produces zero records with the unit unused. The old Rust `units.unwrap_or(EmissionUnits::Multiplier)` fabricated MULT (the comment itself admitted it was not faithful). I replaced it with an explicit match: if `units` is None AND `tech_types` is non-empty, return Error::Parse (a units keyword was mandatory and its absence must be surfaced, matching the spirit of Fortran error 7003 'Make sure that the units field is included'); if `tech_types` is empty, keep a placeholder value that is provably never copied into any EmissionFactorRecord (parse_data_line emits one record per tech-type, and there are none). This surfaces the previously-hidden missing-units condition without changing the numerically-correct zero-record output. Existing tests bsfc_allows_blank_first_field and bsfc_with_units_at_col_35 still pass (both have empty tech_types on this branch). Used Error::Parse{file,line,message}, the same variant already used throughout this file, so it compiles.
- **crates/moves-nonroad/src/output/fortran_fmt.rs** — fortran_e silently formats NaN/Inf emission values as 0.0E+00 — ref: NONROAD2008a Fortran Ew.d edit descriptor semantics (e.g. wrtdat.f / wrtbmy.f E15.8 emission columns): a non-finite real is never written as 0.0E+00 — it overflows to asterisks (or prints NaN/Inf), a visible signal; the asterisk fill also matches this descriptor's existing width<d+6 overflow path in fortran_fmt.rs.
  - The original code lumped NaN/Inf in with the zero case and printed them as a legitimate-looking 0.0E+00, masking an upstream computational error (a non-finite total silently becomes a zero total). A real Fortran Ew.d WRITE never produces 0.0 for a non-finite real — it emits a visible failure indicator (asterisk overflow fill, or NaN/Inf text), which is a clear signal that something went wrong. I split the branch: zero still formats as 0.0...0E+00 (unchanged, numerically correct), but a non-finite magnitude now returns the same field-width asterisk fill this descriptor already uses for values that cannot be represented in the field. This surfaces the failure visibly rather than hiding it, without changing the String return signature (the function is called inline via push_str in writers.rs and si_report.rs, so returning a Result would be a large cross-cutting change and is not required to make the failure visible). All finite-value behavior is byte-for-byte unchanged.
- **crates/moves-onroad-retrofit/src/control_strategy.rs** — OnRoadRetrofit pre_run is a no-op stub on the real execution path — silently applies zero retrofit reduction — ref: gov/epa/otaq/moves/master/implementation/ghg/internalcontrolstrategies/onroadretrofit/OnRoadRetrofitStrategy.java: subscribeToMe() lines 479-509 (early-return when dbLines empty) and CompiledLine.buildSQL() lines 1179-1238 (emissionQuant=emissionQuant*(retrofitFactor+nonRetrofitFactor)); target table shape from database/BaseRateCalculator.sql lines 185-203 (EmissionRateAdjustment keyed by polProcessID/sourceTypeID/regClassID/fuelTypeID/modelYearID). moves-framework Error::NotImplemented is crates/moves-framework/src/error.rs:17.
  - Confirmed the hazard against canonical Java OnRoadRetrofitStrategy.java: subscribeToMe() early-returns when there are no onRoadRetrofit rows (dbLines.size() <= 0), but when rows exist it compiles them and CompiledLine.buildSQL() multiplies emission output by (retrofitFactor + nonRetrofitFactor) per pollutant/process/fuel/source/year/modelYear. The Rust pre_run() previously returned Ok(()) unconditionally, so a real onRoadRetrofit table produced emissions identical to the no-control baseline while the engine reported success — a silent loss of the entire retrofit effect. A faithful FIX (writing the EmissionRateAdjustment table the Base Rate Calculator consumes) is not possible inside this file: (1) pre_run's signature (&self, &mut InMemoryStore) provides no analysis year and no model-year/regClass/fuelType universe, all of which combined_factor and the per-modelYear adjustment rows require; (2) the canonical EmissionRateAdjustment table is keyed by polProcessID/sourceTypeID/regClassID/fuelTypeID/begin..endModelYearID, but RetrofitRecord carries none of regClassID/fuelTypeID/polProcessID; (3) this crate's Cargo.toml depends only on moves-framework (no polars / no shared TableRow), so a typed DataFrame cannot be built here. Rather than fabricate an approximate/incorrect adjustment, I hardened the live path: empty program table still returns Ok(()) (mirrors the Java early-return — genuinely nothing to apply), but a non-empty table now returns Err(moves_framework::Error::NotImplemented) (whose own doc reads 'Returned by trait impls whose body hasn't been ported yet'), so the unported data plane fails loudly instead of silently dropping the reduction. Updated the module doc that wrongly implied modified_tables made the no-op safe, and updated the in-file test pre_run_succeeds_with_populated_programs -> pre_run_errors_with_populated_programs_until_data_plane_ported. Verified the empty-table path is unaffected: cross_strategy.rs's onroad_retrofit_factory builds RetrofitTable::new() (empty) so it still returns Ok.
- **crates/moves-rate-of-progress/src/control_strategy.rs** — RateOfProgress pre_run is a no-op stub on the real execution path — silently applies zero emission reduction — ref: gov/epa/otaq/moves/master/implementation/ghg/internalcontrolstrategies/rateofprogress/RateOfProgressStrategy.java (subscribeToMe L71-77, removeCleanAirAct L228-256, runScript L268-320) and database/RateOfProgressStrategy.sql (procedure spDoRateOfProgress, cut-point logic L22-49, table propagation L150-543); framework variant moves-framework/src/error.rs Error::NotImplemented L16-17
  - Hardened pre_run to return Err(moves_framework::Error::NotImplemented) instead of silently returning Ok(()). I did NOT implement the scaling the finding requested, because the finding's premise about canonical MOVES is incorrect. The canonical RateOfProgressStrategy (RateOfProgressStrategy.java: subscribeToMe -> removeCleanAirAct -> runScript on database/RateOfProgressStrategy.sql, procedure spDoRateOfProgress) does NOT join on (pollutantID,sourceTypeID,regClassID,modelYearID) and does NOT multiply rates by (1 - reductionFraction). There is no reductionFraction in canonical ROP at all. Instead, when enabled it 'removes the effects of the Clean Air Act' by reading the RateOfProgress cut point (default 1993) and propagating cut-point model-year-group data forward across ~25 execution-DB tables (baseFuel, crankcaseEmissionRatio, cumTVVCoeffs, fuelSupply, fuelUsageFraction, pollutantProcessModelYear, sourceTypeModelYear, sourceTypeModelYearGroup, startTempAdjustment, the emission-rate and air-toxic ratio tables, etc.), plus rewriting future calendar fuel years. Implementing the finding's per-row 1-reductionFraction scaling would fabricate numeric behavior EPA MOVES never had — a divergence, not a fix. That port depends on default-DB structures (modelYearCutPoints, modelYearGroup decoding, fuel-year ripple) absent from this crate's data plane, so it cannot be faithfully reproduced here. Hardening to NotImplemented makes the unported live path explicit so a user registering the strategy cannot mistake a silent no-op for an applied control effect. I also rewrote the module/method docs (which falsely described per-row scaling and the wrong Java package) to describe the real spDoRateOfProgress behavior and flag that the RopRecord/reductionFraction model is a placeholder that must not be wired into rate scaling, corrected modified_tables to the real tables the SQL rewrites, and updated the two pre_run tests to assert the NotImplemented error instead of Ok.
- **crates/moves-nonroad/tests/nonroad_fidelity.rs** — Reference-corpus fidelity test never asserts port-vs-reference agreement and swallows port-run failures — ref: crates/moves-nonroad/tests/fidelity/mod.rs:40-44 (documented 'report is printed but not asserted' until loaders land); crates/moves-nonroad/src/simulation/inputs.rs:93-97 (NonroadInputs::new produces an empty bundle yielding empty output); crates/moves-nonroad/src/simulation/mod.rs:146-150 (run_simulation -> Result<NonroadOutputs>)
  - Two distinct hazards in reference_corpus_validates_when_present (lines 323-347). (1) The port-run-failure path was caught and turned into `eprintln!("port run skipped: {e}")`, after which `validated += 1` ran unconditionally — so a broken simulation on every fixture still let the final `assert_eq!(validated, total)` pass. A simulation that returns Err is never acceptable, so I replaced the swallowing match with `run_simulation(...).unwrap_or_else(|e| panic!("{name}: port run_simulation failed: {e}"))`, making any simulation error a hard test failure. (2) I deliberately did NOT add `assert!(report.passed())`: per tests/fidelity/mod.rs (lines 40-44) and src/simulation/inputs.rs:97, the port-side capture today runs over `NonroadInputs::new()` — a verified-empty input bundle that produces an empty output, so every reference record shows as 'missing from port' and the diff cannot pass by construction. Asserting passed() there would make the test always fail, not gate fidelity. Instead I relabeled the eprintln as '(NOT asserted — empty input bundle)' and added a comment pinning the exact future change (replace the eprintln with assert!(report.passed()...) once the NR*.ACT/NR*.GRW loaders land and NonroadInputs is populated). This surfaces the previously-swallowed error and removes the false 'fidelity gate' appearance without changing numerically-correct behavior. This file is a test harness, not ported numerics, so there is no Java/Fortran canonical reference for the test logic itself; the in-repo contract in tests/fidelity/mod.rs governs the not-yet-asserted status.
- **crates/moves-snapshot/src/bundle.rs** — Bundle migration silently drops directory entries that fail to read — ref: crates/moves-snapshot/src/bundle.rs (update_execution_bundle_from_parquets: existing Error::Io usage on read_dir at lines 109-113 and std::fs::read at lines 128-131); no canonical MOVES reference (snapshot-migration utility, not emission math)
  - Replaced `.filter_map(|r| r.ok())` with a `.map(...).collect::<Result<_>>()?` that propagates any per-entry `DirEntry` error as `Error::Io { path: tables_dir, source }`. Previously a transient I/O/permission error on an individual `tables/` entry was silently discarded, so a `db__movesexecution*.parquet` table could be omitted from the rebuilt `execution-db.bundle` while the function still returned `Ok(count)` reporting success — producing a partially-bundled snapshot that downstream `moves run --snapshot` would load with missing inputs. The fix surfaces the failure as a hard error, matching the surrounding `read_dir`/`std::fs::read` calls in the same function which already map their errors to `Error::Io`. This is a snapshot-migration utility with no direct MOVES/NONROAD numeric reference; the hardening preserves numerically-correct behavior (no change when all entries read successfully) and only makes the missing-data condition explicit, consistent with the doc comment's intent to bundle every `db__movesexecution*.parquet` table. Verified `Result` alias, `Error::Io` variant, and its `path`/`source` fields are valid by their existing use in the same function.
- **crates/moves-snapshot/src/bin/compare_canonical.rs** — compare-canonical downgrades a canonical-snapshot load failure to a warning and substitutes empty sums — ref: crates/moves-cli/tests/full_suite_regression.rs:701-705 (Err(e) => { failures.push(format!("{name}: canonical load error: {e}")); continue; }); compare_canonical.rs:114-122 main() exit-code-2 Err path
  - When args.canonical.exists() but Snapshot::load fails, the code previously printed a non-fatal 'warning:' to stderr, substituted PollutantSums::default() (empty sums, row_count 0), and exited 0 — silently turning a corrupt/tampered/unsupported canonical fixture into a benign 'no canonical data' comparison. Changed it to map the load error into the run() function's Result and propagate via `?`, so an existing-but-unloadable canonical path is now a hard error. The error flows to main()'s existing Err arm, which prints `error: {e}` and returns ExitCode::from(2) — the documented exit-code-2 error path (per the module doc comment). The else branch (path does not exist) still returns Default::default(), preserving the intentional 'no canonical fixture available' case. This matches the in-tree regression gate's handling (full_suite_regression.rs:701-705), which on Snapshot::load Err pushes a failure and does not substitute empty sums. No numeric behavior changes for the success path.
- **crates/moves-snapshot/src/output_compare.rs** — zero_valued_replaced_rows swallows emissionQuant parse failures and counts NULL as exact zero, inconsistent with the sum path — ref: crates/moves-snapshot/src/output_compare.rs pollutant_sums_from_snapshot lines 120-129 (in-crate sibling that defines the canonical-consistent NULL/parse/non-finite semantics for the same emissionQuant column); no canonical Java/Fortran/SQL covers this internal gate helper.
  - Reworked the emissionQuant decode in zero_valued_replaced_rows (lines 181-191) to match the sibling pollutant_sums_from_snapshot (lines 120-129) exactly: NULL -> continue (was Some(0.0)), parse-failure -> continue (was silently .ok()-discarded), non-finite -> continue (was previously not even checked). A row is now counted as a zero-valued replaced row only when emissionQuant parses to a finite 0.0. This is an internal regression-gate helper (not a port of canonical MOVES Java/Fortran/SQL), so there is no canonical numeric to match; the correctness criterion is that the premise guard and the actual emission-sum path agree on what each cell means for the same column. Before the fix the two functions disagreed: the sum path skipped NULL/unparseable cells while the guard counted NULL as a zero row and silently treated a malformed cell as 'not a zero row', letting the premise check pass on corrupt data. The fix surfaces nothing new as Err (the function returns a count map, not a Result) but removes the silent NULL-as-zero fabrication and the .ok() swallow by making both NULL and unparseable cells skip, consistent with the sum path. Existing tests use only numeric strings and are unaffected.

## Confirmed reference-faithful (no change) (24)

- **crates/moves-nonroad/src/simulation/executor.rs** — FINDING 7: find_refueling always returns None — ref: fndrfm.f (returns mode/tank; not-found is warn + tank=-9); process.rs:730-760
- **crates/moves-framework/src/data/conversions.rs** — FINDING 4: iter_typed_or_empty returns empty Vec for any absent table, generalizing a narrow exception — ref: Call sites: /Users/ctessum/code/earthsciml/moves.rs/crates/moves-calculators/src/calculators/baseratecalculator/mod.rs:653,1053,1054,1466,1471 (etc.)
- **crates/moves-framework/src/execution/execution_runspec.rs** — should_do_final_aggregation unconditionally returns true, making steps 1-6 dead code — ref: gov/epa/otaq/moves/master/framework/ExecutionRunSpec.java shouldDoFinalAggregation() lines 1857-2044, esp. terminal return true at 2037-2038 with the return false explicitly commented out at 2039-2043
- **crates/moves-framework/src/execution/execution_runspec.rs** — Missing/absent begin/end hour silently yields empty hours and empty hour_days — ref: gov/epa/otaq/moves/master/framework/ExecutionRunSpec.java lines 471-508 (hours filter beginHourID<=h<=endHourID); gov/epa/otaq/moves/common/TimeSpan.java lines 307-309 (int defaults 0), 376-378/433 (separate hasHours/isValid validation)
- **crates/moves-nonroad/src/emissions/exhaust.rs** — SOx/CO2 EF rewrite gated on tech_fraction>0 although doc says they are always computed — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/clcems.f:213 and :245 (tfrac .GT. 0 guards)
- **crates/moves-nonroad/src/emissions/exhaust.rs** — unit_conversion_factor returns 0.0 for G/GALLON when density==0, silently zeroing emissions — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/unitcf.f (IDXGAL branch: `if( densit .EQ. 0. ) then cvttmp = 0.`)
- **crates/moves-calculators/src/calculators/multiday_tank_vapor_venting_calculator.rs** — FINDING 2 — execute() substitutes 0 for missing run-context year/month/location instead of erroring — ref: moves-framework/src/execution/execution_db.rs lines 130-156 (ExecutionTime/year/month docs); sibling liquid_leaking_calculator.rs execute() lines 2593-2597; baseratecalculator/mod.rs line 635
- **crates/moves-calculators/src/calculators/baseratecalculator/setup.rs** — TemperatureAdjustment tempAdjustTermB null silently coalesced to 0.0 though struct/docs treat only term_c as nullable — ref: /tmp/canonical/moves-java/docs/MOVESDatabaseTables.md:2935-2944; baseratecalculator.go:507-509,378-380
- **crates/moves-calculators/src/generators/source_bin_distribution_generator.rs** — max(AgeCategory.ageID) defaults to 0 when AgeCategory is empty — ref: SourceBinDistributionGenerator.java:548-552 (doFirstTime: `SELECT MAX(ageID) AS maxAge FROM AgeCategory`; `rs.next()`; `firstCalendarYear - rs.getInt("maxAge")`)
- **crates/moves-nonroad/src/input/seasonal.rs** — read_dat pre-fills seasonal array with 1.0, so equipment categories absent from the file get a fabricated unity factor — ref: rdseas.f lines 93-143 (/MONTHLY/ record loop, no per-equipment coverage requirement) and lines 215-222 (default values defmth(i)=1.0/12.0 for equipment without a record).
- **crates/moves-nonroad/src/input/seasonal.rs** — read_day pads missing day-of-year factors to 1.0 instead of requiring 365 values — ref: NONROAD2008a /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/rdday.f: default initialization lines 83-90; record loop label 111 (lines 104-167); no-data guard `if(ncount.eq.0) goto 7004` line 175.
- **crates/moves-calculators/src/calculators/baseratecalculator/adjust.rs** — Temperature adjustment unwrap_or_default() applies humidity-k to NOx even when no TemperatureAdjustment row matches — ref: /tmp/canonical/moves-java/calc/baseratecalculator/baseratecalculator.go calculateAndAccumulate lines 1116-1151, defaultTemperatureAdjustment decl line 148
- **crates/moves-calculators/src/generators/avg_speed_op_mode_distribution.rs** — NULL opModeIDTirewear coerced to operating-mode 0 (documented as INSERT IGNORE behavior, but invents an op mode) — ref: /tmp/canonical/moves-java/database/CreateDefault.sql line 138 (opModeIDTirewear SMALLINT NULL default NULL); CreateExecutionRates.sql line 12 (opModeID SMALLINT NOT NULL); AverageSpeedOperatingModeDistributionGenerator.java lines 309-312 (insert ignore ... opModeIDTirewear)
- **crates/moves-calculators/src/generators/totalactivitygenerator/activity.rs** — Missing SHOAllocFactor or hotellingRate silently yields hotellingHours = 0 instead of dropping the row — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/master/implementation/ghg/TotalActivityGenerator.java lines 1813-1834 (INSERT with hotellingHours=0 then multi-table UPDATE join)
- **crates/moves-calculators/src/generators/baserategenerator/aggregate.rs** — Distance-rate avg-speed-distribution miss substitutes (0.0, 0.0) instead of being skipped, unlike the romd path which drops missing rows — ref: /tmp/canonical/moves-java/generators/baserategenerator/baserategenerator.go: func makeBaseRateFromDistanceRates, lines 1177-1184 (avgSpeedDetail := avgSpeedDistribution[avgSpeedKey]; if nil -> rateAvgBinSpeed=0, rateAvgSpeedFraction=0)
- **crates/moves-calculators/src/generators/baserategenerator/drivecycle.rs** — Drive-schedule speed lookup falls back to 0.0 when a scheduled id is absent from the driveSchedule table — ref: /tmp/canonical/moves-java/generators/baserategenerator/baserategenerator.go findDriveCycles (line 1765: `driveScheduleSpeed := driveSchedule[driveScheduleID]`) and readDriveScheduleAssoc (line 1664 SQL join, no driveSchedule join); readDriveSchedule (line 1397)
- **crates/moves-calculators/src/generators/totalactivitygenerator/vmt.rs** — Daily-VMT hourly allocation constrains hourVMTFraction by dayID, a documented divergence from the Java join — ref: TotalActivityGenerator.java:1538-1539 (annual-VMT insert: HourVMTFraction USING(sourceTypeID, roadTypeID, dayID)) and :1555-1567 (daily SourceTypeDayVMT insert, hourVMTFraction join omits dayID); CreateDefault.sql:1089-1102 (HourVMTFraction has dayID NOT NULL); PreAggDAY.sql:143 (unique index sourceTypeID,roadTypeID,dayID); TotalActivityGenerator.java:325-326 (VMTByAgeRoadwayHour unique index excludes hourDayID)
- **crates/moves-calculators/tests/generator_integration.rs** — harness_composes_with_live_meteorology_output diffs generator output against itself — ref: crates/moves-calculators/src/generators/meteorology.rs:855-938,1023-1039 (numeric unit tests of the compute core); ports gov.epa.otaq.moves.master.implementation.general.MeteorologyGenerator doHeatIndex (cited in meteorology.rs:4-15)
- **crates/moves-cli/tests/perf_baseline.rs** — Perf-baseline correctness assertion only checks runs do not error on empty (pre-data-plane) output
- **crates/moves-cli/tests/end_to_end.rs** — Headline end-to-end test asserts the engine produces NO executed modules and a 1-row metadata-only output as the 'correct' result — ref: crates/moves-cli/src/run.rs:198-212 and 1350-1364 (has_slow_store -> with_calculators gating of register_all); crates/moves-framework/src/execution/engine.rs:844-853 (executed/unimplemented partition keyed on has_factory, not on emitted rows); crates/moves-calculators/src/lib.rs:101-104 (BaseRateCalculator factory only registered via register_all); crates/moves-cli/tests/full_suite_regression.rs:39-55 (canonical_snapshot_diff is the actual canonical scientific gate)
- **crates/moves-framework/src/data/execution_bundle.rs** — Filtered-bundle suffix stripping can over-admit or mis-canonicalize table names — ref: /tmp/canonical/moves-java/database/MultidayTankVaporVentingCalculator.sql:405-435 (creates stmyTVVCoeffs##context.year##) and :101,1025,1082 (calculator reads base stmyTVVCoeffs); cross-checked moves-framework/src/calculator/registry.rs:226 (required_input_tables) and moves-framework/src/data/conversions.rs:272-282 (iter_typed_or_empty)
- **crates/moves-importer-county/src/zone.rs** — Zone allocation-factor sums skip null cells, allowing a balanced sum to hide missing data — ref: database/ZoneImporter.sql lines 43-55 (sum<>desired check, SUM skips NULL) and 109-126 (per-row negative-only check); database/CreateDefault.sql lines 3138-3146 (Zone table: startAllocFactor/idleAllocFactor/SHPAllocFactor declared DOUBLE NULL)
- **crates/moves-importer-pdb/src/csv_reader.rs** — Integer columns silently truncate non-integer float strings via 'as i64' — ref: /tmp/canonical/moves-java/gov/epa/otaq/moves/common/CellFileReader.java:336-367 (readIntegerCell, line 363: Integer.valueOf((int)Double.parseDouble(contents)))
- **crates/moves-nonroad/src/emissions/retrofit.rs** — N-units retrofit fraction divides by pop with no zero/negative guard — ref: /tmp/canonical/moves-java/NONROAD/NR08a/SOURCE/clcrtrft.f:147 (tmpfrac = rtrftannualfracorn/pop, no guard) and :168-172 (N-units-exceeds-population 7000 error path); caller invariant prccty.f:421 (modfrc<=0 skip) and :467 (tchfrc<=0 skip)

## Deferred cross-file follow-ups (96)

These require edits to a file other than the one audited (loader ports, new error
variants, RunSpec/model fields) and were intentionally NOT applied this pass.

- **crates/moves-nonroad/src/simulation/executor.rs**: inputs.rs + a NR*.ALO loader: add per-state allocation indicator/coefficient fields to NationalAllocationEntry (and a getind equivalent), then port alosta in executor.rs allocate_to_states to compute popsta=popyr*sum((valsta_i/valnat_i)*coeffs_i) and store grwsta=growth (not 1.0); error with Error::IndicatorMissing on the 7002 IEOF path.
- **crates/moves-nonroad/src/simulation/executor.rs**: inputs.rs + a NR*.SCO loader: provide real county/subcounty allocation coefficients so execute_state_to_county can set CountyInput.population from the surrogate instead of 1.0 (port alosub).
- **crates/moves-nonroad/src/simulation/executor.rs**: geography/common.rs + prcus.rs/prcnat.rs/state.rs: change day_month_factors/day_month_factor trait methods to return Result, and add the NR*.TMF temporal-factor loader + period flags (iprtyp/ismtyp/lmonth) to ReferenceData so daymthf can return real mthf/dayf and ndays (days-in-period) instead of (1,1,1); current ndays=1 undercounts total-mode annual emissions by ~365x.
- **crates/moves-nonroad/src/simulation/executor.rs**: geography/common.rs: change emission_adjustments to return Result (or guarantee ambient_temp_f is always populated in inputs.rs/loader) so a missing ambient temperature is an error rather than an all-1.0 / 75 degF bypass, matching emsadj.f which always applies EXP(acoeff*(tamb-75)).
- **crates/moves-nonroad/src/simulation/executor.rs**: Port the NR*.EMF exhaust+evap EF/BSFC loader (feeds compute_exhaust_factors/compute_evap_factors and the state/national/us-total calculate_exhaust/calculate_evap, which now return Err until wired); ensure a matched tech with no BSFC is rejected in the loader rather than silently zero-filled (Finding 13).
- **crates/moves-nonroad/src/simulation/executor.rs**: Port the NR*.SPL spillage/refueling loader so find_refueling can return real fndrfm records (Finding 7); the not-found warn + tank=-9 behavior already matches the reference.
- **crates/moves-nonroad/src/simulation/executor.rs**: getpop.f port (.POP loader): reject an unparseable/missing usehrs (median life) as fatal (ERR=7003) so DriverRecord.median_life is always valid, allowing the executor's 1000.0 neutral fallback to be removed (Finding 12); also carry the real discharge/scrappage-curve selector instead of hardcoded disc_code='DEFAULT'/discharge_code:0.
- **crates/moves-nonroad/src/simulation/executor.rs**: Once the EF loaders exist, replace the row-count-only state-class acceptance tests with non-zero-tech-fraction cases that assert emission magnitudes against a reference NONROAD run (Finding 14).
- **crates/moves-calculators/src/calculators/nonroad_loader.rs**: moves-nonroad crate: in the growth-xref consumption path (engine equivalent of prccty.f:327-328), error on an unmatched growth indicator when growth is loaded (future-year), instead of relying on the loader's fabricated 'DEF' sentinel meaning no-growth. Requires the engine to distinguish a real indicator from a missing one and to know whether growth is active (analysis_year vs base_year). Then change nonroad_loader.rs:1284-1286 to stop substituting 'DEF' and propagate/flag the unmatched SCC.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs**: moves-nonroad crate: implement evaporative emissions (port evemfclc.f / clcevems.f / fndevefc.f and the nrevapemissionrate consumption) so EvapTechEntry tech_fractions carry real values; OR add an explicit 'evap-not-computed' marker to the output so the zero-fraction evap stub in nonroad_loader.rs:1262-1271 is not silently reported as complete zero evaporative HC.
- **crates/moves-calculators/src/calculators/nonroad_loader.rs**: moves-calculators error.rs (not edited this pass): consider adding structured variants (e.g. MissingColumn { table, column } / MissingTable) and making the nonroad_loader builder chain fallible (load_nonroad_reference -> Result<ReferenceData>, build_production_executor -> Result<...>) so the int_col/float_col/build_scrappage_curve/unit_code_for hardening can return Err instead of panicking. Cross-file: touches nonroad_emission.rs and other callers.
- **crates/moves-framework/src/data/conversions.rs**: In crates/moves-calculators/src/calculators/baseratecalculator/mod.rs, audit each iter_typed_or_empty call site and switch any REQUIRED table to iter_typed (which errors on absence). Candidates that look required (not process-90-optional): FuelSubtype (line 1053), MonthOfAnyYear (1054), DayOfAnyWeek (1466), HourDay (1471), and any others where an absent table should be a hard error rather than zero emissions. Confirm each against canonical MOVES (whether the calculator's SQL JOINs that table unconditionally).
- **crates/moves-framework/src/data/conversions.rs**: Consider adding an explicit optional-table allowlist (in moves-calculators or the schema registry) so iter_typed_or_empty can assert the passed table name is actually optional, turning a required-table miss into an error instead of a silent empty Vec.
- **crates/moves-framework/src/execution/execution_runspec.rs**: moves-runspec crate: add a doNotPerformFinalAggregation field to the RunSpec/TimeSpan model and a TOGSpeciation needsFinalAggregation hook, then wire the two false/true early-exits into should_do_final_aggregation (ExecutionRunSpec.java:1859-1866). Until then the always-true default is correct.
- **crates/moves-framework/src/execution/execution_runspec.rs**: moves-data crate: add is_affected_by_onroad / is_affected_by_nonroad metadata to PollutantProcessAssociation (or expose data-plane PPA flags), then update ExecutionRunSpec::does_have_pollutant_or_process / does_have_pollutant_and_process to apply the M1 isAffectedByOnroad and M2 isAffectedByNonroad filtering matching ExecutionRunSpec.java:844-887.
- **crates/moves-framework/src/execution/execution_runspec.rs**: moves-runspec crate: give InternalControlStrategy a discriminated type (incl. RateOfProgressStrategy with a useParameters flag), then implement ExecutionRunSpec::has_rate_of_progress by scanning run_spec.internal_control_strategies per RunSpec.hasRateOfProgress() (RunSpec.java:269-281). Also wire useRateOfProgress through the FuelEffectsGenerator sulfur/predictive paths if/when those are ported.
- **crates/moves-framework/src/execution/execution_runspec.rs**: moves-runspec crate: add RunSpec-level timespan validation (equivalent to TimeSpan.hasHours()/hasendHourGTEbeginHour(), TimeSpan.java:376-385) so a RunSpec with no begin/end hour is rejected before execution, rather than silently producing zero hours.
- **crates/moves-calculators/src/calculators/airtoxics.rs**: AirToxicsCalculator.execute now returns Err(Error::Polars(..)) when the iteration position has no year. Verify the master loop always binds a year before dispatching this calculator (it should, per ##context.year##), otherwise this surfaces a previously-silent zero-emission bug. No change needed in airtoxics.rs.
- **crates/moves-calculators/src/calculators/airtoxics.rs**: Consider adding a dedicated MissingContext / MissingData error variant to crates/moves-framework/src/error.rs so calculators can report 'iteration position has no year' more precisely than Error::Polars(String). The year guard in airtoxics.rs execute() currently reuses Polars(String) because no suitable variant exists.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs**: fuel_region_id should come from the run context (##context.fuelRegionID##), not FuelSupply.first(). Add a fuel_region_id field to moves-framework ExecutionLocation/IterationPosition (crates/moves-framework/src/execution/execution_db.rs) and populate it in the location producer, then have CriteriaStartCalculator::execute read pos.location.fuel_region_id instead of deriving it from FuelSupply.first(). Apply the same to any other calculator deriving fuel region from a table.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs**: pol_process_ids in execute is intersected with the calculator capability {102,202,302} but NOT with the RunSpec's selected pollutant/process set (canonical ##pollutantProcessIDs## = RunSpec ∩ capability). Expose the RunSpec pol-process selection through CalculatorContext (e.g. a RunContext/scale accessor) so execute can apply the full intersection. Same gap likely exists in criteria_running_calculator.rs.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs**: Sibling calculator crates/moves-calculators/src/calculators/criteria_running_calculator.rs execute() (lines ~4390-4396) uses the same unwrap_or(0) fabrication for year/county/zone/link/state context scalars; apply the same ok_or_else hardening there for fidelity with the canonical ##context.*## substitution semantics.
- **crates/moves-calculators/src/calculators/criteria_start_calculator.rs**: Module docs in criteria_start_calculator.rs (lines 120-130) are stale: they describe execute() as 'a documented shell returning an empty CalculatorOutput' but execute is now fully wired to materialise CriteriaStartInputs/RunContext and emit rows. Update the doc comment (low priority, doc-only).
- **crates/moves-calculators/src/calculators/hcspeciation.rs**: BaseRate: BRC_DiscardRegClassID (crates/moves-calculators/src/calculators/baseratecalculator/aggregate.rs:58 `key.reg_class_id = 0;`, plus adjust.rs:500 and setup.rs:3110/3127/3135) collapses regClassID to 0 in the worker output before HCSpeciation runs. Canonical MOVES discards regClassID only at the final output-aggregation step, so MOVESWorkerOutput must preserve the concrete regClassID through to speciation (hcspeciation.go keys methaneTHCRatio/HCSpeciation on fb.Key.RegClassID at lines 231/262/280). Until BaseRate preserves regClassID for the speciation input, onroad CH4/NMHC/NMOG/TOG/VOC speciation matches no ratio rows for real reg classes and produces zero. Verify end-to-end against a reference MOVES run rather than the regClass-0 unit fixture.
- **crates/moves-calculators/src/calculators/multiday_tank_vapor_venting_calculator.rs**: moves-framework/src/execution/execution_db.rs: add a `road_type_id: Option<u32>` field to `ExecutionLocation` (and update `none()/state()/county()/link()` constructors and the location producer in execution/execution_location_producer.rs that already has road_type_id at line 119) so the iteration's roadTypeRecordID is carried in IterationPosition.location.
- **crates/moves-calculators/src/calculators/multiday_tank_vapor_venting_calculator.rs**: After ExecutionLocation exposes road_type_id: in multiday_tank_vapor_venting_calculator.rs execute() (line ~5314) replace `road_type_id: 0` with `pos.location.road_type_id.map(|r| r as i32).unwrap_or(0)` to stamp the real iteration road type onto TVV-9 output rows (canonical SQL line 1978). Apply the same to liquid_leaking_calculator.rs and any other evap calculator that currently derives/hardcodes road type.
- **crates/moves-calculators/src/calculators/multiday_tank_vapor_venting_calculator.rs**: Framework-layer hardening for Finding 2 (cross-cutting, applies to all calculators including baseratecalculator/mod.rs:635 and liquid_leaking_calculator.rs:2593): add a shared helper (e.g. on IterationPosition or in crate::wiring) that returns Err / asserts when a calculator subscribed at MONTH-or-finer granularity is dispatched with a None year/month/county/zone, instead of each calculator silently defaulting to 0. Add a dedicated error variant (e.g. MissingPosition { field }) to moves-framework/src/error.rs to support this.
- **crates/moves-calculators/src/calculators/so2_calculator.rs**: The synthesis now reads the "County" table via ctx.tables(); ensure the SO2 data-plane wiring / DataFrameStore populates a County extract (GPAFract column) for the iteration county, as the SQL's County join requires. Only the non-empty SO2 generalFuelRatio path reads it.
- **crates/moves-calculators/src/calculators/so2_calculator.rs**: The GPA blend uses the County's GPAFract; the SQL also gates gfr.sourceTypeID/fuelTypeID by RunSpecSourceFuelType (SO2Calculator.sql lines 114,122-123) which this port still does not apply — a separate (pre-existing) simplification not in scope of these findings.
- **crates/moves-calculators/src/calculators/baseratecalculator/setup.rs**: crates/moves-calculators/tests/baseratecalculator.rs:427-435 constructs EvEfficiencyRow with the removed fields fuel_type_id/model_year_id; update it to the new fields (age_group_id, begin_model_year_id, end_model_year_id) and supply an AgeCategory row whose age_group_id matches age_groups[constants.year_id - model_year_id], plus a base_rate row with fuel_type_id 9, so the BRC_evefficiency path still resolves a divisor (test currently expects emission_quant 16.0 = 4/0.25).
- **crates/moves-calculators/src/calculators/baseratecalculator/setup.rs**: Optional fidelity follow-up (out of scope for this finding): TemperatureAdjustmentRow.term_a in setup.rs from_dataframe errors on SQL NULL, but the canonical Go reads tempAdjustTermA with parse.GetFloat (coalesces NULL to 0.0) and the column is nullable (MOVESDatabaseTables.md:2942). Consider changing term_a from ok_or_else(null) to unwrap_or(0.0) to match canonical, mirroring term_b.
- **crates/moves-calculators/src/generators/link_op_mode_distribution.rs**: The importer at crates/moves-importer-pdb/src/tables/drive_schedule_second_link.rs and crates/moves-framework input plane already emit the linkID column (CSV header `linkID,secondID,speed,grade`), so no cross-file edit was required for the data plane. Verify in a later integration pass that the multi-link driveScheduleSecondLink DataFrame actually populates the linkID column the generator now filters on (it is a name-only allowlist in schema_registry.rs, no per-column schema there).
- **crates/moves-calculators/src/generators/source_bin_distribution_generator.rs**: In SourceBinDistributionGenerator.execute (source_bin_distribution_generator.rs ~line 1798), the model-year range is currently derived from every row of the loaded Year table. Canonical MOVES (SourceBinDistributionGenerator.java:541, ExecutionRunSpec.java:68,224,420-422) derives min/max calendar year from the RunSpec-selected run years (ExecutionRunSpec.years), and ExecutionRunSpec.java:224 notes the Year table is intentionally NOT filtered to RunSpec years. To match canonical behavior, expose the RunSpec-selected years on CalculatorContext (crates/moves-framework/src/calculator/traits.rs) — e.g. a `run_spec_years()` accessor or a per-run years field — and pass those into `model_year_range` instead of the Year table's yearIDs. Without this, the model-year filter in aggregate_svp can span a wider window than the reference and pull in extra sampleVehiclePopulation rows.
- **crates/moves-calculators/src/generators/mesoscale_lookup/total_activity.rs**: The crate's data-plane / table-loading layer must now provide the "monthOfAnyYear" table (columns monthID, noOfDays) to MesoscaleLookupTotalActivityGenerator's execute() inputs, since it was added to INPUT_TABLES. Verify the engine/wiring that populates ctx.tables() supplies this table for mesoscale-lookup runs (it is already loaded for other calculators such as criteria_running_calculator, but that variant projects only monthID/monthGroupID, not noOfDays).
- **crates/moves-cli/src/run.rs**: crates/moves-calculators/src/generators/meteorology.rs build_meteorology_table (lines 731-737): canonical MeteorologyGenerator.doHeatIndex applies the heatIndex UPDATE (heatIndex = temperature for temp<78, the regression formula capped at 120 for temp>=78) to ALL ZoneMonthHour rows with NO join to Zone/County. The generator currently `continue`s (drops) any ZMH row whose zone has no County match, so run.rs's back-fill None branch sets heatIndex=temperature even for temp>=78 unmatched rows, which diverges from canonical for hot rows in degenerate/missing-geography cases. Consider emitting these rows from the generator with the formula-based heatIndex (using only ZMH temperature+relHumidity) and zero/default humidity, rather than dropping them, so run.rs's unmatched fallback is never needed for hot rows.
- **crates/moves-framework/src/aggregation/output_aggregate.rs**: engine.rs (crates/moves-framework/src/execution/engine.rs lines 391, 824, 898, 908): the production path passes &UnitScaling to StreamingEmissionAgg::extend / aggregate_activity / write_aggregated_activity for real onroad plans that now carry WeeksPerMonth/PortionOfWeekPerDay scaling. With this hardening those calls will return Error::AggregationPlanMismatch at runtime. Wire a real TemporalScalingFactors implementation backed by the monthOfAnyYear (weeksPerMonth) and dayOfAnyWeek (portionOfWeekPerDay) reference tables (whose supplies_temporal_factors() returns true) and pass it instead of UnitScaling. Reference factor formulas: WeeksInMonthHelper.getWeeksPerMonthSQLClause / getPortionOfWeekPerDayClause; an existing weeks_per_month helper lives at crates/moves-calculators/src/generators/totalactivitygenerator/vmt.rs:111.
- **crates/moves-framework/src/aggregation/output_aggregate.rs**: output_processor.rs tests (crates/moves-framework/src/aggregation/output_processor.rs lines ~951, ~995): write_aggregated_emissions / write_aggregated_activity are tested against Year+Nation+Onroad plans (lines 941, 988) with &UnitScaling and expect success; these will now fail with AggregationPlanMismatch. Switch those two tests to a real-factor provider (e.g. an IdentityRealFactors-style impl with supplies_temporal_factors()==true). The line ~1026 test passes an activity_plan to write_aggregated_emissions and still correctly errors on the table mismatch (no change needed).
- **crates/moves-nonroad/src/input/seasonal.rs**: read_dat (seasonal.rs lines 58-181) and read_dat_records use 1.0 as the default seasonal factor per month, but canonical rdseas.f uses defmth(i)=1.0/12.0 and renormalizes each equipment's 12 monthly factors to sum to 1 (rdseas.f lines 114-129, 216-218). If/when these parsers are wired onto the live emissions path, evaluate porting the sum-to-1 renormalization and the 1/12 default magnitude to match canonical monthly allocation.
- **crates/moves-nonroad/src/input/seasonal.rs**: read_day (seasonal.rs lines 254-289) does not error on a completely empty .DAY file, whereas rdday.f errors via `if(ncount.eq.0) goto 7004` (line 175). Consider adding an empty-file guard if read_day is placed on a live path.
- **crates/moves-calculator-info/src/chain.rs**: SubscriptionEntry.process_id is a u32 with no out-of-band sentinel for 'process not statically resolvable' (Java-source fallback path uses 0). If consumers of calculator-dag.json must distinguish an unresolved process from a real process id without parsing process_name, consider widening the v1 schema (e.g. process_id: Option<u32> or an explicit 'process_resolved: bool' / source-based gate) in chain.rs's SubscriptionEntry plus output.rs DAG_VERSION bump — a coordinated cross-file change not made here.
- **crates/moves-calculators/src/calculators/criteria_running_calculator.rs**: The audit noted 'Same pattern exists in the sibling criteria_start_calculator.rs execute().' On inspection, criteria_start_calculator.rs:3670 ALREADY uses `pos.time.year.map(|y| y as i32).ok_or_else(|| missing("year"))?`, so that sibling is already hardened — no change needed there. (Worth a quick confirm that its county/zone/link/state, if it carries them in RunContext, are likewise guarded.)
- **crates/moves-calculators/src/calculators/liquid_leaking_calculator.rs**: INVESTIGATE the repo-wide `/noOfRealDays` divisor in the evap emission calculators (liquid_leaking_calculator.rs line 974/977, tank_vapor_venting_calculator.rs line 3746/3749, multiday_tank_vapor_venting_calculator.rs line 5042/5045). Canonical LiquidLeakingCalculator.sql LL-9 (line 387) and TankVaporVentingCalculator.sql (line 705) compute emissionQuant = weightedMeanBaseRate*sourceHours*opModeFraction with NO division by noOfRealDays, and SourceHours already includes noOfRealDays (SHP = population*noOfRealDays - SUM(sho)). The port's divisor under-counts these emissions by a factor of noOfRealDays vs canonical (e.g. weekday/5, weekend/2) unless a downstream consumer re-multiplies. Confirm whether this is an intentional per-day output contract (and where it is re-aggregated) or a genuine fidelity bug; if a bug, the divisor must be removed in all three calculators consistently.
- **crates/moves-calculators/src/calculators/liquid_leaking_calculator.rs**: Apply the same iteration-position hardening (replace `pos.time.year.map(...).unwrap_or(0)` and the location-field `unwrap_or(0)` with `ok_or_else(|| Error::InvalidBundle(...))?`) in multiday_tank_vapor_venting_calculator.rs execute() (lines 5308-5313) and any other emission calculator using the same silent `unwrap_or(0)` pattern when stamping output location/year, for consistency with the fix now in liquid_leaking_calculator.rs. Consider adding a dedicated framework Error variant (e.g. MissingIterationField) to moves-framework/src/error.rs instead of reusing InvalidBundle.
- **crates/moves-calculators/src/calculators/nonroad_emission.rs**: The framework master loop has no equivalent of canonical doesProcessContext (an IContextFilter) for NonroadEmissionCalculator. The adapter now self-filters inside execute(), but if/when an IContextFilter-style pre-dispatch hook is added to moves-framework's calculator dispatch, this year/month/day guard should move there to match canonical's override-the-execution-hierarchy semantics (and to avoid building inputs for skipped contexts).
- **crates/moves-calculators/src/calculators/nonroad_emission.rs**: Consider adding a dedicated Error variant (e.g. Error::MissingExecutionTable { table }) to moves-framework/src/error.rs; the FINDING 1 hardening currently reuses Error::Nonroad(String) for the missing-population-table case, which is suitable but less specific than a dedicated missing-table variant.
- **crates/moves-calculators/src/calculators/baseratecalculator/mod.rs**: If a fully unconditional fail-fast for the missing-county invariant is desired (exact mirror of baseratecalculator.go's nil-pointer panic, independent of whether ratio tables are non-empty): change BaseRateCalculator::run and run_with_prepared in crates/moves-calculators/src/calculators/baseratecalculator/mod.rs to return moves_framework::Result<BaseRateCalculatorOutput>, return Err on the missing county, and update the 12+ call sites in crates/moves-calculators/tests/baseratecalculator.rs (which currently bind `let output = BaseRateCalculator::run(..)` and read output.blocks) to unwrap the Result. That test file is outside the assigned file and was not edited.
- **crates/moves-calculators/src/calculators/baseratecalculator/adjust.rs**: In crates/moves-calculators/src/calculators/baseratecalculator/mod.rs and adjust.rs: thread a `panic_on_missing_supply: bool` through build_fuel_blocks (adjust.rs) and process_pass (mod.rs), set false for the base_rate_by_age pass and true for the base_rate pass at run_with_prepared (mod.rs:536-539). In build_fuel_blocks, replace the unconditional `let Some(fuel_supply) = fuel_supply else { continue; };` so that when panic_on_missing_supply is true it panic!s like the Go streamBaseRate (baseratecalculator.go:942): panic message "Could not load fuel supply detail for county {county_id} in {month_id}/{year_id} for fuel type {fuel_type_id}" (note Go prints month_id before year_id), and otherwise continues (drops the row) for the by-age pass.
- **crates/moves-calculators/src/generators/avg_speed_op_mode_distribution.rs**: Sibling generators carry the identical incorrect 'MOVES leaves avgBinSpeed NULL (default 0.0)' comment plus `avg_bin_speed: ...unwrap_or(0.0)` read-back and should get the same hardening (error on NULL): crates/moves-calculators/src/generators/sourcetypephysics.rs line 684, crates/moves-calculators/src/generators/rates_op_mode_distribution.rs line 702, crates/moves-calculators/src/generators/baserategenerator/mod.rs line 1089. (rates_op_mode_distribution and sourcetypephysics may warrant per-file verification since those rows are hotelling/off-network with avgBinSpeed=0 by construction, but the read-back NULL coercion is still the same hazard.)
- **crates/moves-calculators/src/generators/project_tag.rs**: Add TableRow implementations (and schema_registry entries) for the project-domain input row types LinkRow ('link'), LinkSourceTypeHourRow ('linkSourceTypeHour'), SourceTypeAgeDistributionRow, HourDayRow, RunSpecHourDayRow, RunSpecMonthRow, DayOfAnyWeekRow, OffNetworkLinkRow ('offNetworkLink'), AvftRow ('avft'), and for the output rows ShoRow/ShpRow/StartsRow/HotellingRow/SourceHoursRow, in crates/moves-framework/src/data/conversions.rs + schema_registry.rs (or wherever the calculator TableRow impls live).
- **crates/moves-calculators/src/generators/project_tag.rs**: Once those TableRow/schema entries exist, wire ProjectTAG::execute in crates/moves-calculators/src/generators/project_tag.rs to: read year/is_rates/process from ctx.position(); project ProjectTagInputs from ctx.tables() via iter_typed/iter_typed_or_empty; call allocate_total_activity_basis(ctx.position().process_id, &inputs, ...); and write the five output Vecs to ctx.scratch_mut().store under 'SHO','SHP','Starts','hotellingHours','sourceHours' (mirroring TotalActivityGenerator::execute's write_scratch! pattern). Replace the Error::NotImplemented return and update the execute-level test to assert the scratch tables are populated.
- **crates/moves-calculators/src/generators/tank_temperature_generator.rs**: None required: the three user-input override tables (AverageTankTemperature, SoakActivityFraction, ColdSoakInitialHourFraction) are already registered as MergeTableSpec entries in crates/moves-framework/src/input/input_data_manager.rs (lines 1096-1098, 1106-1109, 1389), so the merged execution-DB rows are available to ctx.tables(). If end-to-end validation shows the merged isUserInput column arrives as a MySQL CHAR 'Y'/'N' string rather than Boolean, verify that the conversions.rs iter_typed cast-to-schema path (Boolean) handles 'Y'/'N' correctly — but that is a shared concern for every isUserInput table, not specific to this generator.
- **crates/moves-default-db-convert/src/validate.rs**: Consider adding a unit test in validate.rs covering the missing-TSV-on-a-populated-table path (now a RowContentError) to lock in the non-zero exit; not added here to keep the edit surgical and because the test module already exists.
- **crates/moves-framework/src/execution/engine.rs**: crates/moves-runspec/src/model.rs: extend OutputBreakdown with reg_class_id/fuel_sub_type/eng_tech_id bool fields (canonical OutputEmissionsBreakdownSelection.regClassID/fuelSubType/engTechID).
- **crates/moves-framework/src/execution/engine.rs**: crates/moves-runspec/src/xml_format.rs: add regclassid/fuelsubtype/engtechid to XmlOutputBreakdown read/write (RunSpecXML.java:1175-1219) and map them into OutputBreakdown.
- **crates/moves-framework/src/execution/engine.rs**: crates/moves-framework/src/execution/engine.rs aggregation_inputs_from_run_spec: once the above land, wire reg_class_id/fuel_sub_type/eng_tech_id from run_spec.output_breakdown instead of the constant false.
- **crates/moves-framework/src/input/input_data_manager.rs**: Implement the two-phase default-DB merge so fuel_years and region_ids are derived per ExecutionRunSpec.java:217-251 (initializeAfterShallowTables). Phase 1: run InputDataManager::plan/execute restricted to the `Year` and `regionCounty` tables (with the existing year/county filters). Phase 2: from the loaded execution store derive fuel_years = DISTINCT fuelYearID from `Year` where yearID in runspec.years, and region_ids = {0} UNION DISTINCT regionID from `regionCounty`; populate RunSpecFilters.fuel_years / region_ids; then re-plan/execute the remaining tables. Caller to update: crates/moves-cli/src/run.rs build_default_db_store (~lines 1313-1316), which currently does a single plan->execute with empty fuel_years/region_ids. This narrows FuelSupply/AverageTankGasoline/FuelSupplyYear/e10FuelProperties/fuelUsageFraction/regionCounty on fuelYearID and FuelSupply.fuelRegionID/e10FuelProperties.fuelRegionID on region, matching canonical so FuelEffectsGenerator (FuelEffectsGenerator.java:4294) does not ingest wrong-region/year fuel formulations.
- **crates/moves-importer-pdb/src/validate.rs**: No cross-file changes required. The new validate_op_mode_distribution fraction-sum check is now stricter; verify any caller passing OpModeDistribution batches still expects validation to fail on non-summing fractions (this is the intended canonical behavior).
- **crates/moves-nonroad/src/geography/prcnat.rs**: crates/moves-nonroad/src/geography/prcus.rs: add `pub bsfc: f32` field to the shared `ExhaustResult` struct (lines 86-92) so the brake-specific fuel-consumption factor used by clcems/emfclc can be returned to callers.
- **crates/moves-nonroad/src/geography/prcnat.rs**: crates/moves-nonroad/src/simulation/executor.rs: in the live `calculate_exhaust` impl(s), set the new `ExhaustResult.bsfc` to the scalar already computed at ~line 1133 (`factors.bsfc[year_index * n_tech + tech_index]`). Update the other ExhaustResult constructions (prcus.rs:889/1077, prcnat.rs test stub:891, state.rs:1196) to initialize the new field (e.g. 0.0 in test stubs).
- **crates/moves-nonroad/src/geography/prcnat.rs**: crates/moves-nonroad/src/geography/prcnat.rs (this file, AFTER the struct/executor changes land): multiply the fulbmy load term by `er.bsfc`, i.e. change `(hpval * activity.load_factor / denful.max(f32::MIN_POSITIVE))` at lines ~517-523 to `(hpval * activity.load_factor * er.bsfc / denful.max(f32::MIN_POSITIVE))`, matching prcnat.f :723-726 and process.rs:620-633.
- **crates/moves-nonroad/src/geography/prcnat.rs**: crates/moves-nonroad/src/geography/prcus.rs: apply the identical bsfc fix to prcus.rs's fulbmy (the finding notes prcus.rs:517-523 has the same missing-bsfc defect).
- **crates/moves-nonroad/src/geography/process.rs**: Consider adding a dedicated WarningKind/GeographyWarning variant (e.g. MissingExhaustTech/MissingEvapTech with model-year field) for the per-year tech-miss case rather than reusing MissingTechType, to match the richer GeographyWarning::MissingExhaustTech{scc,hp_avg,year} used by prcnat.rs/prcus.rs/state.rs — would require editing common.rs (WarningKind enum) and possibly the warning-aggregation in the executor/output layers.
- **crates/moves-nonroad/src/geography/process.rs**: If a more specific error variant than Error::Config is desired for the bsfc out-of-bounds shape-mismatch (an internal invariant rather than a config error), add one in crates/moves-nonroad/src/error.rs and update this call site.
- **crates/moves-nonroad/src/population/growth.rs**: characterization/nonroad-fidelity/README.md line 218 (GRWFAC row) still documents the zero-base-indicator finite-vs-Inf behavior as a known divergence ('The port divides by the MINGRWIND clamp...'). That divergence has now been removed -- the port matches the Fortran's growthyearind/0 -> +/-Inf. Update or delete that GRWFAC divergence row, and reconsider the note about needing a known-divergence allowlist for this path.
- **crates/moves-wasm/src/lib.rs**: Wire ProductionExecutor into the WASM run_nonroad_simulation path (crates/moves-wasm/src/lib.rs ~line 720) once a ReferenceData bundle can be assembled. This requires porting the NOT-YET-LOADABLE NONROAD reference-data loaders in crates/moves-nonroad/src/simulation/executor.rs (temporal-factor NR*.TMF, emission-factor NR*.EMF, spillage NR*.SPL, subcounty/national allocation NR*.SCO/.ALO) and constructing ReferenceData from browser-supplied bytes, then replacing PlanRecordingExecutor with ProductionExecutor::new(&ref_data) so emissions are actually computed. The same wiring is needed in the native CLI crates/moves-nonroad/src/main.rs, which is currently also an empty/dry run.
- **crates/moves-wasm/src/lib.rs**: Confirm the .POP usage field (PopulationRecord.usage) is the medianLifeFullLoad in hours-at-full-load for the equipment types in the WASM demo inputs; if a future WASM path instead carries median life via a separate source-use-type table (MOVES nrsourceusetype.medianLifeFullLoad), prefer that table value over the .POP usage column when both are present.
- **crates/moves-calculators/src/calculators/nrairtoxics.rs**: Verify the moves.rs data plane actually emits fuelSubTypeID and fuelFormulationID columns on the nonroad MOVESWorkerOutput consumed by NRAirToxicsCalculator (i.e. that the NRFuelSupply market-share expansion from mworeader.go parseLines lines 514-530 is performed upstream). nrhcspeciation.rs already depends on these columns from the same table, so they are expected to be present, but if the nonroad worker-output producer does not yet split rows by NRFuelSupply, that splitting must be implemented there or NRAirToxicsCalculator's from_dataframe will now error on the missing columns.
- **crates/moves-calculators/src/calculators/nh3/running.rs**: crates/moves-calculators/src/calculators/nh3/start.rs lines 705-709: the NH3 start-exhaust execute() has the identical `.unwrap_or(0)` defect for year_id/state_id/county_id/link_id and should be hardened the same way (ok_or_else with row_err), per database/NH3StartCalculator.sql which uses the same ##context.year## / ##context.iterLocation.*## macros.
- **crates/moves-calculators/src/calculators/welltopump/ch4n2o.rs**: total_energy.rs (crates/moves-calculators/src/calculators/welltopump/total_energy.rs:296-301) has the identical fabricated `target_year = ctx.position().time.year ... unwrap_or_else(|| year.first().map(|y| y.year_id).unwrap_or(0))` pattern; harden it the same way (propagate via Error::RowExtraction keyed to 'IterationPosition') and fix its execute-test to supply ExecutionTime::year.
- **crates/moves-calculators/src/calculators/welltopump/ch4n2o.rs**: co2_atmospheric.rs (crates/moves-calculators/src/calculators/welltopump/co2_atmospheric.rs:373-378) has the identical fabricated target_year fallback; harden the same way and update its execute-test.
- **crates/moves-calculators/src/calculators/welltopump/ch4n2o.rs**: co2_equivalent.rs (crates/moves-calculators/src/calculators/welltopump/co2_equivalent.rs) — verify whether its execute derives target_year the same way; if so apply the same hardening.
- **crates/moves-calculators/src/calculators/welltopump/total_energy.rs**: crates/moves-calculators/src/calculators/welltopump/co2_atmospheric.rs has the identical fabricated-default pattern at its execute() (around lines 372-378): `ctx.position().time.year.map(i32::from).unwrap_or_else(|| year.first().map(|y| y.year_id).unwrap_or(0))`. Apply the same hardening (replace with `.ok_or_else(|| Error::RowExtraction{...})?`) and update its corresponding execute test to supply a context year via with_position_and_tables. Note co2_atmospheric.sql joins greetwelltopump on `gwtp.yearID = ##context.year##` directly (no interpolation), so the same canonical rationale applies.
- **crates/moves-calculators/src/generators/baserategenerator/mod.rs**: In crates/moves-framework/src/calculator/traits.rs: add to CalculatorContext a worker-parameters accessor (e.g. `parameters(&self) -> &[String]` or `external_flags`) plus an `is_project(&self) -> bool` (or `domain(&self) -> Option<ModelDomain>`) accessor, with corresponding fields and `set_*` setters mirroring `set_model_scale`.
- **crates/moves-calculators/src/generators/baserategenerator/mod.rs**: In crates/moves-framework/src/execution/engine.rs (around line 686-687 where `ctx.set_model_scale(run_scale)` is called per chunk): populate the new context fields from the run spec — set the `-parameters=` flag list MOVES would pass to the BaseRateGenerator worker (derived from the run's rates/aggregation output settings the way the Java master builds them) and set is_project = (run_spec.domain == Some(ModelDomain::Project)).
- **crates/moves-calculators/src/generators/baserategenerator/mod.rs**: In crates/moves-calculators/src/generators/baserategenerator/mod.rs execute() (lines 215-265): once the accessors exist, replace the hardcoded `ExternalFlags { process_id, year_id, ..default() }` with `ExternalFlags::from_parameters(ctx.parameters())` (overriding/confirming process_id and year_id from the iteration position as needed) and replace `is_project: false` with `ctx.is_project()`. Map any `crate::error::Error::Parameters` from from_parameters into a `moves_framework::Error` variant (it currently returns the moves-calculators crate Error, which differs from execute()'s `moves_framework::Error` return type).
- **crates/moves-calculators/tests/calculator_integration.rs**: Wire the produced-output (data plane) side so canonical_snapshots_dormant_or_validate can run the real numeric diff: CalculatorContext must expose per-fixture row storage and Calculator::execute must return populated CalculatorOutput instead of CalculatorOutput::empty() (moves-framework + moves-calculators crates). Once produced tables exist, replace the hardened hard-failure branch in calculator_integration.rs with a call to compare::validate_table / compare::compare_table per fixture and assert ValidationStatus::Matched within the tolerance budget.
- **crates/moves-calculators/tests/calculator_integration.rs**: Populate characterization/snapshots/<fixture>/ with canonical-MOVES captures (the 'pending compute-node run' inputs) so the gate has a left/expected side to diff against; landing these BEFORE the data-plane wiring will now correctly trip the hardened test, which is the intended forcing function.
- **crates/moves-cli/tests/concurrency_correctness.rs**: When the data plane is wired (calculators emit real per-bucket emissionQuant rows), strengthen crates/moves-cli/tests/concurrency_correctness.rs to compare the real emission output Parquet (e.g. MOVESOutput.parquet) byte-identically across parallelism settings, and remove the temporary !any_fully_implemented guard that currently forces a failure when a run becomes fully implemented.
- **crates/moves-cli/tests/perf_baseline.rs**: Once the data plane is wired in (calculators emit real rows instead of CalculatorOutput::empty()), revisit perf_baseline.rs: tighten WALL_TIME_LIMIT to a value comparable to the canonical-MOVES reference numbers in docs/performance-baseline.md, and update the module docstring's 'pre-data-plane' section (lines 9-16) so the test becomes a true performance/correctness baseline rather than a framework-overhead smoke test.
- **crates/moves-cli/tests/end_to_end.rs**: crates/moves-cli/tests/end_to_end.rs lines 95-117: once the no-store/default data plane is wired so that calculators run on the no-snapshot path (i.e. run_simulation registers calculators even when has_slow_store is false, or the headline test is switched to drive a snapshot/default_db like full_suite_regression.rs does), invert the empty-state assertions: replace assert!(outcome.modules_executed.is_empty()) / assert_eq!(modules_unimplemented, modules_planned) / assert!(!is_fully_implemented()) / assert_eq!(rows, 1) with assertions that BaseRateCalculator executed and that MOVESRun.parquet/emission output carries real rows, so the test stops locking in the empty-output state. This requires a production-side change in crates/moves-cli/src/run.rs (registry wiring on the no-store path), so it cannot be done by editing the test alone.
- **crates/moves-importer/src/filter.rs**: crates/moves-importer/src/reader.rs and/or src/validator.rs: when a column carries Filter::NonNegativeDefault1, substitute 1.0 for any null/blank cell before writing to Parquet (matching canonical FILTER_NON_NEGATIVE_DEFAULT_1 value set {1.0}). Without this, a blank idleMonthAdjust/idleDayAdjust/monthAdjustment cell is emitted as NULL instead of the canonical 1.0 multiplier, corrupting downstream Idle/Hotelling adjustment math. This is currently latent because no importer descriptor adopts the filter yet.
- **crates/moves-nonroad/src/main.rs**: When the disk-side CLI wrapper is implemented in crates/moves-nonroad/src/main.rs, it must: (1) read the .opt file path from std::env::args and error if missing (mirrors nonroad.f:92 -> 7002), (2) parse it into NonroadOptions and load the fixed-width input files into NonroadInputs, (3) use the ProductionExecutor (exported from moves_nonroad::simulation) instead of PlanRecordingExecutor, (4) emit the canonical no-population-data ERROR rather than success when getpop finds nothing (nonroad.f:146 -> 7000), and only then print the "Successful completion" banner with a zero exit code. The parser/loader and ProductionExecutor wiring live in other modules (input/, simulation/) and were out of scope for this single-file pass.
- **crates/moves-nonroad-retrofit/src/control_strategy.rs**: crates/moves-control-strategy-validation/tests/nonroad_retrofit_validation.rs: test `pre_run_succeeds_with_multiple_records` (lines ~109-118) calls strategy.pre_run(...).expect("pre_run must succeed") on a populated NonRoadRetrofitStrategy. It will now Err. Update it to expect_err and assert Error::Nonroad (mirror the in-crate `pre_run_fails_with_populated_records` test).
- **crates/moves-nonroad-retrofit/src/control_strategy.rs**: crates/moves-control-strategy-validation/tests/cross_strategy.rs: `combined_pre_run` test (lines ~273, 341-347) registers a populated NonRoadRetrofitStrategy and unwraps pre_run for all strategies. With the new guard this will panic. Either build that strategy with empty records, or stop calling pre_run on it / handle the expected Err.
- **crates/moves-nonroad-retrofit/src/control_strategy.rs**: Real fix (separate, larger change spanning moves-framework + moves-nonroad): add a framework write API so NonRoadRetrofitStrategy::pre_run can feed its records into moves_nonroad ReferenceData::retrofit_records (the field read by the live calculator). Once that exists, replace the guard in control_strategy.rs pre_run with the actual write and allow non-empty strategies to succeed.
- **/Users/ctessum/code/earthsciml/moves.rs/crates/moves-nonroad/src/geography/prcus.rs**: Add `bsfc: f32` field to ExhaustResult (crates/moves-nonroad/src/geography/prcus.rs:85-92), documented as the emfclc brake-specific fuel-consumption factor bsfc(idxyr, tech) for this (model_year, tech) tuple.
- **/Users/ctessum/code/earthsciml/moves.rs/crates/moves-nonroad/src/geography/prcus.rs**: In crates/moves-nonroad/src/geography/prcus.rs:616-622, replace the literal `1.0` in the fulbmy formula with `er.bsfc` (the ExhaustResult.bsfc returned by calculate_exhaust), and delete the placeholder comment at lines 623-625. Also update the two in-file test ExhaustResult literals (prcus.rs:889, 1077) to set a representative bsfc.
- **/Users/ctessum/code/earthsciml/moves.rs/crates/moves-nonroad/src/geography/prcus.rs**: In crates/moves-nonroad/src/geography/prcnat.rs:554-560, multiply the fulbmy formula by er.bsfc (currently bsfc is omitted entirely — same divergence vs prcnat.f), and add bsfc to the ExhaustResult literal at prcnat.rs:931.
- **/Users/ctessum/code/earthsciml/moves.rs/crates/moves-nonroad/src/geography/prcus.rs**: In crates/moves-nonroad/src/geography/state.rs:1196, add bsfc to the ExhaustResult struct literal (test helper) once the field exists.
- **/Users/ctessum/code/earthsciml/moves.rs/crates/moves-nonroad/src/geography/prcus.rs**: In crates/moves-nonroad/src/simulation/executor.rs, once the US-total/national exhaust EF tables are loadable, have each UsTotalCallbacks::calculate_exhaust impl return the bsfc(idxyr, tech) it computes via emfclc in ExhaustResult.bsfc (mirroring ExhaustFactorsLookup.bsfc used by the county/subcounty GeographyCallbacks path at process.rs:645).
- **crates/moves-onroad-retrofit/src/control_strategy.rs**: crates/moves-control-strategy-validation/tests/onroad_retrofit_validation.rs: test strategy_lifecycle_with_fixture_programs (lines 219-232) builds a POPULATED RetrofitTable and asserts strategy.pre_run(&mut store).expect('pre_run must succeed'); this now fails because pre_run returns Err(NotImplemented) for non-empty programs. Update it to .expect_err / assert matches!(err, Error::NotImplemented) (or skip the pre_run assertion) to match the hardened contract.
- **crates/moves-onroad-retrofit/src/control_strategy.rs**: Port the OnRoadRetrofit data plane so pre_run can actually write the EmissionRateAdjustment table (replacing the NotImplemented guard). This requires cross-file work: (a) give RetrofitRecord the missing key columns regClassID/fuelTypeID/polProcessID (crates/moves-onroad-retrofit/src/model.rs) consistent with database/OnRoadRetrofitImporter.sql and the Java DBLine; (b) reconcile the factor semantics — canonical Java SUMS overlapping programs' fractions (retrofitFactor + nonRetrofitFactor) whereas RetrofitTable::combined_factor multiplies per-program factors (crates/moves-onroad-retrofit/src/model.rs:198); (c) pass the run's analysis years / model-year universe into pre_run or a subscription (framework: crates/moves-framework/src/control_strategy/traits.rs InternalControlStrategy::pre_run signature); (d) add a polars + shared TableRow dependency to crates/moves-onroad-retrofit/Cargo.toml so an EmissionRateAdjustment DataFrame can be inserted, matching the schema BaseRateCalculator reads (crates/moves-calculators/.../baseratecalculator/setup.rs EmissionRateAdjustmentRow).
- **crates/moves-onroad-retrofit/src/control_strategy.rs**: Fix the table-name casing mismatch: OnRoadRetrofitStrategy::modified_tables() declares lowercase 'emissionRateAdjustment' (crates/moves-onroad-retrofit/src/control_strategy.rs:73) but the Base Rate Calculator reads the table via tables.iter_typed('EmissionRateAdjustment') (capital E; crates/moves-calculators/src/calculators/baseratecalculator/mod.rs:691,96). When the data plane is ported, the written table name and modified_tables() must use the same casing the calculator reads, or the adjustment will never be consumed. (Also touches the external test strategy_modified_tables_contains_emission_rate_adjustment which asserts the lowercase string.)
- **crates/moves-rate-of-progress/src/control_strategy.rs**: The crate's data model (crates/moves-rate-of-progress/src/model.rs: RopRecord with reductionFraction keyed by pollutantID/sourceTypeID/regClassID/modelYearID, plus emission_scale_factor/scale_factor) and its csv_io parser do not correspond to any field in canonical MOVES RateOfProgress, which uses a single useParameters boolean and the spDoRateOfProgress SQL. This whole data model likely needs redesign (or removal) to match canonical behavior; assess separately.
- **crates/moves-rate-of-progress/src/control_strategy.rs**: A faithful port of RateOfProgressControlStrategy::pre_run requires the moves-framework / execution-DB data plane to expose default-DB tables (modelYearCutPoints, modelYearGroup, year, and the ~25 rate/fuel/ratio tables) and a model-year-group decode step so spDoRateOfProgress can be reimplemented; track as a framework data-plane work item.
- **crates/moves-nonroad/tests/nonroad_fidelity.rs**: When the NONROAD fixture-data loaders (NR*.ACT, NR*.GRW, …) are ported and NonroadInputs is populated for fixtures in reference_corpus_validates_when_present, replace the eprintln of report.summary() at crates/moves-nonroad/tests/nonroad_fidelity.rs:348-352 with `assert!(report.passed(), "...")` (or an explicit, justified per-fixture divergence budget) so a numerical fidelity regression fails the test. Also update the now-stale contract note in crates/moves-nonroad/tests/fidelity/mod.rs:40-44.
