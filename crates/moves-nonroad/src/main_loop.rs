//! Main driver loop — entry point and SCC × geography × year iteration.
//!
//! Covers `nonroad.f`, `dayloop.f`, `daymthf.f`, `dispit.f`.

pub fn run_driver(_ctx: &mut crate::common::NonroadContext) {
    // TODO(Task 113): Port main driver loop.
}
